# FOUNDATION TARGET DISCOVERY V1 — prospective scientific contract

Status:
`PROSPECTIVE_FOUNDATION_TARGET_DISCOVERY_V1__NO_TRAINING_AUTHORITY`

## 1. Scientific question

Before scientific F1, Production Teacher/Student V2, the full foundation run, or production D1 can be interpreted, determine:

**Does the complete lawful pathology-blind reader-fit corpus contain a molecular target or small target family that is recurrent, stable, distinguishable from measurement noise, inferable from complementary evidence, and transferable across biological/acquisition units?**

This lane may return NO TARGET.

## 2. Lawful population

Primary production discovery population:
- reader_fit cells: 4,553,407
- reader_fit donors: 104
- operators: 42
- donor×operator pairs: 1,400
- HVS: 198,718 cells
- NPH52: 236,476 cells
- SEA_AD: 4,118,213 cells
- Molecular Ledger addresses: 41,238 where expression/support is required

Primary independent biological unit:
**donor**

Cell count is not independent replication.

## 3. Protected populations remain closed

Target discovery must not open or use:
- reader_validation
- reader_oracle
- foundation development
- foundation sealed holdout
- external holdout
- pathology labels/outcomes
- disease labels

Historical aggregate reports that already exist may be read as project history but may not choose/rank the new target.

## 4. Files that are NOT full-real production target authority

The local archive:
`FOUNDATION_DISCOVERY_EXPRESSION_41K_LOG1P10K.zip`
SHA-256:
`63239898b9c93f29c20b62b84dc9b94c2c87e3e3f2b7958b7435847e3b9541f7`

is the previously frozen real discovery archive/pilot material, not a replacement for the complete 4.553M-cell reader-fit population.

Likewise, do not derive production target values from:
- historical 4,540-cell biology cohort;
- old 3,292-cell training cache;
- synthetic D1 fixtures;
- u0 or historical u10–u205 checkpoints.

Claude has direct access to the larger real data. The first step is to bind those exact full-fit sources and prove the execution really reads all lawful rows.

## 5. Observation operator principle

Represent acquisition/technology as an observation process, not as unrestricted biological identity.

Permissible observation descriptors include documented physical/experimental facts:
- scRNA vs snRNA
- platform/chemistry
- measured vocabulary/support
- sequencing depth
- detected-gene characteristics
- count-split/noise characteristics
- other documented acquisition variables

Do not use unrestricted:
- donor-ID embedding
- arbitrary matrix-ID embedding
- dataset-ID embedding

Do not optimize “technology invariance” globally. The question is:

**After conditioning on comparable biology, how much unnecessary acquisition information remains?**

Preserve legitimate donor/cell-state/tissue-context biology.

## 6. Observation-state semantics

Never collapse:
- measured scalar/nonzero
- measured zero
- structurally unmeasured
- unresolved collision where applicable

A structural zero is not a measured zero.

All target construction and context masking must be support-aware.

## 7. Candidate target families

### Family A — gene/query baseline
Use exact target withholding to measure whether individual molecular addresses are identifiable from complementary evidence.

This is a baseline, not the presumed final target.

### Family B — donor-recurrent molecular programs/modules
Derive program/module targets from the full lawful reader-fit population under donor-primary weighting.

Candidate modules must:
- recur across donors;
- have support across acquisition contexts where estimable;
- survive donor-block resampling;
- not be defined primarily by support/depth/operator/source.

### Family C — stable biological subspace/block targets
Derive a global molecular basis from full real data.

Dimension is data-derived, not 160.

Audit:
- eigenspectrum/null separation;
- donor-block subspace stability;
- principal angles;
- canonical correlations;
- Procrustes alignment;
- coordinate stability;
- eigengap/degeneracy structure.

If axes rotate inside a stable subspace, the target is the stable block/subspace—not arbitrarily named axes.

### Family D — innovation/residual candidate family
Only after measurement/support/common-structure decomposition.

Hard-to-predict residuals are not automatically useful biology. They must first prove:
- measurement reliability;
- donor recurrence;
- transfer;
- stability.

### Family E — local/context target
Only if real data independently establish a stable local-state object.

Expression-kNN must never be called physical adjacency.

## 8. TD0 — full-real data binding

Before candidate scoring, produce:
`TD0_FULL_REAL_DATA_AUTHORITY.json`

It must contain:
- exact data paths/assets;
- file/package roots;
- reader/loader source digest;
- normalization contract;
- row locator schema;
- donor/operator/source/study/technology columns;
- observation-state authority;
- exact row counts by donor/operator/source;
- protected-data non-access proof;
- execution code digests.

The 4,553,407-row count must be independently reconstructed, not copied from prose.

If the full lawful input cannot be proved:
`STOP_TARGET_DISCOVERY_FULL_REAL_INPUT_NOT_BOUND`

## 9. TD1 — measurement/noise floor

Establish how much target variation can arise from measurement alone.

Preferred when raw counts exist:
- deterministic count thinning/splitting of the same cell;
- depth-response curves;
- within-cell reliability.

Do not call independent cells technical replicates.

If raw counts/count-level information are unavailable, state exactly what is NOT_MEASURABLE. Model-based Poisson/multinomial sensitivity may be reported but is not equivalent to empirical remeasurement.

A candidate cannot receive final target promotion if measurement reliability of its defining signal is unresolved.

## 10. TD2 — full-real donor-balanced structure

Derive candidate programs/subspaces out-of-core from all lawful fit rows.

Primary weighting:
- donor is the biological unit;
- within-donor cells must not make large donors dominate;
- source/operator/study sensitivity must be reported.

Do not silently inherit:
- D=160
- 50 PCs
- rank 320
- sketch dimension 512
- k={15,30,60,120}
- historical bootstrap counts
- 5% tails
- old community resolutions.

Approximation/sketch dimensions used for computation must be prospective numerical-analysis parameters with convergence/error checks, not biological dimensions.

## 11. TD3 — target existence and recurrence

For every candidate report:
- total variation;
- measurement-attributable variation;
- recurrent biological variation;
- donor recurrence;
- maximum donor share;
- source/operator/study/technology contributions;
- continuous uncertainty.

Use donor-block resampling and structure-preserving nulls.

A target confined to a few donors is not promoted.

## 12. TD4 — complementary-evidence identifiability

This is the central qualification.

Before JEPA training, use transparent cross-fitted non-neural baselines such as:
- ridge regression;
- reduced-rank regression;
- similarly auditable models.

These baselines do not become production architecture.

Evaluation must be donor-grouped.

For each target compare:
A. molecular-context predictor;
B. measurement/support/depth/operator/study-only shortcut predictor;
C. matched/structure-preserving null;
D. leakage-positive control.

A target is useful only when molecular context provides reproducible information beyond B and C and no leakage is present.

High R² alone does not qualify the target.

## 13. TD5 — stability

Use donor-block and leave-donor-group-out refits.

Report:
- principal-angle distributions;
- canonical correlations;
- Procrustes similarity;
- coordinate sign/order stability;
- subspace/block stability;
- module membership stability.

The historical Stage81A3 four-resample failure is a specific attack to beat.

Do not define a universal numerical stability cutoff by copying an old threshold. Derive uncertainty/null behavior under a prospective procedure.

## 14. TD6 — evidence-response curve

For each surviving candidate, freeze an outcome-independent evidence-fraction grid before scoring.

Measure target inferability as legal molecular evidence increases.

Do not make 40% or 60% special.

The curve should establish:
- monotone/informative convergence where scientifically expected;
- evidence efficiency;
- whether apparent performance is driven by a small leakage-like subset.

## 15. TD7 — measurement/depth-response curve

Separately vary measurement quality for the same underlying cell where lawful count information allows it.

This must remain distinct from TD6.

Goal:
separate
- biological information uncertainty
from
- measurement uncertainty.

## 16. TD8 — held-out transfer within reader_fit

Use deterministic cross-fitting within the 104 reader-fit donors.

Increasingly difficult tests:
1. held-out donor;
2. held-out matrix/operator where estimable;
3. held-out dataset/study family where estimable;
4. held-out technology where estimable.

Do not open reader_validation/oracle to make a test estimable.

If source/technology is perfectly confounded so a test cannot be identified, return NOT_MEASURABLE rather than inventing a bridge.

## 17. TD9 — conditional acquisition imprint

For surviving targets, quantify source/operator/study/technology information after conditioning on comparable biological target state.

Do not demand zero acquisition information.

Reject candidates whose claimed biology is primarily an acquisition/support/depth object.

## 18. TD10 — promotion rule

Do not select a winner using one weighted composite score.

Preserve all candidates and the Pareto frontier.

A candidate can be promoted to independent review only if:
- target signal exists above measurement/null structure;
- donor recurrence is broad;
- definition is stable;
- molecular context predicts it beyond measurement shortcuts and matched null;
- leakage attacks fail;
- held-out donor transfer succeeds;
- harder transfer is acceptable or explicitly NOT_MEASURABLE;
- acquisition effects are not the primary signal;
- pathology/protected data were never used.

If several candidates survive, freeze the small Pareto set for independent review.

If none survive:
`STOP_FOUNDATION_TARGET_NOT_ESTABLISHED__DO_NOT_TRAIN`

## 19. Required adversarial attacks

At minimum:
1. leak target values into context → detection/STOP;
2. fail to withhold target genes/coordinates → detection/STOP;
3. randomize donor IDs → recurrence/transfer must degrade;
4. replace structure-preserving null with naive global shuffle → test must detect invalid null;
5. allow SEA_AD cell count to dominate → weighting audit must fail;
6. collapse measured-zero with structural unmeasurement → STOP;
7. rotate axes inside a stable subspace → block target remains stable, individual axes do not;
8. inject an unstable Stage81A3-like candidate → must not pass stability;
9. make target equal depth/support → shortcut audit must reject it;
10. change operator/study labels → conditional-acquisition audit must respond;
11. inject old D=160/50-PC/k/5%-tail constants as production target values → STOP;
12. change normalization/root → STOP;
13. candidate present in only a few donors → fail recurrence;
14. target succeeds only under one source → fail or NOT_MEASURABLE, not PASS;
15. target hardness increases because of measurement noise → must not be called biological novelty.

## 20. Downstream binding

If a target survives and then passes independent review, create:
`FOUNDATION_TARGET_AUTHORITY_V1`

That authority must freeze:
- target mathematical definition;
- target address/module/loadings;
- target dimension/block structure;
- exact target-exclusion/context rule;
- observation operator descriptors;
- normalization/support semantics;
- full-real derivation roots;
- uncertainty semantics;
- transfer/stability/null evidence;
- code/test digests.

Only after that may separate prospective work bind it into:
- scientific F1;
- Production Teacher/Student V2;
- full foundation training;
- healthy teacher qualification;
- production D1 readout;
- later T0/pathology hypotheses.

Current F1 u0 remains mechanics/reference only.

D0 remains an independent raw-data baseline and must not be rewritten to match the selected target.

## 21. Terminals

At least one candidate survives all predeclared requirements:
`PASS_FOUNDATION_TARGET_DISCOVERY_CANDIDATE_FOR_INDEPENDENT_REVIEW__NO_TRAINING_AUTHORITY`

No candidate survives:
`STOP_FOUNDATION_TARGET_NOT_ESTABLISHED__DO_NOT_TRAIN`

Protected data accessed:
`INVALID_FOUNDATION_TARGET_DISCOVERY_PROTECTED_DATA_ACCESS`

Input binding incomplete:
`STOP_TARGET_DISCOVERY_FULL_REAL_INPUT_NOT_BOUND`

None of these authorizes training.
