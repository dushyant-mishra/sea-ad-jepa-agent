# Target Discovery V1 — post-pilot relational-target hypothesis addendum

Status:
`POST_PILOT_HYPOTHESIS_GENERATION__NO_TARGET_AUTHORITY__REQUIRES_PROSPECTIVE_CONFIRMATION`

This document does not amend or retroactively change the original Foundation Target Discovery V1 prospective contract.
It records a new candidate family generated after TD16–TD18 falsification work.

## Why this family was generated

Historical Stage81 and TD13–TD16 show:
- stable axes/subspaces can still encode acquisition structure;
- HVS↔SEA_AD raw loading vectors remain near-orthogonal even on the 17,186 all-operator scalar intersection;
- within-source OPC dependency matrices are reproducible, but same-gene dependency matrices and top-edge memberships do not replicate across HVS/NPH52/SEA_AD.

TD17–TD18 then found a different invariant:
after donor-balanced state centroids and source-specific gene-by-gene standardization across states, the **relative distance geometry among biological states** replicates strongly across sources.

This is not yet a target. The current pilot used existing cell-state annotations as validation anchors.

## Candidate family R — relational biological-state geometry

The candidate object is not a universal raw loading vector or same-gene module.
It is an equivalence class of biological-state geometry under source-specific per-gene affine observation transforms.

A production candidate must be **derived without using the validation cell-class labels**.
Labels may only be opened after the latent/relational object is frozen for biological validation.

## Prospective confirmatory requirements

A future confirmatory run must freeze before full-real scoring:

1. full-real reader_fit input authority;
2. the common-comparability/address rule;
3. label-free state/program discovery procedure within each source;
4. state-resolution selection rule;
5. relational metric and normalization;
6. graph/state matching rule across sources;
7. donor-block refit scheme;
8. operator/region holdouts;
9. cell-count/downsampling attacks;
10. count-split/measurement-depth attacks;
11. class-label validation firewall: validation labels cannot affect discovery, dimension, matching, or thresholds;
12. null generators and decision thresholds.

Required negative controls:
- random pseudo-states matched for size/donor/operator composition;
- source-only/QC-only states;
- state-label permutation;
- gene-panel resampling;
- removal of strongest state-discriminating genes;
- donor-block instability;
- operator-specific state geometry;
- synthetic source-specific capture/depth transforms.

The result may still be:
`NO_VALID_RELATIONAL_TARGET_ESTABLISHED`.

## Hierarchical implication if confirmed

Only if independently confirmed, the evidence would support testing a hierarchy:

`shared relational scaffold`
→ `source/context-specific donor-recurrent molecular state`
→ `fine molecular/query residuals`

The hierarchy is a new hypothesis. It is not the rejected historical tier-hiding cascade and it is not historical Freeze1 authority.
