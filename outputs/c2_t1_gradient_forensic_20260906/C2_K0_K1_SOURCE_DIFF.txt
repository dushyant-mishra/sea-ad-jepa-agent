# C2 closure: exact K0 to K1 source difference
#
# K0_HISTORICAL uses canonical phase_e.run_update unmodified.
# K1_BACKWARD_AUTOCAST_DISABLED is derived from inspect.getsource of that
# same function by textual substitution, so every line except the one shown
# below is byte-identical. Nothing was retyped.
#
# historical_step_sha256: 11b381762787aaae8920cfced3e245dbc8579b335ee2576ffcc21cc0253d4cd6
# ipb_jepa_sha256:        732ea46f72384f29d503de1e0cc9d853315e2493cace054cced74849aa77485a
# forensic_source_sha256: 102bb2f9296965631bb9a240d83f7d7123996d766098e21d7e71bf328833cf14
# geometry: batch 128 microbatch 8 views 4 seed 8113002
# torch 2.7.0+cu128 on NVIDIA GeForce RTX 3080 Laptop GPU

--- canonical_run_update
+++ variant_backward_autocast_disabled
@@ -1,3 +1,3 @@
-def run_update(
+def run_update_backward_autocast_disabled(
     *,
     loader: ProductionTrainLoader,
@@ -86,5 +86,6 @@
                 if not bool(torch.isfinite(raw_loss)):
                     raise RuntimeError("nonfinite JEPA loss")
-                scaler.scale(scaled_loss).backward()
+                with torch.autocast(device_type=device.type, enabled=False):
+                    scaler.scale(scaled_loss).backward()
                 loss_total += float(raw_loss.detach()) / (
                     VIEWS * (effective_batch // microbatch)
