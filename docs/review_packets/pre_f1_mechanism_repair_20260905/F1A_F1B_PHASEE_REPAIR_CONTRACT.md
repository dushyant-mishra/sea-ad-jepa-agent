# F1-A / F1-B / Phase-E Repair Contract

## 1. Phase-E permanent invariant

Phase-E remains the permanent production engineering smoke.

For every optimized parameter expected to receive loss signal:

1. record missing gradient;
2. record exact-zero vs nonzero;
3. record finite/nonfinite;
4. record pre/post-unscale gradient norm when AMP is active;
5. record optimizer first/second moments after first successful step;
6. fail closed when a required trainable path has missing or exact-zero optimizer moments.

For the current six-block IPB, explicitly cover all tensors in `attention_norm/query/key/value` for all six blocks. Never aggregate Q/K/V together with the attention output projection for qualification.

## 2. F1-A scope

F1-A is the architecture-independent target/benchmark validity harness.

Its output is invariant to all training-path properties conditional on identical frozen u0 and forward/evaluation bytes.

A PASS may support only the frozen target/representation estimand. It does **not** license training.

### Required F1-A repairs before execution

- seven independent linear protected axes;
- `core_halo` retained as protected composite stress view;
- historical 8-view telemetry retained;
- balanced 7-axis gate-bearing aggregates added;
- mirror all A-derived aggregate gates on 7-axis arithmetic;
- unadjusted per-view harm screens with terminal STOP for current F1 generation;
- non-estimable → STOP;
- 100% correct-student/teacher identity moved to mechanics/telemetry, not conclusion-bearing evidence trend;
- evidence-trend rule prospectively refrozen on nondegenerate ladder;
- rename/reinterpret the so-called direct control as an **unresidualized contextualized query state**, not context-free;
- if residualization itself is claimed beneficial, prefreeze a positive incremental 7-axis delta gate; otherwise narrow claim scope;
- u0 PASS does not reopen DEC-007/DEC-012 or qualify u0 biology;
- absolute visible-address counts reported by source/operator/evidence level;
- HC3 `(5,0,4)` design reused without reselection; new outcomes recomputed;
- query variance 1/49 correction remains reporting-only;
- canonical source hashing / CRLF repair through complete hash chain;
- WSL/Linux authoritative verification route where required;
- no rare5/rare1/recurrent-state F1 claim without exact recovered cell-level scoring authority;
- integration/query-design/decision/truth-table schema and SHA cascade updated.

## 3. F1-B separate authorization

F1-B is not covered by F1-A authorization because it performs:

- optimizer steps;
- EMA updates;
- checkpoint writes.

Freeze an independent authorization before execution.

## 4. Population

Prefer an F1-B training recipient set disjoint from F1-A's 2,781 evaluation recipients and hash-bind both lists with exact intersection count.

Note: if F1-A remains strictly frozen-u0, F1-B overlap cannot change the F1-A u0 outputs. Disjointness is still preferred for interpretive cleanliness and future reuse, but do not claim mathematical contamination when F1-A never loads F1-B-trained weights.

## 5. Mechanics/biology firewall

Only mechanics are gate-bearing in F1-B.

Biology-like quantities may be descriptive telemetry only unless separately prospectively authorized.

### Gate-bearing mechanics

- parameter-wise gradient coverage;
- Adam-moment coverage;
- movement beyond pure decay;
- finite numerical behavior;
- optimizer/EMA correctness;
- attention concentration;
- routing variation across query addresses.

### Non-gate-bearing telemetry

- identity-only versus evidence-added readouts;
- global versus query-local biological probes;
- protected-program descriptive readouts;
- representation geometry.

No target/architecture selection using those descriptive quantities.

## 6. Historical planned components

Exact historical source must be recovered before code authority is claimed.

Reported components:

- `DirectResidualStateHead`
- `SingletonQueryPredictor`
- `state_block_loss`
- `directional_pair_context_loss`

The direct head reportedly requires a missing `DIRECT_INIT_MANIFEST.json` and conflicts with closed `D_shared=null`.

Therefore do not fabricate the direct basis or choose a positive shared dimension.

## 7. Minimal viable F1-B

Until those two authorities are resolved:

`IPBEncoder + SingletonQueryPredictor + directional_pair_context_loss`

The objective is valuable specifically because query-set centering removes the cell-global component and directional pair matching forces query-specific information. But this has never been run; treat it as a prospective mechanism hypothesis, not established success.

## 8. Named outcomes

At minimum:

- `PASS_MECHANISM_QUALIFIED`
- `STOP_GRADIENT_PATH_DEAD`
- `STOP_OPTIMIZER_MOMENTS_DEAD`
- `STOP_DECAY_ONLY_MOVEMENT`
- `HEALTHY_GRADIENTS_ROUTING_STILL_DIFFUSE`
- `STOP_ROUTING_QUERY_INVARIANT`
- `STOP_NUMERICAL_OR_EMA_MECHANICS`
- `STOP_AUTHORITY_MISMATCH`

Do not collapse `HEALTHY_GRADIENTS_ROUTING_STILL_DIFFUSE` into a generic FAIL. That result distinguishes an implementation defect from an earned architecture-level routing problem.

## 9. Current prohibitions

- no real F1-A biological execution yet;
- no post-F1 contextual microtrajectory;
- no architecture tournament;
- no DEV/SEALED/pathology expression;
- no invention of missing historical source or direct-basis authority;
- no rewrite of historical DEC-013 outcome.

Preserve DEC-013 and append a superseding mechanistic note: same-architecture continuation is unauthorized until the optimizer-dead attention path is explained and the repaired planned mechanism passes bounded qualification.