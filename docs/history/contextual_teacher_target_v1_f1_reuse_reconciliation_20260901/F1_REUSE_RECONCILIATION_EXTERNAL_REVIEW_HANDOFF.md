# F1 historical reuse reconciliation — external-review handoff

Terminal: `PASS_F1_HISTORICAL_REUSE_RECONCILIATION_AWAITING_EXTERNAL_REVIEW`

This read-only pass did not alter F0/F1-preflight bytes, choose query/null/bootstrap counts, run a benchmark, implement an executor, inspect candidate outcomes, open protected expression or train.

F0 remains solved and frozen: both online/student and EMA/teacher roles use pre-mixing q masking; q identity remains; q scalar is excluded before mixing; Hq and context come from the same forward; context is the exact all-eligible arithmetic mean; subtraction precedes parameter-free LayerNorm; no CELL/global/precomputed-H input or context cap exists; measured zero is preserved; and `M_physical` remains separate from `U_evidence`. Historical recovery does not reopen discovery, interface design, implementation, or numerical certification.

Main conclusion: substantial infrastructure is reusable, but not the old scientific assumptions. FULL104 lineage/state/firewall and protected weights are current authority. Query-support arithmetic, query-first/donor-first aggregation, WSL/sorted reads/batching/resume/parity and independent adjudication are reusable algorithms or engineering patterns. Historical 2,464/297 panels, K=32/caps, fixed resample counts, V6R5B endpoint, PCA96 and u0/u205 results are not contextual-F1 constants.

The raw-value null is pre-encoder. It preserves recipient physical/evidence masks and q identity but changes normalized scalar inputs; every null/query needs a distinct six-block forward. Old ALL104 multi-null amortization therefore transfers only at I/O/executor level. The current 227-day estimate survives as `PARTIALLY_REDUCIBLE`; its major correction is to separate expensive null worlds from cheap post-representation donor bootstraps.

Current stops remain open:

- query: prospectively freeze a dense-program support-normalized coverage/claim criterion before deriving a panel;
- statistics: freeze executable contextual S/T equations, precision and independent validation;
- resource: prospectively derive expensive-null precision separately, then benchmark a parity-proven contextual executor.

Next work requires a new prospective repair instruction. No F1 candidate execution is authorized by this reconciliation.
