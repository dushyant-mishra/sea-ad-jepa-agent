# F1 prospective repair — external-review handoff

Terminal requested after review: `PASS_F1_PROSPECTIVE_REPAIR_DERIVATION_AWAITING_EXTERNAL_REVIEW`.

This package is pre-result only. It contains no contextual-candidate performance, training, EMA update, protected-partition expression, or final query-prefix choice. F0 remains closed and hash-identical.

Review focus:

- one deterministic, within-operator, donor-distinct 2,781-row bijection used as a matched wrong-input control against the true recipient teacher;
- one nested per-operator query order and support-only frontier across all eight protected families; dense claims remain `PANEL_CONDITIONED_QUERY_SAMPLE`;
- executable donor-level decision arithmetic, including fail-closed estimability, Holm families, evidence slope, direct-token degradation veto, query-identity gates, HC3 nuisance audit, and cross-source replication;
- exact F0/lean parity, one authenticated frozen-map expression pair, uncapped context, and runtime factorization of one cached teacher + five correct + five null forwards;
- independent reconstruction and adversarial fixtures, including constant-negative/NaN/missing direct endpoints, source-confined nuisance signal, collinearity, zero variance, map binding, and semantic snapshot mutation.

External review must choose/freeze a support criterion and query prefix before any candidate run. This package does not authorize F1 qualification, FULL104 candidate execution, training, or access to reader-validation/oracle, DEV, SEALED, or pathology data.

The manifest is the file-level authority for the 16 published artifacts. The four implementation scripts and all upstream authorities are hash-bound in `F1_REPAIR_SOURCE_AUTHORITY.json`.
