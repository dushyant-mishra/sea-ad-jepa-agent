# Failed materialization attempt — NPH collision-authority path

Status: `ENGINEERING_FAILURE_NO_SCIENTIFIC_RESULT`.

All 42 selected assets authenticated. All 24 HVS and 11 SEA-AD operators materialized successfully, totaling 246,744 selected cells. The process stopped before opening any NPH derivative because the R helper was given the external authority repository root rather than its `results/v4` subdirectory.

No representation statistic, dimension, threshold, basis, standardization, optimizer, EMA, lambda, query, schedule, GPU mechanic, or training quantity was computed. No reader-validation/oracle, DEV, SEALED, pathology, protected donor, or original mixed NPH expression was opened.

The failed contract and exact pre-repair Python code are preserved beside this report. The authorized repair changes only the argument passed to the already-frozen NPH helper from `AUTH` to `AUTH/results/v4`; all completed authenticated blocks remain hash-journaled for exact resume.
