# External review handoff

Terminal: `PASS_F1_DECISION_TRUTH_TABLE_RECONCILIATION_AWAITING_EXTERNAL_REVIEW`.

The submitted v3 package is preserved as historical evidence. This new package reconciles its semantic mismatch without changing randomization, assignments, weights, namespace, null map, thresholds, multiplicity families, or protected endpoints.

The exact frozen prose establishes that protected-program positive Holm results are report-only; program presence and estimability remain mandatory, while significant contextual-minus-direct degradation and significant negative QID margin are program-level vetoes. The later frozen two-draw and QID-v2 contracts govern current one-sided primary/QID inference and design-sampled scope.

Integration v4 applies each current endpoint once through `qualify_current()` and never invokes legacy `v1.qualify()` wholesale. The finite-negative `local_core` failure is reproduced, all fourteen decision fixtures have independently matching gate vectors, and the old population omission/relabel/QID-domain protections remain unchanged.

Real execution is not authorized. Both forward and nuisance authorities remain unset. Future execution must bind specifically to decision-v4/integration-v4, not historical v3 integration.
