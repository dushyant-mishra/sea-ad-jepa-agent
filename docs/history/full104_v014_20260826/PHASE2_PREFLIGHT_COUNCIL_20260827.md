# FULL104 Phase-2 preflight council — 2026-08-27

Historian: `NOVEL_AND_AUTHORIZED`.

Specialist verdicts:

- Teacher Architect: `CONCERN` — freeze hierarchy, folds, views, nulls, one-SE rule, boundary handling, and cross-fitted private state.
- Statistics/Leakage: `PASS` with nested donor-heldout transformations and support-preserving nulls required.
- Observation/Operator: `CONCERN` — bind every row to authenticated lineage/assets/namespace and freeze a physical-descriptor allowlist.
- Target/Identifiability: `PASS` with subspace-level canonicalization and `TEACHER_BIOLOGY_LIMIT` if no joint prefix survives.
- Biology/Weak/Local/Halo/Rare: `CONCERN` — donor-primary and source/support sensitivities must prevent broad/common or SEA-AD volume from hiding under-observed structure.
- Compute: `CONCERN` — dense cell-by-address/cell-by-cell fitting is prohibited; use resumable streaming moments/sketches and do not treat 96 or 160 as capacity authority.
- Optimization/Numerics: `CONCERN` — freeze float64 accumulation, canonical order, solver diagnostics, deterministic sketches, and restart equivalence.

Synthesis: `PROCEED_WITH_REPAIR`.

The repair is one metadata-only, hash-pinned `PHASE2_DERIVATION_FREEZE` before expression statistics. It is an engineering completion of the controlling contract, not a scientific redesign. The freeze must enforce the five-step artifact DAG, equal-donor primary estimand, separately reported source sensitivities, complete donor cross-fitting, matched nulls, exact provenance tags, protected-data firewall, numerical STOPs, and direct-basis nested donor CV. No optimizer, EMA, lambda, query/schedule, GPU-mechanics selection, or training is authorized.

Preserved dissent: two critics judged the scientific contract sufficient in principle; five required the executable freeze. Synthesis treated those concerns as mandatory implementation repairs.
