# F1 pre-result targeted council and Red-Team

No candidate F1 performance or protected expression was inspected.

## 1. Authority / Historian — PASS

The requested preflight is novel and authorized after the authenticated F0 gate. F0 root, reviewed sources, prospective contract, protected weights and closed shared-state authority match. This authorizes preregistration/resource work only.

## 2. Objective / Gradient + Interface — STOP

Authorities, query withholding and matched-support semantics are strong. However conclusion-bearing similarity, retrieval/rank/ties, evidence slope, query identity and program aggregation are not yet executable mathematical definitions. The three-state observation authority must not be renamed as its scalar Boolean projection. Exact formulas are required before outcomes.

## 3. Statistics / Representation Geometry — STOP

Equal-donor hierarchy, source-stratified whole-donor resampling and deterministic evidence/null plans are appropriate. The 319 null count is outcome-independent, but reusing 319 for simultaneous eight-program bootstrap intervals leaves the Bonferroni tail at the single extreme-order-statistic boundary. Bootstrap precision and exact RNG/interpolation/decision maps remain unresolved.

## 4. Biology / Protected Programs — STOP

All families and `local_core` are represented, sparse programs are exhaustive, and selection is outcome-blind. But the top-72 dense panels capture only about 9.0% broad, 9.1% weak, 25.6% local, and 3.4% innovation/rare L2 energy. Physical support also varies by operator. This cannot support whole-dense-program non-degradation without exact support-normalized program scoring or an explicitly narrower claim. Choosing another query budget now would be a new arbitrary constant.

## 5. Observation / Firewall — PASS to a later implementation gate

The plan binds reader-fit104 lineage, split, assets, 42x41,238 state authority, normalized values, checkpoint/model state, tokenizer and encoder. It keeps identity in an audit sidecar, preserves measured zero and states 0/1/2, and denies original mixed NPH/protected inputs. The future lean wrapper must executable-pass these assertions before any result.

## 6. Compute / Resource — STOP

Batch 16 gives 11.70–14.40 queries/s and fits the RTX 3080 without a context cap. The proposed panel has 707,692 queries per evidence level. Five base levels project to about 84 hours at the slowest measured source rate. The frozen 319 primary-level value-reassignment nulls change scalar encoder inputs and project to 225,753,748 additional forwards, about 5,361 hours; total is roughly 227 days. The lean path is design-only and cannot remove this scientific recomputation.

## 7. Scientific Red-Team — STOP

Actual null assignments/golden legality tests and the independent arithmetic implementation do not yet exist. More importantly, query population, protected-program estimand, exact endpoint equations and complete compute design remain open. These must be resolved prospectively; no result execution may begin.

## Synthesis

Strengths are retained as reusable pre-result evidence: authority reconciliation, deterministic nested evidence semantics, exhaustive sparse program coverage, 104-donor/42-operator cell authority, exact no-cap benchmark, and independent-adjudicator design. The first applicable controlling terminal is `STOP_F1_QUERY_POPULATION_UNRESOLVED`; `STOP_F1_STATISTICAL_RULE_UNRESOLVED` and `STOP_F1_RESOURCE_INFEASIBLE` are additional independent blockers. No scientific shortcut or post-result tuning is acceptable.
