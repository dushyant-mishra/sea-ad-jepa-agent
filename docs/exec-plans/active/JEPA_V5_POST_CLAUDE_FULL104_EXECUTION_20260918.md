# JEPA V5 post-Claude FULL104 execution plan — 2026-09-18

Status: `ACTIVE_PRETERMINAL_PLAN__TERMINAL_OUTCOMES_SEALED__TRAINING_OFF`

Scientific anchor: `72338541c3a161cf461dde5215e6841ee9704ee7` (PR #26).

## Phase A — non-terminal real-FULL104 shakedown

Run now on the GPU laptop/attached drive:

1. authenticate physical Level-4 manifest/registry/observation state;
2. reproduce FULL104 census/support receipts from the real substrate;
3. validate all-donor/source/operator/fold coverage;
4. stream all blocks exactly once and capture runtime/memory telemetry;
5. build calibration-only cache and prove terminal/training role is forbidden;
6. calibration-mode preflight and deterministic replay;
7. runtime-directory anti-spillover scan.

Do not inspect terminal masking-policy outcomes in Phase A.

## Phase B — close current specification blockers

1. independently re-review PR #26 G1/H1/H2;
2. perform global `NO_CALLER_SUPPLIED_DERIVABLE_RECEIPT_FIELDS_V1` audit;
3. resolve H3 target-panel sizing for equivalence precision/power;
4. resolve G5 margin rationale and choose/integrate lawful margin authority;
5. resolve G2 policy-selection complexity materiality;
6. add/freeze G4 biological signal-preservation criterion;
7. specify G3 capacity-matched/deep anti-shortcut gate;
8. pre-register H4 complete-ladder failure interpretations.

## Phase C — deliberate branch integration

Evaluate parallel PRs #23/#24/#25. Integrate only justified changes into one successor of PR #26. No housekeeping merge. Run exact-head CI, zero skips, provenance/spillover audit, then independent red team.

## Phase D — final preterminal freeze

After source is final:

1. build/freeze current authorities;
2. rebuild machine/worktree checkpoint on exact clean GPU worktree;
3. build RunContract from artifact paths/live hashes only;
4. independent frozen-package audit;
5. terminal-mode preflight.

## Phase E — terminal ladder

Only then execute:

`5% -> 10% -> 15% -> 20% -> 30% -> 50%`

One rung per invocation. A higher rung requires exact failed prior ExecutionAuthority from the same RunContract. Stop at first qualifier. No post-outcome retuning.

## Phase F — training gate

Even after masking qualification, training remains blocked until current representation/dimension/mechanics authorities and the capacity-matched anti-shortcut/signal-preservation gates close. Masking PASS alone never authorizes training.
