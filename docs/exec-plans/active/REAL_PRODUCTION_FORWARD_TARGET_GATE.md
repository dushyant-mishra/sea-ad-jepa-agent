# ACTIVE EXECUTION PLAN — Real Production Forward Target Gate

## Scientific boundary
Determine whether any current target family lies on a defensible:

`BIOLOGICAL INFORMATION RETENTION x CONDITIONAL PREDICTABILITY`

frontier on actual FOUNDATION TRAIN RNA.

This is the next outcome-changing evidence.

## Step 1 — Reconstruct actual current production pipeline
From repository authority, identify:
- current HEAD / branch / dirty state
- controlling production contracts
- actual molecular-address count and hashes
- current production loader
- three-state observation semantics
- TRAIN donor/split firewall
- current sampler/replay logic
- model/tokenizer/IPB/EMA implementation
- current target-generation code
- FOUNDATION TRAIN expression objects
- available Windows and WSL execution environments

Build a compact pipeline map:
source RNA -> address mapping -> physical observation state -> normalization/value representation -> sampler -> artificial evidence mask -> tokenizer -> H -> EMA teacher -> target operator -> predictor/readout

For each arrow, identify semantics and any gradient/metadata leakage.

If repository authority supersedes the expected 41,238-address state, prove it and follow the newer authority.

## Step 2 — Validate physical evidence semantics on actual TRAIN examples
Across relevant source/operators verify:
- measured scalar including zeros remains measured
- structural absence never becomes biological zero
- collision unresolved never becomes biological zero
- artificial masking acts only on MEASURED_SCALAR
- repeated presentations can receive fresh artificial masks while physical support is unchanged
- source/operator metadata is not an unrestricted biological shortcut

If a production-path defect is found, repair or isolate only what is necessary for a valid forward audit.

## Step 3 — Freeze pathology-blind TRAIN biological audit references BEFORE candidate comparison
Include where supported:
- broad/common
- weak/distributed
- local
- core+halo
- sparse/marker-like
- donor-recurrent rare audit classes
- de-novo TRAIN-only 5% extremes
- de-novo TRAIN-only 1% extremes
- innovation-tail structure
- donor-disjoint evaluation groups

For each quantify:
- prevalence
- donor recurrence
- source/operator recurrence
- N_eff
- physical support
- retained visible evidence E_ik
- insufficient-support cases

## Step 4 — Inspect actual target computation graphs
Starting hypotheses:
- T0 arithmetic block mean
- T1 full same-address teacher H_g
- TCTX query-self-masked contextual teacher H_g

These are hypotheses, not mandatory winners.

For each document:
- biological claim
- teacher inputs
- student inputs
- target tensor shape
- address identity path
- queried scalar path
- teacher privilege
- stop-gradient boundary
- what encoder gradients would hit if trained
- physical-support legality

If independent critics discover that all current candidates share one demonstrated structural defect, they may propose at most ONE better minimal candidate before outcome-driven tuning.

## Step 5 — Real TRAIN forward-only dual gate
No encoder optimizer updates.
No EMA updates.
No pathology.
No DEV/SEALED.

Frozen diagnostic predictors/readouts are allowed with strict donor-disjoint fitting/evaluation.

For each candidate measure independently:

A. TRUE TARGET BIOLOGICAL CONTENT
- broad/common
- weak
- local
- core+halo
- sparse/marker-like
- recurrent 5%
- recurrent 1%
- innovation-tail
- lower-tail behavior

B. CONDITIONAL PREDICTABILITY FROM STUDENT EVIDENCE
- donor-held-out R2
- MSE
- centered cosine
- CKA or equivalent rotation-tolerant metric
- lower-tail predictability
- per-program predictability
- performance vs E_ik / N_eff / physical support

C. FALSIFICATIONS
- donor leakage
- target permutation
- identity-only shortcut
- evidence-only shortcut
- source/operator shortcut
- same-cell/new-mask vs unseen-cell/new-mask where relevant

Classify every meaningful failure as:
- target lost biology
- biology retained but partial evidence cannot identify it
- insufficient physical support
- coordinate/readout artifact
- technical/source shortcut

## Multi-agent budget
Do not spawn a full board at startup.

After decision-relevant metrics exist, use at most three scoped critics:
1. Biology + Rare/Weak/Local
2. Target/Objectives + Gradient/Identifiability
3. Observation/Provenance + Adversarial

Escalate to a larger board only for a major scientific pivot or authorization of encoder training.

## Optional 4,096 Stage81B use
Historical 4,096 artifacts are optional mechanism evidence only.
Use them only if one specific unresolved mechanism question requires them cheaply.
Do not delay the real production gate for exact historical fixture recreation.

## Deliverable
A compact evidence package outside the authoritative source tree containing:
- production-pipeline report
- evidence-semantics audit
- frozen biological reference definitions
- target computation-graph table
- per-candidate biology metrics
- per-candidate predictability metrics
- evidence-support stratification
- falsifications
- pivot log
- three scoped critic reports
- final adjudication
- provenance/hash manifest

Final decision should be one of:
- candidate qualified for tiny real causal target experiment
- no target qualifies; one decisive next test
- production pipeline repair required
- integrity/firewall stop

Do not start a long foundation optimizer run.
