# FULL104 Phase-2 state derivation

Status: `AUTHORIZED_PENDING_PREEXPRESSION_FREEZE`

Scope is limited to five ordered, read-only derivations:

1. shared state;
2. private residual state after shared hash-freeze;
3. production capacity gate without truncation;
4. fit-104 teacher-state standardization;
5. direct molecular basis and ridge by donor-aware CV.

No optimizer, EMA, lambda, query geometry, schedule, GPU-mechanics selection, causal u0/u205 run, validation/oracle expression, DEV, SEALED, or pathology is authorized.

The primary estimand gives every one of the 104 reader-fit donors equal influence. Cells are weighted within donor, so source exposure follows the frozen fit-donor composition rather than raw cell volume or forced equal thirds. Equal-source and source-deletion views are sensitivity reports only and cannot select dimensions.

Expression remains closed until `PHASE2_DERIVATION_FREEZE.json` and its external hash anchor pass. Engineering retries within the frozen scientific/data contract are standing-authorized and must preserve failed attempts.

Terminal scientific routes:

- no jointly supported shared prefix: `TEACHER_BIOLOGY_LIMIT`;
- no reproducible private prefix: freeze `D_private=0` and continue;
- unsaturated boundary or unsupported selected state: `STUDENT_CAPACITY_LIMIT`;
- unstable numerical/restart geometry: `STOP_NUMERICAL_GEOMETRY_UNSTABLE`;
- otherwise publish all five artifacts for final council and Red-Team.
