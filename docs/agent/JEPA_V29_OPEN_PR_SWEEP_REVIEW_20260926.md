# V29 complete current-open-PR authority artifact sweep — exact fixed snapshot

**114/114** distinct open GitHub PR heads were independently fetched as full recursive Git trees (none truncated or failed), compared against PR149 exact reference `938fe5d3a7288826fef86b2650fb3a9a21fd3794`. PR tips were re-fetched and confirmed unchanged before publishing. There are **87 head×candidate appearances** spanning **18 unique changed path+Git-blob SHA pairs** under the declared *targeted JSON-path* filter. This is an intentionally scoped **GitHub PR-tip path/byte-identity audit**, not a scan of all 262 repo branches, remote GPU files, or a running current-V5 class validator.

The [machine-readable complete snapshot](JEPA_V29_ALL_OPEN_PR_AUTHORITY_CANDIDATE_SWEEP_20260926.json) includes all 114 immutable PR tip SHAs and the 18 unique changed candidate paths, their Git blob SHA1/size, observed contributing PR numbers, and conservative scope classification. No candidate in the discovered difference set is automatically promoted to a current-V5 authority merely because its path contains “AUTHORITY” or a SHA. **The six existing PR149 own-schema candidates still stand, with zero V2 closure roots**: this sweep validated no additional own-class root and is not proof that the 25 missing on PR149's original reviewed head are globally absent.

Source-role triage, supported by actual inspected schema examples: `TARGET_SOURCE_ESTIMABILITY.json` is a historical target/source estimability analysis, not the current V5 support authority; phase-I `independent_candidate_manifest` is a corrected N1 physical-input derivative explicitly marked `PHYSICAL_INPUT_REPAIR__NOT_EXECUTION_AUTHORITY`; `AUDIT_B_PREEXECUTION_STATUS_V1` is a September21 **N1 status report** and is not `CurrentTrainerPreexecutionAuthorityV2`; `RARE_TAIL_MOLECULAR_AUTHORITY_V1` belongs to a separate guarded rare-tail experiment with unproven molecular estimability; other changed JSONs are N1/rare-tail support, historical handoffs or file manifests. Remaining files were classified by scoped paths/roles, **not exhaustively schema-validated**. If a branch claims a precise current V5 role, run its defining-class validator and authenticate its upstream bytes before updating the 33-slot ledger.

**Still genuinely open:** prospectively chosen teacher target/mask/donor split/EMA/geometry; version-compatibility blockers in PR151; all upstream source/role validators and original Level4 manifest byte attestation; independent external GPU-drive inventory; B1 frozen `reader_fit` training contract, B2 signed 32+1 receipt closure and authentic critical-test/runtime roots. Current main remains V25; S9 false broken-environment diagnosis retracted. No historical/synthetic 94-donor parameters or PR146 metadata ZIP SHA can substitute for current104 expression-manifest and training authority.

Run `PYTHONPATH=. python -m pytest -q tests/test_v29_open_pr_artifact_sweep.py` for fail-closed documentary checks on the stored snapshot. This is a **bounded evidence inventory only**, no heavy source opened and no protected N1 or V5 training authorization.
