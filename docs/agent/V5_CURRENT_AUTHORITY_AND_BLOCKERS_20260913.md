# V5 current authority and blockers — refreshed 2026-09-14

Status: `D_SHARED_AUTHORITY_V2_FROZEN__FEATURE_LINEAGE_AND_FULL104_DATA_AWARE_MEASUREMENT_QUALIFICATION_REQUIRED__D_SHARED_NOT_RUN__NO_TRAINING_AUTHORITY`

This successor ledger supersedes the *status* portions of older blocker documents while preserving all historical artifacts as provenance.

## Closed current-V5 prerequisites

1. Historical FULL104 physical bytes were relocated and authenticated.
2. Hardened FULL104 binder replay passed physical/identity closure over 4,553,407 cells, 104 donors, 42 operators, 42 matrices, 8,915 blocks, and 41,238 molecular addresses.
3. Exact sealed FULL104 dimension-input artifact SHA-256: `eb1264489306413fb57316abe7f70205a771248881302d8c867c70b69273f1ad`.
4. V1 prospective precision authority and V1 FULL104↔precision cross-binding were frozen before any current-V5 dimension outcomes.
5. Heavy-machine reconnaissance demonstrated five pre-outcome D_shared authority defects before any D_shared outcome existed: scalar-max count collapse, unspecified unique null generator, missing separate effect rule, unfrozen rank envelope, and rank multiplicity outside the declared precision budget.
6. V1 remains immutable provenance only for D_shared execution:
   - precision V1 SHA `cc4ac4d5116fa81990f1c3bd0497fc578eda86bb2d7d3cd2747abcf7ffcf9428`
   - V1 cross-binding receipt SHA `eccc30f9f5d01f7905a0740982d678d0ce45532a08cd5a61c51f5448b63b3d3a`
7. D_shared Authority V2 is prospectively frozen before D_shared outcome access:
   - artifact `docs/agent/V5_PROSPECTIVE_D_SHARED_AUTHORITY_V2.json`
   - canonical SHA-256 `f9568eb19a22f106b0be3ce0580bc1058695ca1a2de6dd2d0475c3240a6bbda2`
8. V2 freezes ranks 1..512, per-quantity/rank execution counts, 10×512 precision multiplicity, full-population deterministic within-stratum independent-view permutation nulls, strict lower-bound >0 effect criteria, unconditional failure semantics, and D_shared-only stage authority.
9. Repository V2 code contains `d_shared_authority_v2.py`, `d_shared_execution_firewall_v2.py`, and `d_shared_rank_adjudication_v2.py`.
10. No D_shared, D_private, or D_obs current-V5 outcome has been generated or inspected.

See `V5_D_SHARED_AUTHORITY_V2_SUPERSESSION_20260914.md` for the V1→V2 supersession record.

## Course correction from T0 lessons

The frozen T0 closeout remains:

`T0_MEASUREMENT_PROCEDURE_NOT_QUALIFIED_AT_N28__BIOLOGY_UNRESOLVED`

T0 tested a specific latent/common-factor measurement procedure at n=28 with prospectively frozen synthetic studies at the real T0 geometry. That exact procedure is **not** the V5 D_shared procedure.

However, the T0↔V5 crosswalk identified a transferable framework gap: V5 lacked an explicit target/data-distribution characterization stage before choosing the measurement rule and lacked an explicit measurement-procedure qualification stage. That lesson now applies upstream of real D_shared.

The governing rule is:

`DATA_AWARE__OUTCOME_BLIND`

FULL104 must be characterized deeply enough to design and qualify the pipeline around the actual dataset. Only the final decision-bearing D_shared hypothesis-test outputs remain sealed until the procedure is frozen.

Full record:

`docs/agent/V5_T0_LESSONS_DATA_AWARE_MEASUREMENT_QUALIFICATION_COURSE_CORRECTION_20260914.md`

## Immediate blockers before real D_shared

### 1. Feature-lineage qualification

D_shared would consume derived `A_full`, `B_full`, `A_views`, and `B_views` matrices whose historical package roots exist but are not yet sufficient current-V5 derivation authority.

Known roots:

- feature matrix `c9a6ede6f33a4a9d4ce22cde0f1a8c0fb5e7e039bac3df364325c778487329ef`
- multiview `d6f70ee1bca777f3d2cbd89dd560395cc46ec25f6a648ae4ba0fd6e77c1c2cf1`

The exact transformation, row/address identity, view construction, donor/operator mapping, absence of capping/sampling/adaptive choices, and deterministic reproducibility must be certified. If not certifiable, rebuild from authenticated FULL104.

Heavy-machine instructions:

`docs/agent/V5_CLAUDE_DSHARED_FEATURE_LINEAGE_INSTRUCTIONS_20260914.md`

### 2. FULL104 data reconnaissance

Before final D_shared, characterize non-decision-bearing properties of the actual production data and derived representation, including support, sparsity, depth/detection, donor/operator/source balance, technical confounding, matched-null stratum sizes/singletons, feature covariance/effective rank, A/B-view redundancy, leverage/influence, estimability, and runtime/I/O mechanics.

This reconnaissance may inform upstream design because no current-V5 D_shared outcome has yet been opened.

It must not inspect or preserve final per-rank D_shared qualification outputs.

### 3. V5 measurement-procedure qualification at real FULL104 geometry

Before real D_shared, prospectively freeze and independently review controls demonstrating that the **complete V5 D_shared procedure** can distinguish the intended shared signal from structured technical dependence at the actual FULL104 geometry.

Required categories:

- real-geometry negative controls;
- real-geometry positive controls;
- prospective difficulty/detection curve;
- unconditional failure accounting;
- explicit donor influence/concentration diagnostics;
- matched nuisance/simple comparator.

Toy independent synthetic fixtures may test mechanics but cannot establish production measurement qualification.

### 4. Matching-state discreteness and exact successor binding

After the upstream items above are closed, prove `donor`, `operator`, `Q_DEPTH`, `Q_DETECT`, and `support_measurability` have frozen discrete representations. Do not invent post-hoc bins. Then produce the exact successor pre-outcome binding that includes every current authority parent required for real execution, including qualified feature lineage.

## D_shared Authority V2 standing

V2 remains a valid prospectively frozen **statistical authority**, but is now classified as:

`STATISTICAL_AUTHORITY_FROZEN__NOT_SUFFICIENT_FOR_REAL_D_SHARED_EXECUTION_WITHOUT_UPSTREAM_DATA_AND_MEASUREMENT_QUALIFICATION`

This does not invalidate the V2 repairs. It prevents those statistical rules from being mistaken for proof that the measurement procedure itself is biologically/technically discriminating.

## Premature outcome-access rule

If decision-bearing D_shared outcomes are inspected before the upstream procedure is frozen, that run becomes exploratory. Post-inspection changes to ranks, thresholds, nulls, matching bins, feature construction, replicate counts, failure semantics, or decision rules cannot use that same run as independent confirmatory evidence. A new prospectively frozen confirmation run/design would be required.

## Still-open downstream blockers

- D_shared authority-bearing producer/executor remains to be completed/reviewed after upstream qualification.
- D_private remains closed until exact D_shared freezes and its candidate authority receives explicit independent finalization.
- D_obs remains closed until its candidate authority receives explicit independent finalization.
- Dataset-derived proposal/schedule/packing/model/runtime geometry remains downstream of dimensions.
- Executable two-sided anti-cheat gate evidence producers remain required.
- Production-geometry CUDA qualification runner remains required.
- Executable environment preflight remains required.
- External/confirmation cohorts remain role-frozen and inaccessible for upstream design feedback.
- Exact-byte lineage must continue through every downstream authority artifact.
- Branch→unique-content/ancestry cleanup remains operational work, not a D_shared blocker.

## Hard boundaries

`training_authorized = false`

`protected_data_authorized = false`

`numeric_dimensions_authorized = false`

`d_shared_real_outcome_access_authorized = false`

`d_private_execution_authorized = false`

`d_obs_execution_authorized = false`

`td60_authorized = false`

`relational_target_activation_authorized = false`

`external_validation_outcomes_authorized_for_design = false`

## Corrected current sequence

`feature-lineage qualification/rebuild -> FULL104 data reconnaissance -> support/estimability review -> V5 D_shared measurement-procedure qualification at real geometry -> freeze any required successor authority -> exact successor pre-outcome binding -> independent executor review -> real D_shared once -> seal raw evidence -> V2/successor rank adjudication -> freeze D_shared -> finalize/review D_private -> D_private -> finalize/review D_obs -> D_obs -> dataset-derived runtime geometry -> executable anti-cheat controls -> production CUDA qualification -> bounded base learning -> lawful EMA teacher -> TD60 -> relational student qualification -> internal validation -> matched-context controls -> external transport -> integrated review -> possible later training authority`
