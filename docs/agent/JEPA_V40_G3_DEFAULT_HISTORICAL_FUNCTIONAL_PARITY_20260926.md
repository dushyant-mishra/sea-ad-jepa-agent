# V40 — physical historical pre-G3 versus default-off G3 functional parity

**2026-09-26. NON-AUTHORIZING.** This branch is stacked on V37 draft PR #166, itself stacked on PR #144. The exact issue is different from any historical Stage81A3 training or T1/C2 gradient review: PR #144 updated the planner-source file *after* the September 21 Phase-IV target-sample freeze, causing five correctly fail-closed original checks. V37 physically confirmed that the old planner SHA `143645be...` is the real Sept20 source and the G3 opt-in SHA `de2f019e...` is current, with the other six bound inputs unchanged. A SHA comparison does **not** prove whether code behavior with G3 disabled remained identical; V40 answers that narrow changed-input question.

V40 dynamically loads the exact original executor bytes using `git show 2ebfd6ad...:src/sea_ad_jepa/v5/full104_masking_streaming_executor_v1.py`, verifies its complete frozen SHA-256, and separately authenticates the current G3 source and the original frozen receipt. Both executors run *independently* on the **same preexisting, synthetic source-balanced 12-donor/48-cell two-target/eight-column authenticated-block-shaped fixture** from the original runner tests. The test asserts strict, unrounded equality of every result mapping and float in all three donor-heldout folds and in the full 24-row wrapper; it also demands original schema under default-off G3 and explicit development-only labeling when any new G3 fit objective is opted into. An invalid new objective must fail before evaluating any fold. A separate negative control confirms the original frozen source SHA is still different from the G3 SHA.

This is a **bounded synthetic numerical equivalence** test. It does not authenticate the original ~30GB FULL104 physical blocks on the separate GPU laptop, establish equality across all real FULL104 source/donor combinations, validate scientific weighting or donor-transport, inspect held-out Audit-B N1 burden, or authorize masking-policy selection. **Do not repoint the September 21 immutable freeze to the new G3 source** merely because these synthetic results are equal. A fresh versioned freeze with independently re-bound provenance and prospective scientific signoff remains required for the changed current runner, including review of G3's opt-in objectives. PR #144's original five expected source-binding failures may remain until that separate successor is approved. TRAINING OFF, N1 UNOPENED, protected outcome unopened.
