# JEPA NEW-CHAT HANDOFF — CURRENT V5 AUTHORITY SUCCESSOR

Date: 2026-09-15
Status: `CURRENT_V5_AUTHORITY_SUCCESSOR__UPSTREAM_SCIENCE_PARTIALLY_FROZEN__RUNTIME_ABSENT__NO_TRAINING_AUTHORITY`

This handoff supersedes the earlier September-15 target-authority startup package for *current work state*. It does **not** erase the historical-audits index or the predecessor planning handoff. Read history before repeating any audit.

## 0. Immediate startup instructions

New chat must do the following before changing anything:

1. Re-fetch live heads for:
   - `main`
   - `impl/v5-current-target-authority-successors-20260915`
   - this handoff branch
2. Read `START_HERE.md` on this handoff branch.
3. Read `docs/agent/JEPA_LATEST_HANDOFF_POINTER.json` on this handoff branch.
4. Read this file completely.
5. Read `docs/agent/JEPA_NEW_CHAT_HANDOFF_STATE_20260915_V5_SUCCESSOR_CURRENT.json`.
6. Read `docs/agent/JEPA_NEW_CHAT_INSTRUCTIONS_20260915_V5_SUCCESSOR_CURRENT.md`.
7. Read `docs/agent/JEPA_HISTORICAL_AUDITS_INDEX_20260915.md` from `main @ c8898923fc10ffa5ef0662b04908b0d94bcb158b`.
8. Read the predecessor V15 handoff branch `handoff/jepa-v5-target-authority-build-20260915 @ af0ae024808e6642509121db8278c15259959632` if deeper design context is needed.
9. Before any computation/audit, classify it as one of:
   - `ALREADY_AUDITED`
   - `SUPERSEDED`
   - `OPEN`
   - `CHANGED_INPUT_REQUIRES_REQUALIFICATION`
10. Do not start training. Do not inspect protected/confirmation outcomes while design choices remain open.

## 1. Live lineage at handoff creation

- canonical `main`: `c8898923fc10ffa5ef0662b04908b0d94bcb158b`
- active implementation lane: `impl/v5-current-target-authority-successors-20260915 @ 812352a70ce1322f1a7d35a3fa9feeda4e4148c3`
- implementation tree: `497fd3021c82aae05f0b36c8f4567674ebcd9f40`
- predecessor implementation parent before estimand recovery: `408db2362c2f8401fcbc825f99a64f5c3fc59dc9`
- predecessor V15 handoff: `handoff/jepa-v5-target-authority-build-20260915 @ af0ae024808e6642509121db8278c15259959632`
- implementation lane relation to main at handoff creation: `ahead 56 / behind 0`

Do not infer authority from branch names. Exact commit and artifact hashes are the authority references.

## 2. Permanent governance boundaries

`DATA -> SUPPORT/ESTIMABILITY -> SCIENTIFIC ESTIMAND -> TARGET SEMANTICS -> PRODUCTION GEOMETRY -> MODEL`

`IF_CONFIRMATION_DATA_COULD_CHANGE_A_DESIGN_CHOICE_DO_NOT_LOOK`

`TRAINING_OFF`

`NO_D_SHARED_OUTCOME_EXECUTION_OR_INSPECTION`

Do not inspect protected/pathology/DEV/SEALED outcomes, real D_shared/D_private/D_obs decision-bearing outcomes, TD60, or relational activation while design choices remain open.

Permanent semantic rules:

- `NAMES_DO_NOT_CREATE_CAUSAL_OR_BIOLOGICAL_AUTHORITY`
- `COMMON_CORE_SUPPORT_IS_COMPARABILITY_NOT_BIOLOGY_AUTHORITY`
- `DIMENSION_ESTIMAND_AUTHORITY != BASE_TRAINING_ESTIMAND_AUTHORITY`
- `QUALIFICATION_RANK_CEILING != PRODUCTION_MODEL_WIDTH`
- `DO_NOT_UNLOCK_V5_BY_RECEIPT_ONLY`
- `NO_REAL_RESULT_FROM_THE_DISCARDED_X4_WITHIN_ESTIMATOR_IS_AUTHORITY`

## 3. FULL104 substrate — already recovered, do not redo

Population:

- cells: `4,553,407`
- donors: `104`
- operators: `42`
- addresses: `41,238`
- common-core addresses: `17,186`
- Level-4 blocks: `8,915`
- sources: `SEA_AD`, `HVS`, `NPH52`
- donor counts by source: HVS `41`, NPH52 `17`, SEA_AD `46`
- historical FULL104 manifest: `66f589e56badb1487058f2c95940c3e4b37196e3ab5e9c6ea1ffbe7098d2ea29`
- heavy expression path: `D:/Jepa project/outputs/full104_v014_20260826/03_phase2_state_derivation_v1/expression_level4/`

Normalization fact:

`FULL_SOURCE_LIBRARY_EXTENDS_BEYOND_41K_LEDGER__SOURCE_DEPENDENT_OUTSIDE_LEDGER_FRACTION`

K=2 common-core partition:

- V0 addresses: `8,568`
- V1 addresses: `8,618`
- intersection: `0`
- union: `17,186`
- partition artifact: `129631f95c9ef90bd5a4af2a70cbc2adbbf907ed6357b9c13c4aa370cafc1575`
- V0 full parent SHA: `3b3f102c6767727ca4ab56832f8e70baf203676d6b65973c42903b22b6d56ada`
- V1 full parent SHA: `c41df46d842d643f04566b8523a8aa711fa54bec1c836e0b394c0146017f231c`

Do not rebuild V0/V1 expecting address partitioning to identify cell-level source/operator effects. Both views use the same cells/operators/sources by construction.

## 4. Historical audits — do not repeat by default

Read `docs/agent/JEPA_HISTORICAL_AUDITS_INDEX_20260915.md` on main. In particular, the following are already audited/settled:

- T1/C2 protected-gradient failure and repaired update chronology
- Stage81A3 corrected TRAIN-cache scope
- T0 V20 independent review
- T0 measurement terminal failure state
- QID semantic mismatch
- F1-B optimizer/receipt mechanics
- FULL104 lineage
- V0/V1 rebuild
- visibility ablation
- same-cell thinning at tested model class
- context estimand sensitivity
- defective `x4_within.py` withdrawal
- corrected within-group estimator fixtures
- within-donor/source-specific signal
- donor recurrence
- donor generalization at tested linear class
- loss-geometry proxy as supporting-only
- teacher/student/EMA mechanics existence
- V5 fail-closed wrapper carryover defect
- target-identity shortcut discovery itself
- observation-state gradient firewall mechanics
- data-first schema audit
- protected-registry/checkpoint dynamic-count pattern
- EMA mechanics (not timescale authority)
- branch/governance warning
- runtime asset provenance warning

Current work must start from those findings, not rediscover them.

## 5. What this successor branch actually changed

`impl/v5-current-target-authority-successors-20260915` adds a current-V5 authority layer without modifying historical implementation files. At the handoff SHA the branch is 56 commits ahead / 0 behind main and adds the current authority/test/docs surface.

Important: **module existence is not the same as a frozen scientific choice.** The branch contains both:

1. genuinely frozen current authorities; and
2. generic fail-closed schemas that keep unresolved choices explicit.

### 5.1 Frozen current authority: primary molecular representation

Status:

`PRIMARY_MOLECULAR_REPRESENTATION_FROZEN__VALUE_ONLY_256__NO_TRAINING_AUTHORITY`

Frozen role:

`PRIMARY_MOLECULAR_EVIDENCE = VALUE_ONLY_256`

`VISIBILITY_CHANNELS = OBSERVATION_QC_CONTROL_ONLY__NOT_PRIMARY_MOLECULAR_INPUT`

Selection is deterministic columns `[0:256)` from authenticated V0/V1 512-channel parents. Visibility `[256:512)` is not materialized as a new primary feature array and may not silently re-enter the molecular path.

Authority artifacts:

- `docs/agent/V5_PRIMARY_REPRESENTATION_FREEZE_20260915.md`
- `docs/agent/V5_PRIMARY_REPRESENTATION_AUTHORITY_20260915.json`
- `docs/agent/V5_PRIMARY_REPRESENTATION_FEATURE_PARENTS_20260915.json`
- `docs/agent/V5_PRIMARY_REPRESENTATION_SELECTION_20260915.json`
- `docs/agent/V5_PRIMARY_REPRESENTATION_FREEZE_RECEIPT_20260915.json`

Canonical authority SHA-256:

`92756711fde939e27abc982d6ab1a0bc0dab53fae209c0f5a3fba4fde86ef4b1`

Freeze receipt:

`bc6f66c0b35c640caff80ee0922d0bc338f47fb1b562da07eb529348729c10a4`

This freeze does not claim biology, choose rank/model width, choose teacher semantics, or authorize training.

### 5.2 Frozen current authority: support/estimability invariants

Artifact:

`docs/agent/V5_SUPPORT_ESTIMABILITY_AUTHORITY_20260915.json`

Canonical authority SHA-256:

`cab2cecdd5ff31c2fbcaff408e1b1b7548eb2f72c1d3213931f1ce39188b6e08`

Frozen invariants:

- target component must be measured on the cell;
- unmeasured means missing, never zero;
- common core means comparability support, not biological truth;
- native measured support is evidence, not automatically private biology;
- support family does not imply latent semantic role.

Component-specific support assignment remains open inside teacher-target semantics.

### 5.3 Recovered/frozen current authority: base-training scientific estimand

Historical classification correction:

The September-15 handoff said the base-training estimand was open. Project history showed that the scientific target had already been frozen earlier and was never explicitly superseded. This was a governance/scope regression, not a need to rediscover the population target.

Recovered estimand:

`DONOR_UNIFORM__CELL_UNIFORM_WITHIN_DONOR_V1`

Scientific target law:

`p_i = 1 / (D * n_d)`

where `D` is the number of eligible donors and `n_d` is the number of eligible cells for donor `d`.

Normalization semantics:

`MEAN_OVER_DONORS__MEAN_OVER_ELIGIBLE_CELLS_WITHIN_DONOR`

Proposal separation:

`PROPOSAL_Q_MAY_DIFFER__EXACT_P_OVER_Q_CORRECTION_REQUIRED`

Operator policy:

`OPERATOR_DOES_NOT_SET_BASE_SCIENTIFIC_MASS`

Source policy:

`SOURCE_IS_DOMAIN_AND_ROBUSTNESS_STRATUM__NOT_AUTOMATIC_OBJECTIVE_MASS`

Artifacts:

- `docs/agent/V5_BASE_TRAINING_ESTIMAND_RECONCILIATION_20260915.md`
- `docs/agent/V5_BASE_TRAINING_ESTIMAND_CARRYOVER_AUDIT_20260915.md`
- `docs/agent/V5_RECOVERED_BASE_TRAINING_SCIENTIFIC_WEIGHT_LAW_20260915.json`
- `docs/agent/V5_BASE_TRAINING_ESTIMAND_AUTHORITY_20260915.json`

Recovered scientific-weight artifact canonical SHA-256:

`0ddab1d6d88dfd671ce615c7b470a2ce0545c4f06fe2388f48499e6eb18aa774`

Recovered base-training estimand authority canonical SHA-256:

`a766d42f9f8e37aa63e6c694d6c65b30962e45568f41b1cde8199cbed118ce26`

Population authority root:

`a771f08be31a840b5472448c438a153fbca7de93ba2ed31fe692eaeda02e6913`

Measurement-support eligibility root:

`7d61ed7bb649d129496c45cdf49adbb8b85faf7330803803287a2ec93631e4fd`

The recovered authority intentionally does **not** inherit proposal coefficients, source-mixture coefficients, operator-coverage coefficients, presentation horizon, affine order, physical packing sizes, optimizer settings, masking, geometry, seeds, or EMA timescale.

### 5.4 Implemented mechanics/schema successors — not all scientifically instantiated

Current modules added on the implementation branch:

- `src/sea_ad_jepa/v5/anti_cheat_authority_bundle_v1.py`
- `src/sea_ad_jepa/v5/base_training_estimand_authority_v1.py`
- `src/sea_ad_jepa/v5/base_training_estimand_recovery_v1.py`
- `src/sea_ad_jepa/v5/critical_test_execution_authority_v1.py`
- `src/sea_ad_jepa/v5/current_atomic_checkpoint_guard_v1.py`
- `src/sea_ad_jepa/v5/current_authority_closure_v1.py`
- `src/sea_ad_jepa/v5/current_authority_roots_v1.py`
- `src/sea_ad_jepa/v5/current_teacher_target_receipt_v1.py`
- `src/sea_ad_jepa/v5/current_trainer_preexecution_contract_v1.py`
- `src/sea_ad_jepa/v5/ema_presentation_v1.py`
- `src/sea_ad_jepa/v5/ema_timescale_authority_v1.py`
- `src/sea_ad_jepa/v5/masking_audit_metrics_v1.py`
- `src/sea_ad_jepa/v5/masking_authority_v1.py`
- `src/sea_ad_jepa/v5/measurement_robustness_authority_v1.py`
- `src/sea_ad_jepa/v5/model_geometry_authority_v1.py`
- `src/sea_ad_jepa/v5/primary_representation_authority_v1.py`
- `src/sea_ad_jepa/v5/production_protected_registry_authority_v1.py`
- `src/sea_ad_jepa/v5/qualified_optimizer_guard_v2.py`
- `src/sea_ad_jepa/v5/support_estimability_authority_v1.py`
- `src/sea_ad_jepa/v5/target_address_query_authority_v1.py`
- `src/sea_ad_jepa/v5/target_identity_shortcut_gate_authority_v1.py`
- `src/sea_ad_jepa/v5/teacher_target_semantics_authority_v1.py`

Current exact upstream root vocabulary:

1. `full104_substrate_sha256`
2. `representation_authority_sha256`
3. `support_estimability_authority_sha256`
4. `base_training_estimand_sha256`
5. `teacher_target_semantics_sha256`
6. `target_address_query_authority_sha256`
7. `masking_authority_sha256`
8. `model_geometry_authority_sha256`
9. `schedule_authority_sha256`
10. `ema_authority_sha256`
11. `anti_cheat_authority_sha256`
12. `runtime_source_sha256`

Receipt adds:

`preexecution_authority_sha256`

The closure validator cross-checks that these authorities refer to the same upstream roots and always returns `training_authorized = false`.

### 5.5 Current preexecution / receipt / optimizer / checkpoint anti-carryover repair

The successor branch contains current versions that do **not** intentionally delegate to the old V2/V4 authority chain:

- dynamic protected registry rather than fixed 48 tensors;
- explicit critical-test execution authority; skipped/non-executed is not equivalent to pass;
- geometry-neutral current preexecution root closure;
- fresh current teacher-target receipt with exact root vocabulary;
- optimizer resident guard rebound to the current receipt;
- neutral current atomic checkpoint guard deriving protected count from the current registry;
- cross-authority closure to catch root splicing.

Historical fixed 6-block/48-tensor/128x8 vocabulary is not current authority.

## 6. Verification evidence from this chat

Do not collapse the following into one fabricated “full suite pass.” They were separate verification checkpoints during incremental construction:

- early successor scaffold: `84 passed`, runtime sentinel intentionally deselected;
- after representation freeze: `87 passed`, runtime sentinel intentionally deselected;
- support-root integration subset/full reconstructed successor mechanics checkpoint: `49/49 passed`;
- estimand recovery/integration unit: `24/24 passed`;
- compile checks were clean at those checkpoints;
- carryover scans at those checkpoints found zero hits in the new production sources for the tracked forbidden V2/V4 geometry/EMA/masking tokens;
- current runtime file remained intentionally absent during those checks.

Because the environment/workspace was recycled between some checks, **the next chat should rerun the complete successor test set from the exact implementation SHA before claiming one final branch-wide test total.** Do not turn infrastructure/test-discovery failure into green evidence.

## 7. Accidental-carryover firewall — keep checking continuously

Known forbidden or dangerous carryover seams:

1. V4 `PRODUCTION_CONFIG` becoming current V5 authority.
2. V5 runtime delegating to historical V4 `production_update`.
3. V2 preexecution six-block / 48-tensor assumptions.
4. historical `HISTORICAL_128X8_CORRECTED_UPDATE_REGRESSION` vocabulary.
5. historical `160 / 4 heads / 6 blocks / batch128 / microbatch8 / 4 views / 0.40 mask / 16 target blocks / 0.996 EMA` becoming defaults.
6. legacy target-receipt validation becoming current receipt authority.
7. visibility channels silently re-entering the primary molecular path.
8. `z_bio`/`biology_*` naming becoming evidence of biology.
9. common-core support being treated as biological truth.
10. native support being treated as private biology.
11. dimension-estimand weights becoming base-training weights.
12. D_shared qualification ceiling `512` becoming production model width.
13. shared online trainable target-identity table remaining in the predictor query path.
14. proposal/source/operator balancing becoming scientific target mass.
15. schema existence being mistaken for a frozen instantiated authority.
16. receipt closure being mistaken for training authorization.

Before each implementation unit, run static scans plus direct dependency inspection, not only tests.

## 8. Target-address / identity shortcut — current active investigation

Historical discovery is `ALREADY_AUDITED`:

Inherited structural path:

`HIDDEN_TARGET_ID -> PREDICTOR_QUERY -> JEPA_GRADIENT -> ONLINE_GENE_IDENTITY -> EMA_TEACHER -> FUTURE_TARGET`

No hidden expression leakage is alleged. The risk is identity-only predictability and co-adaptation.

Current branch already contains the generic schema:

`src/sea_ad_jepa/v5/target_address_query_authority_v1.py`

It requires explicit:

- address registry root;
- query provider identity;
- query artifact hash;
- replay policy;
- parameter-sharing policy;
- gradient policy;
- `training_authorized=false`.

This is **schema-only**. It does not choose the provider.

History review during the latest work found:

- older singleton/target-query code is sampling-only: it chooses which already-hidden target identities are queried;
- it does not itself define a separate/frozen target-identity embedding provider;
- inherited `BlockPredictor` mechanics can accept an identity embedding, so the predictor itself is not necessarily the authority defect;
- the V5 inherited call site passes `modules.online.tokenizer.gene_identity` directly, enabling predictor gradients to update the same online identity table that later enters the EMA teacher;
- no already-frozen separate/fixed provider was recovered under the obvious historical query/provider searches before this handoff.

Therefore current classification:

- shortcut discovery: `ALREADY_AUDITED`
- provider choice: `OPEN`
- Stage-A structural dependency/gradient audit: `OPEN CURRENT NEXT WORK`
- Stage-B empirical identity-shortcut comparator run: **not authorized until target semantics, comparator rule, thresholds, masks and weights are prospectively frozen**.

Leading candidate remains a fixed/separate replay-stable target-address code, but it is not frozen.

Required prospective comparator families already identified historically:

- `IDENTITY_ONLY_SHARED_CURRENT`
- `IDENTITY_ONLY_FIXED_ADDRESS`
- `FULL_SHARED_CURRENT`
- `FULL_FIXED_ADDRESS`
- `FULL_STOPGRAD_ONLINE_ID`
- `CELL_PERMUTED_VISIBLE_MEMORY_WITH_TARGET_IDS_FIXED`
- `TARGET_ID_PERTURBATION_WHERE_LAWFUL`

Do not run them until the decision rule is frozen.

## 9. Masking — schema exists, policy still open

Current branch contains:

- `masking_authority_v1.py`
- `masking_audit_metrics_v1.py`

The schema intentionally contains no historical mask fraction, no target-block count, and no assumption that pooled covariance equals biology.

Outcome-blind audit metrics include concepts such as:

- correlated-partner exposure;
- address coverage;
- mask-budget concentration;
- rare-address coverage;
- source/operator dependence;
- deterministic replay;
- invalid masking outside measured support.

Policy still open. Candidate comparators from prior planning:

- `GRAPH_FREE_UNIFORM`
- `HISTORICAL_GRAPH_EXPANDED_DIAGNOSTIC`
- `DONOR_SOURCE_RECURRENT_OR_CROSS_SOURCE_CONSENSUS_HYBRID`

Do not blindly restore historical Pearson-graph masking.

## 10. Teacher-target semantics — schema exists, scientific instantiation open

Current branch contains:

`teacher_target_semantics_authority_v1.py`

It forces explicit binding of:

- teacher input support;
- teacher state layer/location;
- aggregation;
- normalization;
- target-address authority;
- student-visible support;
- scientific-weight authority;
- masking authority;
- gradient boundary;
- EMA boundary.

The inherited candidate `LN(mean(final teacher gene states over target block))` is **not** current authority merely because it is executable historically.

Do not hard-code final layer / block mean / post-mean LN / 16 blocks / 40% mask by inheritance.

## 11. EMA — mechanics available, timescale still open

Mechanic helper:

`m_u = exp(log(0.5) * p_u / H)`

where:

- `p_u` = authorized scientific presentation mass contributed by update `u`;
- `H` = half-life expressed in the same authorized presentation unit.

Current branch has both `ema_presentation_v1.py` and `ema_timescale_authority_v1.py`.

Historical `.996` is forbidden as implicit authority.

Open choice:

- presentation unit;
- half-life `H`.

## 12. Measurement robustness — schema exists, decision rule open

Current branch contains `measurement_robustness_authority_v1.py`.

The old thinning ladder/result is historical qualification evidence only. A deep objective-aligned measurement-robustness rule still requires prospective binding of:

- perturbation protocol;
- primary metric;
- acceptance margin;
- stratification guardrail;
- failure semantics.

Do not invent a margin after results are visible.

## 13. Production geometry — schema exists, real dimension outcome remains sealed

Current branch contains `model_geometry_authority_v1.py` plus dynamic protected registry mechanics.

Do not set width/rank from:

- historical width 160;
- six-block mechanics;
- D_shared qualification ceiling 512;
- Layer-2 proxy rank;
- common-core width.

Production geometry must bind to the protected dimension chain only after all upstream design choices are frozen and real dimension outcomes are lawfully opened.

`NO_D_SHARED_OUTCOME_EXECUTION_OR_INSPECTION` remains active.

## 14. Anti-cheat bundle / checkpoint closure

Current anti-cheat bundle is intentionally a bundle of independent authority roots rather than a boolean “anti-cheat passed.” It binds the target-identity gate, masking, measurement robustness, observation-gradient firewall and critical-test authority.

Current checkpoint guard derives protected tensor count from the dynamic registry and rejects opened forbidden gates or root splicing.

This structure is good. Do not convert it into a single unstructured pass flag.

## 15. Runtime state

The intended current runtime file remains intentionally absent:

`src/sea_ad_jepa/v5/current_teacher_student_runtime_v1.py`

That absence is a fail-closed state, not missing cleanup.

Do not create the runtime until the upstream scientific authorities it would consume are genuinely frozen and cross-bound.

No production training authorization exists.

## 16. Current classification of predecessor “open” items

The predecessor V15 handoff listed many items as open. Current status is:

### Now frozen / reconciled

- primary molecular representation -> `VALUE_ONLY_256` channel-role authority frozen;
- support/estimability invariants -> frozen;
- base-training estimand/scientific-weight root -> recovered from prior scientific authority and frozen;
- preexecution successor mechanics -> implemented current schema;
- current receipt/optimizer guard mechanics -> implemented;
- dynamic protected-registry/checkpoint pattern -> implemented current successors;
- cross-authority closure -> implemented.

### Schema/mechanics implemented, scientific choice still open

- exact teacher-target semantics;
- target-address provider;
- identity-shortcut thresholds/decision rule instantiation;
- masking policy;
- EMA presentation unit/half-life;
- measurement-robustness protocol/margin;
- production model geometry;
- anti-cheat bundle instantiation with all qualified component roots;
- schedule root/current schedule binding where required by the final closure;
- observation-gradient-firewall authority binding into final anti-cheat closure.

### Still genuinely open outside this branch

- HVS/NPH52 donor-transport explanation without forced invariance;
- V3-null governance reconciliation;
- real protected dimension-chain outcome after lawful unsealing;
- explicit current runtime implementation;
- full mechanics/anti-cheat qualification with frozen authorities;
- explicit production-training authorization after every upstream root is immutable.

## 17. Exact next work order for the new chat

Do not branch into unrelated audits. Continue in this order:

1. Re-fetch live heads and verify the implementation branch is still fast-forward from main.
2. Run the complete successor tests from exact implementation SHA. Keep infrastructure failures separate from test outcomes.
3. Run whole-successor static carryover scans for the forbidden seams listed above.
4. Finish target-address Stage-A structural audit:
   - identify exact inherited online identity tensor;
   - identify every consumer in target-query construction;
   - prove whether predictor loss gradient reaches it;
   - prove whether EMA copies its updated state into the teacher;
   - identify any alternative historical provider and classify it;
   - produce a fail-closed structural audit artifact, not an empirical result.
5. History-check the target-address provider choice. If no prior authority exists, design the provider prospectively.
6. Freeze target-address provider semantics and static gradient isolation tests before objective-aligned Stage-B.
7. Reconcile teacher-target semantics around that provider without inheriting V4 final-layer/block-mean/LN by accident.
8. Freeze masking decision rule and outcome-blind audit thresholds before any heavy masking statistics are computed.
9. Freeze target-identity shortcut comparator decision rule and thresholds before results.
10. Freeze measurement-robustness decision rule before deep perturbation results.
11. Bind schedule + EMA presentation unit/half-life.
12. Only when all upstream choices are immutable, open the protected dimension chain and derive production geometry.
13. Implement the distinct current runtime with TDD and the carryover firewall active.
14. Run full mechanics/anti-cheat qualification.
15. Create explicit production-training authority only after all prior roots pass and remain immutable.

## 18. Heavy/GPU work division

Do not send Claude/GPU work that is already audited.

Heavy-machine work becomes useful only after the corresponding prospective specs are frozen, for example:

- outcome-blind dependency graph statistics for the frozen masking specification;
- full-pop support/address-frequency summaries needed by a frozen mask policy;
- objective-aligned target-identity comparators after provider/metrics/margins are frozen;
- deep measurement-robustness experiments after protocol/margin freeze;
- full-pop mechanics qualification after current runtime and contracts are fixed;
- protected dimension-chain execution only after lawful upstream closure.

The 30+ GB FULL104 heavy substrate remains on the external GPU-enabled laptop/hard drive and should be referenced, not duplicated into chat handoffs.

## 19. Uploaded/heavy asset references

Available/referenced project assets include:

- Foundation 41K discovery expression archive, historical SHA-256 `63239898b9c93f29c20b62b84dc9b94c2c87e3e3f2b7958b7435847e3b9541f7`;
- Foundation calibration bundle, historical SHA-256 `07748d5bd21fe0857ccad3002fba3946d1791d25898b841d41056a3707117444`;
- `stage81a3r_corrected_real_train.zip`, SHA-256 `3b86097d514f1cc2d84b19fd289344e0634356359462173a9b2b0f7541dd7e48`, corrected TRAIN-cache scope only;
- `t1_checkpoint_u0200.zip` is historical T1 trajectory/checkpoint evidence, not a current runtime artifact;
- heavy FULL104 expression path and manifest listed above.

Use the repository heavy-asset reference/status files for authoritative path/size/SHA detail. Do not assume runtime assets are valid merely because filenames exist; preserve known provenance warnings.

## 20. Formulas to carry forward

### Base training scientific target

For donor `d`, with `n_d` eligible cells and `D` eligible donors:

`p_i = 1 / (D * n_d)`

If computational proposal is `q_i`, correction must recover the scientific target exactly:

`w_i = p_i / q_i`

subject to the frozen proposal/schedule authority and normalization convention.

### Presentation-normalized EMA

`m_u = exp(log(0.5) * p_u / H)`

EMA update:

`theta_teacher <- m_u * theta_teacher + (1 - m_u) * theta_online`

Only after a proved optimizer step. EMA must not advance on a skipped step.

### Candidate direct JEPA target loss structure

A generic form discussed prospectively is:

`L_direct = [sum_i w_i * mean_(b,d)(y_hat_ibd - stopgrad(zT_ibd))^2] / sum_i w_i`

This formula does **not** freeze `zT` construction. Teacher state location, aggregation and normalization remain separate semantics.

### Identity-shortcut diagnostics

Prospective examples already discussed:

`F_id = 1 - L(identity_only) / L(null_reference)`

`Delta_mol = L(identity_only_same_address_code) - L(full_same_address_code)`

`Delta_share = L(full_fixed_address) - L(full_shared_current)`

Do not use these to inspect real results until the exact null, metric, margins, rare-address guardrails and failure semantics are frozen.

## 21. What must never be inferred from the current state

Do not infer any of the following:

- VALUE_ONLY is proven biological;
- within-donor signal is proven biology;
- source/operator structure is technical batch;
- common-core is biological truth;
- native support is private biology;
- the target-address fixed-codebook candidate is approved;
- graph masking is approved;
- uniform masking is approved;
- `.996` EMA is approved;
- `512` is production width;
- closure/receipt means training is authorized;
- successful historical mechanics mean the current runtime may delegate to V4;
- a schema file means its scientific choice has been frozen.

## 22. Final handoff state

The project has moved materially beyond the earlier V15 handoff:

- representation is now formally frozen to VALUE_ONLY_256 for the primary molecular role;
- support/estimability invariants are first-class current authority;
- base-training estimand was recovered from already-frozen historical scientific authority and bound to current V5;
- the current authority graph now has explicit root vocabulary, cross-root validation, current preexecution/receipt/optimizer/checkpoint mechanics, and anti-cheat component schemas;
- the runtime remains intentionally absent;
- the main active scientific blocker is now target semantics, beginning with target-address identity/provider authority and Stage-A gradient/carryover closure;
- masking, EMA timescale, measurement robustness and model geometry remain genuine open decisions behind their respective gates.

Keep working from the exact implementation SHA. Do not restart the project from older handoffs, and do not let accidental V4/V21/T0 carryover substitute for current authority.

`TRAINING_OFF`

`NO_D_SHARED_OUTCOME_EXECUTION_OR_INSPECTION`
