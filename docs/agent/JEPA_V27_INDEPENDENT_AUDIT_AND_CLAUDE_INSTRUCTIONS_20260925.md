# JEPA V27 independent audit and exact Claude takeover instructions

**Dated:** September 25, 2026 (US Eastern). **Scope:** independent review of PR #147 exact head `9f7818472bdf515680b85eddbbf761eae2208020`, the governing main `START_HERE.md` V25, source issuance contract, PR #146 committed physical metadata, and active V26 ownership branches. **Docs-only audit, does not grant data access, training authority, or permission to change any existing gate.** The main controlling state is V25 until a reviewed merge. Re-fetch every head; hashes below are dated evidence, not instructions to check out obsolete code.

## 1. Audit verdict: distinguish observed execution, code existence, and missing authority

| Claim | What can be verified now | Qualification / challenge |
|---|---|---|
| PR147 V5 test sweep | 1,620 collected; 1,617 passed, 0 skipped, 3 failed. Current V4 freeze separately passes 21 source rows/7 active tests, 0 failure. | The overall sweep is **not** an all-green pass. V1/V2/V3 freeze failures are historically superseded, but need explicit test retirement/xfail with a re-verifiable V4 baseline; do not hide or blanket-ignore. |
| PR147 Phase1A physical bridge | Published `results/v26/PASS1_READER_FIT_COUNT_BRIDGE_V1.json` says 104 per-donor exact count match / 4,553,407 cells against frozen pass1 SHA `37f79e49f11364daa487ad9e5a5680f72378daf338852765d2f52e1e98d90ba1` and authenticated calibration ZIP `07748d5bd21fe0857ccad3002fba3946d1791d25898b841d41056a3707117444`. | Producer performed 0 raw Level4 block reads. Identical-count donor swaps undetectable by histogram; per-cell donor lineage, raw-level source-library and proposal q not validated. The misleading 1.093GB same-named ZIP `faa52d8e…` must never be accepted as the frozen 410,278,055B bundle. |
| PR147 mechanics | Committed 54,990-byte synthetic 40-update CPU receipt, per-step nonzero gradient records, 59/59 Adam moment evidence, exactly-one optimizer counter, 0 EMA residual and replay fingerprint parity. | **No GPU or reader_fit scientific training.** Strict gate rechecked against saved 40 records *after the run*, not enforced inline on the original trajectory. Re-run a bounded fixture with fail-closed inline checks before claiming an online live gate. |
| PR147 geometry | Exact published driver has `cells_per_update=12`, `width=32`, `heads=4`, `blocks=2`, `vocabulary_size=96`, `ema_momentum=.99`, `mask_fraction=.40`. | Contract §3 erroneously states **24 cells/update** and **64/8/128** width/heads/FFN. Receipt and driver agree on 12/32/4/64. §3 also says the first attempt used 24 cells; that is *not* the accepted 40-update run. Correct documents without rewriting original receipt or transposing synthetic choices into V5. The synthetic .40 mask is **not** an authorized real-data mask fraction. |
| PR147 GSE301119 tie-safe | V27 state records real tie-safe rerun, same observed/null medians −1.3446/−1.5040, changed extreme-tail statistics and source certificate. | Negative control still invalidates this perturbation estimator for JEPA benchmark truth; separate ETL lane, **not** a prerequisite for the healthy JEPA engineering run. |
| B1 access | `docs/agent/JEPA_POPULATION_ACCESS_REGISTRY_V1_20260907.json`: reader_fit `ELIGIBLE_POOL__NOT_EXECUTION_AUTHORITY`; new teacher training forbidden without a **separate frozen contract**. | Do not edit the registry to pretend permission. Present a bounded scientific decision packet and seek real prospective approval under the existing policy. |
| B2 issuance | `current_training_authority_v1.issue_training_authority_v1` checks exact closure V2, preexecution V2, receipt V2 with frozen teacher-target package root, critical-test and runtime-source authority digest parity. | Finding a target package and granting a named training contract are **necessary, not sufficient**; V2 upstream closure lists **32 exact roots**, and both target receipt and preexecution roots must close. No actual frozen package or closure V2 artifact was identified in PR147. Do not invent digests or authorize via a syntactically plausible placeholder. |
| PR146 physical donor data | Exact-main-based [PR #146](https://github.com/dushyant-mishra/sea-ad-jepa-agent/pull/146) @ `8369efe5839f9aca5912cc8280a05330b2b104a3` publishes original real 104-donor/42-operator/1,400-group CSVs, producer, independent verifier, exact local input roots and 24 adversaries. Exact-head CI [36213175528](https://github.com/dushyant-mishra/sea-ad-jepa-agent/actions/runs/36213175528): output verifier pass; 24/24 tests pass, zero skips. | Original heavy-input authentication was physically run **locally**; GitHub CI replays the committed derived evidence but does **not** reread the 410MB ZIP/2.7GB SQLite. Base JEPA weight `1/(D*n_d)` differs from separate D1-P002 donor×operator analytical weight `1/(D*|O_d|*n_do)`. |
| G3 fit law | [PR #143](https://github.com/dushyant-mishra/sea-ad-jepa-agent/pull/143) reference independent row-level tests 28/28; [PR #144](https://github.com/dushyant-mishra/sea-ad-jepa-agent/pull/144) 50/50 integrated synthetic tests, explicit matched fit in primary and RIDGE8. | Remain separate drafts. The legacy scorer still maps undefined heldout correlation to zero; new G3 rows are development-only. Physical source/fold binding, six-state scoring and authorized current-V5 evaluation are not proven by these tests. G3 **evaluation qualification** is distinct from JEPA engineering mechanics. |
| All104 raw-count verification | PR #120/#124 code exists; PR147 V27 state says latest 8,915-block attempt **terminated on a session limit**, no result. | Never infer PASS from timeout or from PR132's metadata bridge. Capture a restartable per-block receipt before a new attempt; no N1 terminal mask outcomes without their separate authorization. |
| Historical97/94 linear proxy and external assays | Historical Layer2 recorded score scope is selected94, and PR133 authentic original `bind_population.npz`, `screen_out.npz`, `final_manifest.csv` not found on original machine scan. PR137 clean screened microglia pair not benchmark truth. | Do not present old94 values as current104, or GSE301119/CRISPRbrain as validated biological outcomes. |

## 2. Claude: required order of operations (do now without opening real expression)

1. **Re-fetch active heads and make a separate clean worktree.** Read `main:START_HERE.md`, main latest handoff pointer, PR #147 V27 handoff/state, this audit, original FULL104 ETL September21 report/receipt on PR77, and PR #146 index. Inspect PR #141 owner/collision map. Do not use one worktree for multiple subagents. Record exact branch/base/head, source SHA and source file role before any new execution. Write only to your owned new `review/v27-...` branch; PR #147, #146, #139, #144, #145, PR #120/#124 have distinct owners. Do not blindly merge stacked PRs.
2. **Repair the truthful status and test hygiene first.** Correct PR147 contract's 24-vs-12 and 64/8/128-vs-32/4/64 inconsistency via append-only correction on your own review branch; identify the exact driver version whose receipt hash is recorded and do not rewrite that historical artifact. Establish current V4 freeze as sole active baseline while superseded V1–V3 tests remain visible as historical checks (explicit markers and red-team that V4 source mutation really fails). Re-run the full *selected active* suite with strict census; report all old failures separately. Repair Windows `resource` import path only if this can be demonstrated without altering the scientific preflight estimand.
3. **Retest NumPy in the actual intended GPU runtime.** In both `sea-ad-jepa` and healthy `sea-ad-jepa-v3`, run a tiny independent subprocess that imports NumPy and performs a 3×3 matmul, log exit code, version, BLAS config and OS error. The defective environment should be quarantined; do not run critical scientific scripts there. Inventory **actual historical output receipts and their recorded environment**, identify specifically which NumPy-linear-algebra scientific results could have been contaminated, and re-execute only those with frozen original data/code/estimand in the healthy env. Do not assume every previously published result is bad or regenerate missing old94 inputs as a fake replication.
4. **Enforce the protected-gradient predicate inline before the next optimizer step.** The current PR147 script aggregates results and labels the strict check after finishing all updates; preserve the original 40-update receipt as historical. Add a test that injects a DEAD-GRADIENT gate object and proves the new runner stops immediately, *before* optimizer, EMA, presentation cursor or checkpoint advances. Also inject absent/malformed report fields, skipped optimizer step, empty Adam moments and partial checkpoint. Re-run a separate bounded synthetic CPU trajectory (no real reader_fit), record a new versioned receipt, exact code SHA, 0 skips and the exact update where a planted failure stops. Do not call the 40 historical records “inline gated.”
5. **Use PR #146's existing real metadata, not a new metadata scan.** Source-bind actual 104 fit donors and their canonical IDs, 42 operator/support rows and 1,400 donor/operator groups. Check against PR147 exact pass1 bridge and PR #142 source partition crosstab. Preserve the actual 81-cell smallest-donor constraint. Normalize scientific cell mass `p_i=1/(|TRAIN_DONORS|*n_d)` over the **actually selected, prospectively frozen training donor set**; if a new development-heldout fold exists, recompute denominator and counts from that fold only. Never substitute D1's donor×operator weights. A model trained on all104 has no independently unseen reader_fit donor; do not retroactively call all104 scoring LODO.
6. **Prepare the real target-package decision sheet without choosing its outcomes.** Inventory all existing current-V5 approved target-construction, teacher/state semantic, target-address, measured visibility, masking candidate, evidence-budget, support, normalization, augmentation and EMA/schedule authorities; list each exact currently available source/digest and every absent/unqualified root. Explicit scientific goal: **predict query-local hidden-gene *latent cellular state* from remaining RNA, not the hidden gene's scalar value, global cell identity, QC or source support**. Preserve measurement-state channels as separate negative-control outputs, not a privileged shortcut into the primary molecular state. Present the smallest single named prospective *development* candidate and unresolved decision(s) for user/scientific approval. The September16 frozen masking K/R nine-cell grid had no qualified winner; do not relabel failed arms qualified, relax thresholds after looking, or open protected N1. A deliberately unqualified candidate may be evaluated only under a *separately explicit development-only authority*, not claimed production.
7. **Audit *all* V2 closure roots for actual executable bindings, not only B1/B2 labels.** Code schemas alone are not a frozen package or authorization. Inspect `current_authority_roots_v2.py` (32 upstream), `current_authority_closure_v2.py`, `current_trainer_preexecution_contract_v2.py`, `current_teacher_target_receipt_v2.py`, `current_training_authority_v1.py`, and respective validators. Produce a machine-readable table `root_name → expected authoritative artifact/digest → exact source path/head → independent validation evidence → blocking status`. **NEVER** fill an unknown digest with repeated hex, schema digest instead of actual artifact, a synthetic fixture root or a receipt copied from another role. No closure V2 / receipt V2 / training authority is issued before every current prerequisite is truly met.
8. **Create a reviewable *proposal*, not an authorization forgery.** Put the precise teacher-target package candidate (inputs, teacher query target, loss, donor population/split, one mask, width/tokens/EMA/update/horizon and evidence/control plan) in a non-authorizing decision document with exact upstream SHA roots and missing-value markers. Separate independently derived parameters (FULL104 source masses/support/group capacities), declared GPU resource-bounded provisional instrument values, and scientifically unresolved D_shared/D_private/D_obs/D1/measurement-shortcut boundaries. Highlight the actual questions whose scientific approval is required. The project owner/scientific authority must approve the separate frozen reader_fit diagnostic contract according to the registry, after which you can run existing official issuance/closure functions with *verified* artifacts only. Do not “sign” for the owner by writing `training_authorized=true` or editing a gate.
9. **Only after documented authorization:** make the *one* missing exact-current-V5 FULL104→harness adapter, with source/pass1/reader-split identity revalidation and actual row-to-donor identity checks, operator-specific structural measured vs zero vs unresolved collision support, explicit target query/visibility/remaining-RNA mask, source-library denominator check and declared sampler q/p correction. Integrate current-v5 gradient-gate/optimizer-once/EMA/checkpoint proof and strict reject-on-any-failed-invariant semantics. Add 3 adversarial data-path controls that rehash fake donors/sources, remap same-count donor IDs, and perturb raw denominator while preserving visible values: the metadata count bridge cannot catch them. Physically execute exactly the bounded authorized current104 development plan once on GPU; do not restart 40 historical synthetic iterations as real JEPA or present V4 freeze as physical current V5 training.
10. **Separate evaluation from execution:** after an authorized healthy checkpoint, the first *scientific* diagnostic additionally needs objective-matched G3 evaluation from PR143/144 with verified real fit-fold donor masses and the existing six-state heldout estimability/coverage contract. Source-balanced held-out scoring is a separate estimand from donor-uniform fit. Keep historical cell-weighted/source-balanced **fit** as prespecified sensitivity arms. If physical G3/scorer qualification is not complete, report gradients/optimizer/EMA as ENGINEERING_ONLY and do not infer biological gain or safe masking. The strict three-source common core is 17,186 scalar measured in all42 operators; 17,346 is a different “measured in at least one per source” condition. Do not conflate.
11. **Raw all104 separate lane:** PR120/#124's 8,915-block raw-count task stopped on a session limit. Do not treat as green. If there is already authority to execute its **outcome-blind read-only verification**, resume only after partitioning into deterministic disjoint block chunks with atomic per-chunk source SHA/row count/cursor receipts and a final *all-block exactly-once* reducer. If access/authority differs, stop with an exact request for it. This is raw input qualification only, **not** authorization to open terminal masking N1 or other protected outcomes.

## 3. Required handoff and acceptance evidence

Publish separate, reviewable outputs (do not mutate authoritative older branches): (a) one corrected scope/status addendum identifying PR147 documentation mismatches and keeping original receipts immutable; (b) exact test/CI log including zero-skip/xfail/deselected accounting, V4 mutation-negative control and new inline-gradient negative control; (c) environmental 3×3 BLAS audit with provenance-matched affected-run inventory; (d) actual FULL104 metadata-to-current-pass1/selected-split join receipts, without per-cell-lineage claim before testing it; (e) 32-root closure and teacher-target package **decision matrix**, no guessed digest; (f) prospective named training-contract **proposal for user approval**, not an issued contract; and, **only if separately authorized**, (g) exact current-V5 GPU adapter source/test and fresh healthy limited real reader_fit training receipt. Explicitly call out open PR120 raw lane and G3/six-state score integration, as appropriate.

Report status with no green-by-nonexecution: `PHYSICAL_EXECUTED`, `SYNTHETIC_TESTED`, `INDEPENDENT_REPRODUCED`, `BLOCKED_BY_AUTHORIZATION`, `NOT_EXECUTED`, `NOT_ESTIMABLE`. Empty/missing verifier outcomes are **failures to verify**, never “0 defects”. Prefix every biological claim with its valid real source/population and exposure scope. Preserve all protected reader_validation22/oracle23, foundation development/sealed, whole-study Siletti, pathology, N1 terminal outcome and D_shared sealed until their separately named release criteria truly pass. Report any unexpectedly touched protected field and halt.

**First response expected from Claude:** a bounded 32-root inventory with exact source-head status, the corrected actual synthetic geometry, a precise list of scientifically unapproved choices and an isolated code/test plan; do not begin real-data training. Work on independent worktrees for independent lanes, with explicit owners and exact Git commit/byte evidence. Stop at the B1/B2 gate unless the owner issues a real frozen contract and every other V2 validation succeeds.
