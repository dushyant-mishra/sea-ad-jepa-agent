# JEPA NEW-CHAT HANDOFF — 2026-09-14

## Purpose
This is the restart-safe transfer package for continuing the JEPA project without restarting analysis from scratch.

Repository: `dushyant-mishra/sea-ad-jepa-agent`

Active branch at handoff creation: `repair/v5-dshared-authority-v2-20260914`

Live head at handoff creation: `d9938dd782443b5310919ecf6c14099c0b4c23ec`

The next chat MUST re-fetch the live branch before any current-state claim or write because Claude or another lane may have pushed newer work.

## Read order
1. `START_HERE.md`
2. `docs/agent/JEPA_LATEST_HANDOFF_POINTER_20260914_CURRENT.json`
3. `docs/agent/JEPA_NEW_CHAT_HANDOFF_20260914_CURRENT.md`
4. `docs/agent/JEPA_NEW_CHAT_HANDOFF_STATE_20260914_CURRENT.json`
5. `docs/agent/V5_CURRENT_AUTHORITY_AND_BLOCKERS_20260913.md`
6. `docs/agent/V5_MASTER_OPEN_ITEMS_20260913.md`
7. `docs/agent/V5_T0_LESSONS_DATA_AWARE_MEASUREMENT_QUALIFICATION_COURSE_CORRECTION_20260914.md`
8. `docs/superpowers/specs/2026-09-14-v5-dshared-reconnaissance-resolution-v1.md`
9. `docs/agent/V5_CLAUDE_FULL104_RECON_AND_MATCHING_RESOLUTION_20260914.md`
10. `docs/agent/V5_PROSPECTIVE_D_SHARED_AUTHORITY_V2.json`
11. `docs/agent/V5_FULL104_RECONNAISSANCE_AUTHORITY_V1.json`
12. `docs/agent/V5_EXTERNAL_COHORT_ROLE_REGISTRY_20260913.json`
13. `docs/agent/V5_ENVIRONMENT_PREFLIGHT_REQUIREMENT_20260913.md`
14. `docs/agent/T0_V5_ORIENTATION_AND_CROSSWALK_20260912.md`
15. `docs/agent/T0_MEASUREMENT_METHOD_CLOSEOUT_20260912.md`

## Governing rule
Permanent scientific order:

`DATA -> SUPPORT/ESTIMABILITY -> SCIENTIFIC ESTIMAND -> PRODUCTION GEOMETRY -> MODEL`

Current refinement:

`UNDERSTAND_FULL104_DEEPLY__KEEP_FINAL_D_SHARED_HYPOTHESIS_TEST_SEALED`

Prospective design does NOT mean blindness to the dataset. FULL104 should be characterized deeply enough to understand donor/operator/source balance, sparsity, support, technical confounding, covariance, feature geometry, matching feasibility, leverage, conditioning and estimator feasibility. What remains sealed is the final decision-bearing D_shared result: rank-wise effects, null-effect values, pass/fail curves, held-donor qualification values, selected rank and final biological conclusion.

If decision-bearing D_shared outcomes are opened before the complete procedure is frozen, that run becomes exploratory and cannot retain independent confirmatory standing after outcome-informed design changes.

## Hard boundaries
All remain FALSE:

- `training_authorized`
- `protected_data_authorized`
- `numeric_dimensions_authorized`
- `d_shared_real_outcome_access_authorized`
- `d_private_execution_authorized`
- `d_obs_execution_authorized`
- `td60_authorized`
- `relational_target_activation_authorized`
- `external_validation_outcomes_authorized_for_design`

No current-V5 D_shared metric/rank/null/held-donor qualification value, D_private result or D_obs result has been generated or inspected.

## T0 status and course correction
T0 remains CLOSED.

Frozen terminal:

`T0_MEASUREMENT_PROCEDURE_NOT_QUALIFIED_AT_N28__BIOLOGY_UNRESOLVED`

T0 tested a specific latent/common-factor measurement procedure at n=28. V5 D_shared is NOT the same procedure. However, the T0→V5 audit identified a transferable failure mode: V5 was at risk of freezing a sophisticated measurement procedure before fully characterizing the real FULL104 data and before proving that the procedure can distinguish intended shared biology from structured technical dependence. This is now explicitly on the V5 critical path.

Do not reopen T0 simply because V5 needs better characterization. Preserve the lesson that estimator/measurement failure is not equivalent to absent biology.

## FULL104 production substrate
Authenticated geometry:

- 4,553,407 cells
- 104 donors
- 42 operators / 42 matrices
- 8,915 expression blocks
- 41,238 Molecular Ledger addresses
- level 4
- production sources SEA-AD + HVS + NPH52

Heavy path available to Claude on the user's GPU laptop/external drive:

`D:/Jepa project/outputs/full104_v014_20260826/03_phase2_state_derivation_v1/expression_level4/`

Do not ask the user to upload FULL104 (>30 GB).

Key frozen hashes:

- block manifest `66f589e56badb1487058f2c95940c3e4b37196e3ab5e9c6ea1ffbe7098d2ea29`
- materialization contract `612b45742ad80498cbe2f061a75af08c0a10692dc731e0ac8e649417b7e62f17`
- materialization audit `9fa0ede3135a606bb1fe4cd4cc11881c439b7726b6dec62147c1892967eba7cf`
- selection `edec0fe29d1425ecbe9fa889a610c4ce18621ae060c8144866315db57c3fc62b`
- selection manifest `3db3614bf544b183143f39b27bad516b3a7a75284df4b2410d9f3e99f0b0842e`
- metadata SQLite `a771f08be31a840b5472448c438a153fbca7de93ba2ed31fe692eaeda02e6913`
- dimension interface `dcc8c95ef8ed4b8106ee3b8f1536aa6fac6b338cafd3057b9f567a5336c673df`
- sealed FULL104 dimension-input artifact `eb1264489306413fb57316abe7f70205a771248881302d8c867c70b69273f1ad`
- historical feature-matrix package root `c9a6ede6f33a4a9d4ce22cde0f1a8c0fb5e7e039bac3df364325c778487329ef`
- historical multiview root `d6f70ee1bca777f3d2cbd89dd560395cc46ec25f6a648ae4ba0fd6e77c1c2cf1`

## D_shared Authority V2
Artifact: `docs/agent/V5_PROSPECTIVE_D_SHARED_AUTHORITY_V2.json`

Canonical SHA-256: `f9568eb19a22f106b0be3ce0580bc1058695ca1a2de6dd2d0475c3240a6bbda2`

Status: `FROZEN_BEFORE_D_SHARED_OUTCOME_ACCESS`

V1 provenance remains immutable:

- precision V1 `cc4ac4d5116fa81990f1c3bd0497fc578eda86bb2d7d3cd2747abcf7ffcf9428`
- V1 cross-binding `eccc30f9f5d01f7905a0740982d678d0ce45532a08cd5a61c51f5448b63b3d3a`

V2 closed J1–J5: scalar replicate collapse, unspecified unique null generator, missing effect criterion, unfrozen rank envelope, and missing quantity×rank multiplicity allocation.

Frozen V2 rules include ranks 1..512, 10 quantity families for precision allocation, familywise precision risk 0.05, `alpha=0.05/(10*512)=9.765625e-06`, Hoeffding fixed-N, effect rule `SIMULTANEOUS_LOWER_BOUND_STRICTLY_GT_ZERO_V1`, unconditional decision population, estimator failure counted as nonqualifying, full-refit nulls, survivor-only intervals forbidden, post-outcome tuning forbidden, and RNG `SHA256_PARENT_QUANTITY_RANK_REPLICATE_INDEX_V2`.

Five D_shared quantities:
1. shared_matched_null_exceedance
2. shared_subspace_stability
3. shared_held_donor_cross_view_predictability
4. shared_independent_view_agreement
5. shared_measurement_shortcut_increment

Each is frozen with range [-1,1], epsilon 0.05 and 9,784 replicates/rank under the V2 precision authority.

Current classification:

`STATISTICAL_AUTHORITY_FROZEN__NOT_SUFFICIENT_FOR_REAL_D_SHARED_EXECUTION_WITHOUT_UPSTREAM_DATA_AND_MEASUREMENT_QUALIFICATION`

Do not edit V2 in place. Any changed null/matching design must be a prospective successor.

## Claude feature-lineage findings
Recovered chain:

authenticated block manifest → `build_full104_phase2_multiview_features.py` + `PHASE2_SKETCH_PROJECTIONS.npz` + `MULTIVIEW_FEATURE_CONTRACT.json` → 8,915 multiview blocks → assembler → validator/publisher → `A_full/B_full/A_views/B_views`.

Evidence supports: complete 4,553,407-row identity, exactly-once assembly, canonical row order, donor/source/operator metadata, address-state binding, normalization `log1p(raw_count*10000/full_source_library)` exactly once, no clipping/winsorization/imputation/PCA/SVD/row or stratum sampling/capping, no pathology/protected/checkpoint/training dependence, and ~93 GB published feature arrays byte-identical to the manifest.

Claude classification: `CERTIFIABLE_WITH_MECHANICS_REPAIR_ONLY`, but final receipt correctly withheld.

F1: unresolved historical writer hash `4c39d215…`. Ruling: `ORIGINAL_WRITER_HASH_UNRESOLVED__PUBLISHED_BYTES_AND_SEMANTICS_VERIFIED`. Do not claim exact historical-writer replay.

F2: historical features encode `views=4`, visible fraction 0.60 / mask fraction 0.40. These are NOT automatically current-V5 biological measurement authority. The bytes may serve provenance/mechanics/reconnaissance, but final D_shared use requires outcome-blind measurement-geometry qualification or a prospective rebuild.

F3: stale absolute Windows staging paths are non-authoritative. Ruling: `CONTENT_HASH_AND_LOGICAL_NAME_AUTHORITATIVE__ABSOLUTE_PATH_INFORMATIONAL_ONLY_V1`.

## Current blocking STOP — matched-null state
V2 matching tuple:

`(donor, operator, Q_DEPTH, Q_DETECT, support_measurability)`

Claude found donor/operator discrete; `Q_DEPTH` and `Q_DETECT` continuous float32 per cell; support-measurability discrete but deterministic per operator. Claude correctly returned:

`STOP_D_SHARED_MATCHED_NULL_STRATIFICATION_UNSPECIFIED`

No bins were invented.

## Current matching-state resolution strategy
Do not jump directly to arbitrary bins or V3. First test whether the continuous floats are lossless encodings of discrete generating counts.

Q_DEPTH stored semantic:

`Q_DEPTH = float32(log1p(source_library_count))`

Prospective recovery:

`Q_DEPTH_COUNT = round(expm1(float64(Q_DEPTH)))`

then require bitwise equality after recomputing `float32(log1p(Q_DEPTH_COUNT))` over all 4,553,407 rows.

Q_DETECT stored semantic:

`Q_DETECT = float32(nonzero_count / max(scalar_support_count,1))`

Recover frozen operator-level scalar support, set `Q_DETECT_COUNT = round(float64(Q_DETECT)*scalar_support_count)`, recompute historical float32 ratio and require bitwise identity.

If either fails:

`STOP_D_SHARED_CONTINUOUS_MATCHING_STATE_REQUIRES_V3_NULL`

Then design a new continuous-nuisance null prospectively. No arbitrary bins.

If both pass, characterize complete candidate key `(donor, operator, Q_DEPTH_COUNT, Q_DETECT_COUNT)`. Remove support-measurability only if exact operator redundancy is proven. Successful recovery still does not authorize D_shared.

## Claude current assignment
Instruction file:

`docs/agent/V5_CLAUDE_FULL104_RECON_AND_MATCHING_RESOLUTION_20260914.md`

Claude should test Q_DEPTH/Q_DETECT lossless preimages, compute full matching-stratum occupancy if valid, prove/refute support redundancy, produce hash-bound evidence for all 14 allowed FULL104 reconnaissance diagnostics, return hardened donor/operator/row/address feature-lineage evidence and complete transform disclosures, and provide outcome-blind data scales for the future measurement-qualification plan. No D_shared outcome execution/inspection.

## FULL104 reconnaissance authority
The repo now explicitly permits outcome-blind structural diagnostics while forbidding decision-bearing D_shared fields. Allowed categories include donor/operator/source/study/technology counts; depth/detection; sparsity/support/missingness; feature variance/covariance/conditioning; effective rank/redundancy; view redundancy; donor leverage; technical-variable correlations; cell-state support; matched-null stratum occupancy; matching-state discreteness; technical variance dominance; control estimability; I/O/memory/sharding mechanics.

Red-team hardening now requires actual SHA-256-bound evidence for each diagnostic plus a deterministic evidence-root. A statement that a diagnostic was completed is not sufficient evidence.

## V5 measurement-procedure qualification
A requirements layer now exists for assay-like qualification before testing unknown D_shared biology. Required categories: real-geometry negative controls, real-geometry positive controls, prospective difficulty/detection curve, unconditional failure accounting, donor influence/concentration diagnostics, and a matched nuisance/simple comparator.

Numerical injection levels, thresholds and acceptance values are deliberately NOT frozen yet because doing so before FULL104 reconnaissance would be guessing. Claude's outcome-blind characterization must supply the scales used to freeze those values prospectively. Toy independent synthetic fixtures remain mechanics-only.

## Repo-side work and prior GREEN milestones
New safeguards were implemented on the active branch:

- FULL104 reconnaissance authority: prior GREEN 244 passed; 24/24 critical; compile PASS.
- Feature-lineage authority: prior GREEN 249 passed; 26/26 critical; compile PASS. It is now undergoing stronger red-team hardening.
- D_shared measurement-qualification requirements: prior GREEN 254 passed; 29/29 critical; compile PASS.
- Environment preflight: catches missing imports/dependencies, collection failures, skips, critical-test omission, wrong Git head, dirty authority files, byte/EOL mismatch and missing CUDA when required.

A SHA-binding bug in environment sealing was found and fixed.

Last clean GREEN before current red-team tests:

`0d857e33bf70c6350752681032910e80fad5a5ad`

Result: `258 passed; 31/31 critical; compile PASS`.

## Current CI state — INTENTIONAL RED
Live handoff head: `d9938dd782443b5310919ecf6c14099c0b4c23ec`

Latest workflow: `V5 dataset-first production closure`, run `34866260614`, conclusion FAILURE.

Test result: `8 failed, 256 passed`.

Known failures:

1. valid reconnaissance fixture lacks new diagnostic-evidence map/root
2. decision-field rejection fixture dies earlier on missing evidence
3. outcome-access fixture dies earlier on missing evidence
4. feature-lineage validator still fails open on missing donor/operator identity + complete transform disclosure
5–8. new `d_shared_matching_state_resolution_v1` module is not implemented, producing four ModuleNotFoundError failures

This is a characterized RED TDD phase, not an unknown regression. Do not delete or weaken the new tests simply to restore green.

Protected new tests include:

- `tests/test_v5_redteam_evidence_binding_20260914.py`
- `tests/test_d_shared_matching_state_resolution_v1.py`
- updated `tests/v5_critical_test_manifest_v1.json`

## Immediate next-chat repo tasks
1. Repair valid reconnaissance fixtures to include deterministic fake evidence digests/root while preserving evidence binding and useful error ordering.
2. Harden `full104_feature_lineage_v1.py` to require donor/operator/row/address identity digests, complete transform disclosure, logical-name→hash mapping, F1/F3 mechanics-repair semantics, and an explicit separation between lineage certification and V5 measurement-geometry authorization.
3. Implement `d_shared_matching_state_resolution_v1.py` to satisfy the protected tests. It may seal a resolution receipt but must always leave `d_shared_real_outcome_access_authorized=false` and `training_authorized=false`.
4. Run exact-head full CI + critical-test guard until GREEN.
5. Adjudicate Claude's outcome-blind evidence.
6. Freeze the numerical V5 measurement-qualification plan from the observed FULL104 structural scales, not guesses.
7. Qualify the procedure before real D_shared.

## After Claude returns
Adjudicate in this order: Q_DEPTH recovery; Q_DETECT recovery; real stratum occupancy/permutability; support redundancy proof; 14-diagnostic evidence root; hardened feature lineage; F1/F2/F3 terminals; real-data scales for measurement qualification.

Path A: if lossless discrete preimages are valid and strata usable, freeze a prospective matching successor replacing the invalid float key with exact recovered generating states; remove support only if redundancy proven. Still do not open D_shared until measurement qualification closes.

Path B: if lossless recovery fails or strata are unusable, keep V2 immutable provenance and freeze a new V3 continuous-nuisance null. Do not invent bins.

Feature geometry: even if lineage is mechanically certifiable, historical four-view/60%-visible geometry must be outcome-blindly qualified before becoming final D_shared measurement geometry; otherwise rederive prospectively from authenticated FULL104.

## Scientific stopping rule
The project must allow a genuine negative. Once the final D_shared procedure is frozen, if no rank 1..512 qualifies, lawful conclusion is:

`D_shared = 0 / current shared-state hypothesis not qualified`

Do not react by expanding rank, relaxing thresholds, inventing bins, swapping the null, extending replication after looking, changing features, dropping failures or redesigning and reusing the same run as confirmation.

## Historical binding lessons
T1: fp16 autocast backward killed protected gradients. Preserve mechanics sequence:

`fp16 forward -> backward autocast disabled -> unscale -> protected-gradient gate -> prove optimizer movement beyond decay -> both Adam moments -> EMA`

QID/F1 warning at historical `dd078625c3537f5ff2c3f8c0b382ba2803b24ee2`: QID intended `own_similarity - paired_wrong_similarity`, but producer supplied matched-null student state vs true teacher. Matched-null preserved query identity, so matched-null state intervention is not demonstrated equivalent to paired-wrong-query intervention. Do not import this gap into V5.

Major shortcut threat remains approximately:

`disease ~ dataset/source ~ sequencing technology ~ living/postmortem ~ region`

## Target Discovery / downstream state
Historical TD57B global donor-recurrent ordering PASS 24/24; TD57C nearest-third locality FAIL; TD59 nearest-half mesoscale PASS 24/24 pilot only; TD60 remains prospective and waits for a lawful learned EMA teacher.

Relational primitive: `q(i;j,k)=sign(d(i,j)-d(i,k))`.

Prospective EMA rule conceptually: `m=exp(log(0.5)*presentations_this_update/half_life_presentations)`; do not inherit `.996` as authority.

External roles remain frozen: FULL104 production substrate; 50K archive mechanics-only; historical T1/V4 checkpoints mechanics/history only; SEA-AD pathology later internal confirmation; GSE146639/GSE243292 external recurrence/transport; GSE133357 paired STG/cerebellum regional control. External outcomes must not flow backward into upstream design.

## Exact startup checklist for next chat
1. Connect to `dushyant-mishra/sea-ad-jepa-agent`.
2. Open `START_HERE.md` FIRST.
3. Re-fetch active branch live head.
4. Compare against handoff head `d9938dd782443b5310919ecf6c14099c0b4c23ec`; inspect all intervening commits.
5. Check whether Claude has pushed/returned new FULL104 reconnaissance/matching evidence.
6. Confirm no D_shared outcome was opened.
7. Inspect latest V5 workflow.
8. If current RED persists, close the exact 8 known failures with RED→GREEN TDD.
9. Re-run full protected CI + critical guard at exact head.
10. Adjudicate Claude evidence and freeze numeric measurement qualification from real structural scales.
11. Execute/review measurement qualification before real D_shared.
12. Only after feature lineage + matching authority + reconnaissance + measurement qualification + environment + executor review all close, create the final gate that can authorize real D_shared.
13. Execute real D_shared once, seal raw evidence, then adjudicate rank.
14. Continue downstream only per the master open-items ledger.

## One-line orientation
Physical FULL104 is authenticated and D_shared statistical rules are largely frozen, but the project intentionally paused before revealing D_shared because the proposed matching strata were not actually defined on the stored continuous nuisance states and because T0's larger lesson requires real-data characterization and assay qualification first.