# V5 LOCAL WORK COMPLETION — 2026-09-14

## Purpose

This record marks the lightweight/local lane as **substantively exhausted for currently available historical discovery + calibration assets**. It records what was derived here, what historical work was reused, and which remaining questions genuinely require Claude's rebuilt FULL104 feature substrate or later full-procedure qualification.

Authority:

`LOCAL_DISCOVERY_CALIBRATION_AND_FALSIFICATION_ONLY__NOT_V3_AUTHORITY`

No D_shared/protected/pathology/training outcome was used.

## 1. Historical work reused rather than rediscovered

The local lane reused the frozen Foundation discovery/calibration work instead of treating the project as a blank slate. In particular:

- common-core, operator-aware, within-source and optional integrated geometry concepts were already prospectively separated;
- donor-block resampling/generalization was already required;
- support-only geometry was already required as a falsification control;
- fixed historical exploratory scales `k={15,30,60,120}` and resolutions `{0.25,0.5,1.0,2.0}` were recovered;
- source/support readability before training was already documented;
- lawful RNA-only comparator ceilings across broad/local/rare endpoints were already measured;
- historical T1 anti-cheat mechanics and QID/T0 warnings remain inherited constraints.

## 2. Closed local parameter derivations

### Common molecular universe

- total addresses: 41,238;
- universal common core: 17,186;
- operator support patterns: 9.

### Candidate molecular-view count

Local screening on the 84-cell/all-operator truth table and frozen 50K discovery expression narrows serious disjoint-view candidates to approximately:

`K in {2,3,4,6}`

Approximate 50K screening behavior on the common core:

| K | minimum view size | minimum view-vs-full distance Spearman | minimum median nonzeros/cell/view |
|---:|---:|---:|---:|
| 2 | 8,593 | 0.9968 | 1,399.5 |
| 3 | 5,655 | 0.9942 | 933.5 |
| 4 | 4,252 | 0.9904 | 688.0 |
| 6 | 2,792 | 0.9865 | 457.0 |
| 8 | 2,097 | 0.9787 | 329.5 |

K>=8 begins paying a clearer per-view sparsity/information penalty. Local data do not justify choosing among 2/3/4/6 without rebuilt production-scale spectrum/QC evidence.

### Source-conditioned exchangeability

- donors spanning >1 source = 0/104;
- operators spanning >1 source = 0/42;
- donor counts: HVS 41 / NPH52 17 / SEA_AD 46.

Therefore unrestricted donor permutation across source is ruled out.

### Measurement-depth distribution

Frozen 50K source-library quantiles:

- p01 1,224;
- p05 2,647;
- p10 3,881;
- p25 7,560;
- p50 16,127;
- p75 32,520;
- p90 54,224;
- p95 71,246;
- p99 115,969.

Source medians:

- HVS 14,149;
- NPH52 8,151;
- SEA_AD 19,243.

A locally supported same-cell thinning **screening ladder** is retention fractions `{0.90, 0.75, 0.50, 0.25}` plus unthinned reference. This is not a final acceptance authority; it spans mild to severe measurement degradation while retaining nontrivial expected library size across all three sources. At 25% retention, source-specific median expected libraries remain approximately HVS 3,537 / NPH52 2,038 / SEA_AD 4,811.

### Historical difficulty ladder for positive/control qualification

The frozen `FOUNDATION_PARTIAL_EVIDENCE_CEILING.csv` provides an existing lawful-RNA baseline difficulty spectrum on held-out donors. Heldout-combined lawful RNA predictive R2 values were approximately:

- core_halo 0.9996;
- local_halo 0.9987;
- local 0.9940;
- broad_common 0.9792;
- local_core 0.9756;
- weak_distributed 0.9530;
- sparse_marker_like 0.9340;
- innovation_tail 0.5753;
- recurrent_5pct 0.3409;
- recurrent_1pct 0.0312.

This should be reused as an inherited **difficulty ordering** when designing future positive/negative measurement controls. It is not a current V5 pass threshold and must not be mistaken for D_shared biology.

## 3. Local null/nuisance candidates falsified

Using real 50K donor/source/operator geometry plus synthetic conditional-null outcomes, the local lane rejected:

1. unrestricted whole-donor permutation across source;
2. source-preserving donor permutation with no operator-exposure nuisance handling;
3. cell-level within-operator permutation that discards donor clustering;
4. aggressive operator/source residualization judged only by nuisance removal;
5. one pooled operator-exposure nuisance slope across all sources;
6. the claim that common-core restriction by itself solves source/operator confounding.

Source-specific operator-exposure nuisance slopes with source-blocked donor-level inference survived the current local stress better and remain an unfrozen candidate family.

## 4. Local measurement/geometry findings

- Claude's exact K=2 partition rule/hashes were independently reproduced from local calibration assets.
- The two genuinely disjoint K=2 halves preserve 84-cell calibration geometry strongly (cosine-distance Spearman ~0.984 between views; each view vs full common core ~0.995–0.996).
- Common-core restriction removes support-availability leakage but does not remove source/operator-associated expression structure.
- Aggressive nuisance removal can nearly eliminate nuisance summaries while substantially distorting pair geometry; nuisance reduction must always be paired with structure-preservation qualification.
- Source-aligned biology is fundamentally non-identifiable from source-linked technical effects inside this dataset; source-orthogonal positive controls can test method preservation, but source-aligned injected controls should expose the identifiability boundary rather than impose impossible recovery.

## 5. Closed substrate invariants now separated from rebuild-dependent work

See:

`docs/agent/V5_CLOSED_SUBSTRATE_INVARIANT_REGISTRY_20260914.md`

In particular, repeated `ASSEMBLY_SEEN` all-ones coverage over the unchanged 4,553,407-row universe is a lineage check, not a new discovery task.

## 6. Remaining questions that genuinely require rebuilt FULL104 features

The local lane cannot defensibly close these without Claude's new arrays / full procedure:

- final choice among K=2/3/4/6;
- production per-view covariance/eigen-spectrum;
- effective rank / conditioning on new disjoint views;
- per-view QC/source/operator predictability in the rebuilt representation;
- final rank-support rule;
- production-scale nuisance-adjustment/structure-preservation tradeoff;
- representation response under same-cell thinning;
- whole-procedure conditional-randomization/null calibration;
- positive-control detection curve on the actual rebuilt measurement;
- final V3 null family and thresholds.

## 7. Local lane terminal

There is no additional high-value parameter that should be invented from the lightweight assets merely to remain busy. The next scientifically justified work is to ingest Claude's rebuilt FULL104 evidence and answer the remaining representation-dependent questions above.

No second heavy K-way rebuild should be authorized until K=2 production characterization is compared with the locally narrowed K={2,3,4,6} information budget and the exact residual question is written down.

`LOCAL_PARAMETER_DERIVATION_AND_FALSIFICATION_LANE_SUBSTANTIVELY_COMPLETE`

`WAITING_FOR_REBUILT_FULL104_REPRESENTATION_DEPENDENT_EVIDENCE`

`NO_D_SHARED_OUTCOME_EXECUTION_OR_INSPECTION`

`TRAINING_OFF`
