# JEPA Global Blocker Review — 2026-09-08

Status: **EXTERNAL REVIEW / NO EXECUTION AUTHORITY**

This ledger spans the current T0, Teacher/Student, Target Discovery, F1,
healthy-teacher, D1, and population-governance branches without merging their
code. Every finding is bound to an explicit branch/head.

## Critical-path conclusion

The project is not blocked by lack of biological ideas. It is blocked by
**authority convergence and execution architecture**.

The current critical path is:

```text
T0 execution-substrate closure
        +
global authority/supersession consolidation
        +
V5 full-reader scientific design freeze
        ↓
one reviewed V5 production runtime
        ↓
bounded mechanics qualification
        ↓
full-reader exposure-defined learned-teacher checkpoint
        ↓
TD60 learned-teacher relational continuity
        ↓
partial-evidence student qualification / later D1
```

## Highest-priority cross-lane contradiction

The frozen healthy-teacher base and V4 history use the 3,292-cell schedule and
u40/u205 clocks. That remains valid for **mechanics qualification**.

The later data-first audit establishes that the production reader-fit population
is 4,553,407 cells and explicitly states:

- u40 exposes only 5,120 presentations, about 0.1124% of reader-fit cells;
- u40/u205 may remain mechanics clocks;
- they must not automatically become learned-biology/full-training clocks.

But TD60 still freezes **successor u40 EMA teacher** as its decision-bearing
learned checkpoint.

That is a contract contradiction. It must be repaired prospectively before any
decision-bearing successor training.

### Recommended resolution

Create two separate clocks:

1. **Q40_MECHANICS** — bounded mechanics-only qualification. It proves
   optimizer/AMP/gradient/Adam/EMA/checkpoint health and nothing biological.
2. **BIO_COVERAGE_1** (name provisional) — a prospectively frozen full-reader
   exposure/coverage milestone produced by the V5 schedule compiler.

Amend/supersede TD60 so its learned-teacher continuity gate consumes the
full-reader exposure-defined checkpoint, not Q40.

Do not reinterpret historical u40 after training. Freeze the new distinction now.

## T0 blockers

Three hard STOPs remain at producer head
`24b2c9ffc0ab2d63061c20af8829bcb86114b11a`:

1. B2 logical root does not bind closure root / execution paths with complete
   stored=recomputed=external equality.
2. source_library proof still trusts caller-supplied raw row + provenance labels
   instead of extracting `layers/UMIs` from the authenticated MTG H5AD.
3. age/sex authority accepts the candidate donor universe as a detached caller
   list.

These already have reviewer red tests on
`review/t0-external-24b2c9-20260908`.

## Teacher/Student V5 blockers

V5 has made meaningful engineering progress: finite non-enumerative relational
sampling, scientific group weighting, full-path packing cost, support geometry,
and a Philox reference exist.

But production science is still deliberately unset:

- target estimand;
- proposal sampler;
- importance weighting;
- relational sampling policy;
- relational group weighting;
- support-aware target/masking policy;
- full-reader exposure schedule;
- optimizer/EMA timescales in exposure units;
- hardware calibration.

The latest descriptive estimand analysis deliberately has
`selected_estimand=null` and `selected_proposal_sampler=null`.

No training should begin by silently filling those nulls with historical values.

## V5 code-integration blockers

The proof modules are not yet a production trainer.

Required before execution review:

- one canonical source-bound keyed RNG/dropout implementation;
- packed-token training-mode equivalence including gradients;
- exact unequal-microbatch weighted accumulation equivalence;
- proved optimizer/Adam/EMA equivalence after the same scientific update;
- relational finite-sampler integration with frozen group weights;
- hardware cost calibration and fail-closed packing;
- end-to-end checkpoint/resume identity under the new schedule.

The repository currently contains both a Philox reference and a distinct
modular-hash keyed-dropout proof function. This is acceptable as prototype
history, but production must select exactly one and retire ambiguity.

## Teacher/Student lineage governance

V3 review bridge, V4 candidate, V4 governance-repair, V5 candidate, and V5
planning lines coexist.

The existing `CURRENT_AUTHORITY_INDEX.md` and
`CURRENT_SUPERSESSION_MAP.md` are dated September 3, 2026 and do not mention
T0 V20, V5, or D1 V2.

Do not close/delete historical branches blindly. First publish a new immutable
supersession table that says, for each live line:

- controlling branch/head;
- accepted package/root if any;
- whether it is historical mechanics, candidate, or current;
- what exact successor supersedes it;
- whether execution is allowed.

Only after that map is reviewed should stale branches be archived/closed.


## F1-B/C3 mechanics successor

The frozen F1-B behavioral attack authority is valuable and the
`c0eaf2acc...` successor defends the frozen attacks, but it is still not the
execution-bound trainer.

Three concrete gaps remain before even a mechanics-only qualification run:

1. `MOVEMENT_OVER_DECAY_MARGIN = 2.0` is hard-coded in the successor. The
   healthy-teacher base explicitly records that the F1-B attack authority did
   **not** freeze a 2x margin. A reviewed analytical movement criterion and
   tolerance must be source-bound prospectively.
2. The mandatory predictor registry is currently discovered from all
   `requires_grad` predictor parameters. The execution overlay requires an
   exact reviewed predictor-parameter registry identity.
3. The source is a protected-update mechanics module, not a frozen end-to-end
   data/masking/loss/schedule/checkpoint trainer.

Therefore a green F1-B attack suite is necessary mechanics evidence, not
training authority.

## Target Discovery

The relational biological object remains alive:

- TD57B: 24/24 global scale-free relational recurrence;
- TD59: 24/24 nearest-half mesoscale p95 recurrence;
- TD57C: nearest-third local geometry failed in HVS.

No more 50k locality tuning is allowed.

The next scientific problem is not another target search. It is whether a
lawfully trained full-reader EMA teacher preserves the already-qualified
relational geometry. TD60 needs the checkpoint-clock amendment described above.

## F1

F1-A is not currently the central Teacher/Student critical path.

Its producer/replay branch is mechanically mature but still lacks:

- strict independent review with every external authority present;
- issued execution authorization;
- real reader-fit sweep;
- post-result data-only closure.

Keep it as a parallel lane unless a new global authority explicitly makes F1-A
a prerequisite for V5.

## D1

The latest D1 branch is substantially more advanced than the older status prose:
later commits add a future-safe readout/qualification resolver, teacher-state
archive, incremental resampling, discovery atlas, score archive, sparse
expression reader, and molecular sufficient-statistic store.

So R1-R7 should not simply be repeated as current blockers.

The current D1 blockers are instead:

- no actual frozen canonical teacher readout contract for the future healthy
  checkpoint;
- no healthy qualified teacher checkpoint;
- no qualification authority selecting that checkpoint;
- no final independent closure terminal over the complete current D1 V2 head.

Real D1 remains correctly at `WAIT_HEALTHY_TRAINED_TEACHER`.

## What is healthy

The population-access/sealed-holdout registry is not a defect. Keep it frozen.

The 3,292-cell healthy-teacher base is not defective when interpreted as a
mechanics-only base. The defect would be using it as the final full-reader
biological training schedule.

## Immediate work order

1. close the three T0 STOPs;
2. publish a global supersession/authority map;
3. prospectively split mechanics time from biological exposure time;
4. freeze V5 scientific estimand/proposal/support-aware masking;
5. integrate and independently attack the V5 production update;
6. mechanically qualify it;
7. train only under the full-reader exposure authority;
8. run corrected TD60;
9. only then unlock D1/partial-evidence student downstream gates.

No protected population or pathology access is needed to complete items 2–6.
