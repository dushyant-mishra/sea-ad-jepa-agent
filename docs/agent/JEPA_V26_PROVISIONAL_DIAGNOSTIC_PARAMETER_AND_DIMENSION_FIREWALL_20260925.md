# V26: how the first current-V5 real-data diagnostic avoids circular D_shared and historical numeric spillover

Date: 2026-09-25. **Scope: engineering/development contract proposal only; no physical GPU run, protected outcomes, dimension result or training authorization.** This doc is additive to the September 15 dimension hierarchy, September 7 D1 full-fit real-parameter derivation authority, September 25 V26 handoff and September 26 independent G3 PR #143/#144.

## Distinguish four quantities that look like "dimensions"

1. `model_width` / `attention_heads` / depth / packing: COMPUTE/ARCHITECTURE. Their provisional **diagnostic** values are explicitly declared in an immutable prespecified GPU/resource memo. They are NOT biological dimensionality estimates. Historical 160/96/224/320/512 are not authority. Choosing the same integer for independent current memory reasons is possible but must be justified independently, not copied.
2. `D_shared`: shared biological state rank to be qualified from authenticated real evidence: full-refit matched-null separation, donor-bootstrap/subspace stability, genuinely held-donor prediction, independent-view/sketch agreement, contiguous-prefix selection, and gain beyond frozen measurement-shortcut baselines. Its scientific outcomes are currently SEALED. The qualified number does not exist in the current branch lineage. It cannot be invented from model width or fit-donor ridge R².
3. `D_private`: supplementary private biological rank conditional on `D_shared`, requiring additional held-donor / held-operator predictive information and same-cell measurement counterfactual survival. It cannot be typed from a historical embedding width.
4. `D_obs`: measurement/observation rank, separately qualified. The observation head's gradient firewall does not certify D_obs or biological purity.

Also `D1` teacher-state program count is a separate downstream discovery estimand. Its September 7 proposed method uses donor/operator-preserving parallel-analysis nulls and donor-block subspace stability from an **already healthy teacher** across the real full fit population. `D1 D != D_shared`. No 50k discovery sample, 4,540-cell old cohort or planted synthetic latent fixture can supply either production rank. A qualified D1 result must not be silently substituted for the separately defined D_shared dimension-qualification chain.

## The unavoidable circularity and lawful two-stage sequence

A mechanically healthy trained teacher is prerequisite for any teacher-state-dependent dimension study; the final architecture is supposed to be chosen downstream from qualified dimensions. Therefore a first teacher must have **one resource-bounded diagnostic architecture** before D_shared is known. This is a declared experimental instrument, not `DATA_DERIVED_JEPA_GEOMETRY_V2` production authority. If its provisional width turns out too narrow to inspect high-rank shared biology, the diagnostic is underpowered for that purpose; no "rank = width" result may be reported. Do not select a new width after peeking at a protected dimension result.

**Stage E: real mechanical smoke, no science.** The independently explicit engineering config requires genuine full104 reader_fit source/split/support/teacher-target/mask/sampling/EMA roots and a resource memo, every numeric width/depth/head, token budget, update/batch/schedule/seed, EMA half-life is supplied and recorded with its provenance. Mechanical smoke examines nonzero protected gradient, actual optimizer step beyond weight decay, both Adam moments, post-success EMA and exactly-once atomic resume, NOT d_shared or masking science. No inference from the failure/success of old historical 128x8 T1 checks to current geometry. No D_shared/D_private/D_obs or geometry-authority digest may be present.

**Stage D: one prospective scientific reader_fit development diagnostic.** Separately name the diagnostic execution authority, freeze one chosen existing candidate teacher target, address-query authority, VALUE_ONLY/measurement visibility, one candidate mask/burden, split and donor-uniform training law, exact G3 primary `PRODUCTION_OBJECTIVE_MATCHED` plus fixed sensitivity axes, a six-state non-estimability/coverage contract, model/compute values, optimizer/reproducibility and EMA presentation horizon **before** fitting/score inspection. If masking remains unqualified, report it as a *candidate* and never infer that the teacher is biologically clean. The 104 reader_fit donors are DISCOVERY; if held-donor predictive performance is a claim, split some reader_fit donors BEFORE fitting that diagnostic teacher and train only on the designated training folds. A model trained on all 104 cannot be evaluated as held-donor on those same 104. A full-104 model may instead serve descriptive full-fit teacher-state reconnaissance in a subsequent explicitly authorized phase. Reader_validation22, oracle23, foundation sealed and Siletti remain closed.

**Stage Q: dimension qualification after healthy frozen teacher and scientific evaluation.** Only then execute the already-frozen `D_shared` hierarchy on a separately lawful and prospectively specified dataset. Preserve true held-donor and independent-view roles and never reuse the same fitted-on donors as if they were untouched confirmation. Do not open sealed D_shared results while using them to alter the original diagnostic configuration. If later data-derived production geometry is chosen, bind qualified rank receipt -> frozen rank-to-geometry mapping -> geometry-specific memorization qualification -> dynamic protected tensor registry; retrain/requalify where changed geometry requires it. D_private and D_obs have distinct data-dependent qualifications. The first provisional checkpoint is not a production final-model checkpoint.

The `D1` separate full real fit program-discovery procedure may subsequently estimate its own D from the healthy, exact-readout frozen teacher. Its synthetic u0/planted controls qualify code only, not any dimensional result. A discovered program is DISCOVERY_ONLY until separately confirmed.

## What is physically measured vs externally chosen vs undecided

| Item | Correct source | Never use |
|---|---|---|
| Donor and per-donor cell counts, support and operator token cost | authenticated frozen full reader_fit / exact source library and visibility | 94-donor proxy, 50k sample or historical Stage81A3 |
| Scientific cell mass p=1/(D*n_d) | existing V5 immutable 104-fit law; D here means donor COUNT, not biological rank | source/operator mixture inferred from initial validation scores |
| q proposal and p/q correction | explicit independently frozen current-V5 sampler schedule PR #135/#139 | inherited V4 schedule, repeated cells mislabeled IID |
| Diagnostic architecture, microbatch memory budget | external GPU compute bound + prospective run memo; instrument only | D_shared=160 or default 6/48/128x8 |
| EMA half-life H | prospectively frozen presentation-unit schedule (m=exp(log(.5)*p/H)); H numeric still undecided | historical m=.996 or inherited update-count assumption |
| Mask fraction/partner choice/target query | previously authored candidate governance, one prospectively declared diagnostic choice | cycling the failed 9-cell historical K/R grid or tuning from protected N1 |
| First diagnostic validation | named fit-donor development fold(s), identical source/target comparator, six-state coverage | all104 training + claiming all104 donor-heldout, validation/oracle peeking |
| D_shared / D_private / D_obs / D1 program rank | later existing full real teacher-derived qualified procedures at their separate lawful data scopes | old T1 checkpoints, 4,540-cell old cohort, 50k discovery sample, synthetic true-rank fixture |

## A new small fail-closed machine-readable preflight

`src/sea_ad_jepa/v5/provisional_diagnostic_parameter_firewall_v1.py` contains **zero numeric diagnostic defaults**. The strict payload must explicitly supply every listed integer and all SHA-looking root fields. Every numeric design field is labeled `EXPLICIT_RESOURCE_BOUNDED_DIAGNOSTIC_ONLY` or `EXPLICIT_FROZEN_DEVELOPMENT_SCHEDULE`. The manifest prohibits nonnull `d_shared`, `d_private`, `d_obs`, dimension/production-geometry authority roots, hidden/unknown historical payload keys, any protected/nonfit data role, and any `training_authorized=True`. For a scientifically scoped candidate diagnostic it requires exact objective-matched G3 ID and both standalone G3 implementation and **existing** six-state evidence-contract roots. For an engineering smoke it expressly forbids mislabeling itself as G3-qualified.

**Critical honesty:** It is impossible for a pure config parser to authenticate physical bytes, inspect whether the external resource memo is independent of historical settings, confirm that a candidate masking authority was honestly frozen before data viewing, authorize GPU execution, or check a real gradient. It therefore ALWAYS emits `PARAMETERS_DECLARED__NO_RUNTIME_SOURCE_ATTESTATION` and `training_authorized=False`, even if given 64 syntactically valid hex strings. This proposal is not wired into Claude's trainer: a separate exact-head reviewed current-V5 entrypoint must call it *before* a named authorized diagnostic run. Never cite passing synthetic tests as physical safety, real rank estimation or sufficient training authority.

### Historical non-duplication audit

- Already audited and not rerun: T0 V20; old T1 zero gradients after fp16; Layer-2 corrected 94-donor linear proxy; visibility ablation; failed historical Pearson masking; frozen 9-cell recurrent grid; QID matched-null semantics; incorrect x4 within-group estimator; full 8,915-block heavy-statistics parser qualification; PR #136/#138 new104 baseline numeric checks.
- Existing but scope-limited: Stage-A source/target semantics, two independent molecular-view derivations, query-denominator route, target-side identity ablations, PR #143/#144 synthetic G3 and streaming fit, PR #132 synthetic pass1 count bridge.
- New here only: prospective **diagnostic versus dimension-qualification** firewall and adversarial rejection of accidental hardcoded numerical/dimensional authority spillover.

**Terminal state:** NO DIAGNOSTIC GPU RUN HERE; D_shared SEALED; production geometry UNCHOSEN; TRAINING OFF; G3 PR #144 synthetic tests PASS but real source/scorer closure pending.
