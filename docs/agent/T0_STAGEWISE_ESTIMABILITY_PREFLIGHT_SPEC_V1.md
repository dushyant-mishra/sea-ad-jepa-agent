# T0 Stagewise Pathology-Blind Estimability Preflight Specification V1

Status: **EXTERNAL INTERFACE FREEZE — 2026-09-08**

Do not invent a new rank tolerance. Production must reuse/bind the exact accepted V20 nuisance/rank numerical primitives or a separately proven byte-equivalent implementation.

## Stage A — after exact roles

For DISCOVERY, CONFIRMATION, and every discovery LOODO training set, require the exact frozen nuisance `[1, age_c, age_c², sex]` to be accepted by the frozen nuisance/rank primitive.

## Stage B — after STATE_SCORE is frozen, before confirmation AT8 magnitude access

On all 18 confirmation donors, establish incremental estimability of STATE_SCORE for:

- primary reduced = nuisance;
- composition reduced = nuisance + IMMUNE_FRACTION;
- measurement reduced = nuisance + Q_DEPTH + Q_DETECT.

No pathology magnitude is needed.

## Stage C — after TAIL_PREVALENCE is frozen, before tail pathology testing

Restrict to exact frozen tail_measurable confirmation donors and require n in {17,18}.

Establish incremental estimability of TAIL_PREVALENCE given:

- nuisance + STATE_SCORE;
- nuisance + STATE_SCORE + IMMUNE_FRACTION;
- nuisance + STATE_SCORE + Q_DEPTH + Q_DETECT.

Use the exact accepted HC3/rank numerical authority or proven equivalent.

A rank/estimability failure may not change eligibility, roles, target, thresholds, covariates, or alpha.

The preflight artifact must bind its donor list/order, design columns, upstream authority roots, and exact numerical primitive identities.

`real_execution_ready=False` until separate final execution promotion.
