# T0 Stagewise Estimability Acceptance Oracle V1

Status: **EXTERNAL REVIEW ORACLE — NO OUTCOME MAGNITUDE REQUIRED**

The goal is to discover non-executable statistical designs before the
corresponding pathology outcome is interpreted.

Do not invent a new rank tolerance. Reuse/bind the exact accepted V20 numerical
primitives or a separately proven byte-equivalent implementation.

## Stage A — after eligible donors and deterministic roles

Check the exact nuisance design:

`[1, age_c, age_c^2, sex]`

for:

- full DISCOVERY;
- full CONFIRMATION;
- every discovery LOODO training set.

Failure is a loud design STOP. Never alter the split to rescue it.

## Stage B — after frozen STATE_SCORE, before confirmation AT8 magnitude access

For all 18 confirmation donors, require STATE_SCORE to be estimable after each
frozen reduced design:

Primary:
`nuisance + STATE_SCORE`

Composition:
`nuisance + IMMUNE_FRACTION + STATE_SCORE`

Measurement:
`nuisance + Q_DEPTH + Q_DETECT + STATE_SCORE`

This is pathology-blind because AT8 is not needed to check design rank.

## Stage C — after frozen TAIL_PREVALENCE, before tail outcome test

Restrict exactly to frozen tail-measurable confirmation donors.

Require tail n in `{17,18}`.

Require TAIL_PREVALENCE to be estimable after:

- nuisance + STATE_SCORE;
- nuisance + STATE_SCORE + IMMUNE_FRACTION;
- nuisance + STATE_SCORE + Q_DEPTH + Q_DETECT.

Use the accepted HC3/rank numerical authority or a proven equivalent.

## Failure semantics

A rank/estimability failure may not change:

- eligibility;
- roles;
- target;
- feature set;
- tail threshold;
- nuisance covariates;
- alpha.

It terminates as the frozen lawful STOP / NOT_ESTIMABLE state for that stage.

## Root binding

The preflight artifact must bind, as applicable:

- eligible donor authority root;
- donor-role root;
- age/sex root;
- IMMUNE support root;
- IMMUNE_FRACTION root;
- technical-completeness/Q root;
- frozen STATE_SCORE target authority;
- frozen tail authority;
- exact numerical primitive identities;
- exact donor list/order;
- exact columns checked.

`real_execution_ready=False` until a separate execution-promotion authority.
