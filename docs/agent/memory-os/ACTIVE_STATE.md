# ACTIVE STATE — JEPA PROJECT

Date: 2026-09-08

## Canonical branch

`main`

The project histories were consolidated into main on 2026-09-08. Historical
branch names are no longer authority.

Machine-readable next action:
`docs/agent/memory-os/NEXT_ALLOWED_ACTION.json`

Current authority:
`docs/agent/CURRENT_AUTHORITY_INDEX.md`

Supersession map:
`docs/agent/CURRENT_SUPERSESSION_MAP.md`

## Controlling gate

`STOP_JEPA_EXECUTION_AUTHORITIES_INCOMPLETE`

This is a pre-execution engineering/governance gate, not a biological negative
result.

## Current scientific state

1. The project objective remains donor-generalizable biological state/program
   prediction from partial lawful RNA evidence while preserving the
   41,238-address Molecular Ledger.
2. Reader-fit production geometry remains 4,553,407 cells / 104 donors /
   42 operators / 41,238 addresses.
3. Protected reader_validation, reader_oracle, development/sealed, external
   holdout and pathology remain closed unless a frozen release authority opens
   them.
4. T0 V18/V20 science is accepted. Real T0 is not yet executable because three
   implementation/provenance STOPs remain.
5. Target Discovery has a real relational object: TD57B and TD59 passed across
   their frozen cases. Do not restart fixed-coordinate target search.
6. V5 data-first continuation at `028989a5...` is the current prospective Teacher/Student successor. Scientific target V2, relational proposal V2, base proposal V3, and presentation horizon V1 are frozen, but training remains unauthorized.
7. Historical 3,292-cell u40/u205 is mechanics evidence only. Full-reader
   biological checkpoints must be defined in exposure/coverage units.
8. F1-B/C3 successor is mechanics evidence, not the final trainer.
9. D1 V2 remains downstream of a lawful qualified full-reader teacher.

## Current T0 STOPs

- logical root / closure / execution-path chain incomplete;
- raw H5AD source row not actually authenticated by the source_library proof;
- age/sex candidate donor parent detached.

Until these are externally closed:

- no production B2;
- donor-role gate shut;
- no numeric confirmation AT8;
- `real_execution_ready=False`.

## Current V5 state

Current V5 continuation head:
`028989a5f1504e4d6403a44e7c90d37172c54150`

Materialization:
`71484ab99d276d7251836c109885d22ab4e7abb1`

Prototype root:
`9684f4c2b7eff1da863ae50124c6aad49d25f137a84898e05e98d2ae1f0c67ad`

Evidence:
- V5 active suite 66/66 PASS;
- V4 regression 98/98 PASS;
- current V5 active-test surface is the V2 manifest/selection restored onto
  consolidated main.

Frozen:
- scientific target V2;
- relational proposal V2;
- base proposal V3 with exact p/q correction;
- exactly one reader-fit-population-equivalent presentation horizon (4,553,407), no automatic extension;
- donor-primary objective direction.

Still pending:
- evidence/view/block schedule;
- finite relational triplet budget;
- update size / token budget;
- EMA half-life in presentations;
- biological learned-teacher checkpoint milestone;
- optimized GPU keyed RNG/dropout parity;
- hardware packing calibration;
- end-to-end trainer binding;
- mechanics qualification;
- external V5 review.

## Immediate work

Parallel pathology-blind work is allowed:

### T0
repair the three STOPs, run red/green external regressions, and stop before
production B2 until external acceptance.

### V5
finish the remaining prospective pre-execution authorities and mechanical
integration without running training.

### Governance
work only from main. Branch deletion is hygiene only after exact ancestor
verification.

## Still forbidden

- production T0 / numeric confirmation outcome access;
- real F1 biological sweep without its own execution authorization;
- production V5 training / optimizer / checkpoint / EMA updates;
- TD60 learned-teacher execution;
- real D1;
- protected-population access outside frozen lane-specific release authorities;
- resurrecting historical defaults because files remain preserved.
