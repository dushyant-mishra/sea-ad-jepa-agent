# V29 scientific decision package — reviewer copy, NOT a frozen authority

This document accompanies [the machine-checkable science decision request](JEPA_V29_TEACHER_DECISION_REQUEST_20260926.json), not a training receipt. It builds exclusively on the V28 documentary and independent relational physical-metadata replay from PR150 `fecd8aadaed5581fc00611bc64063c69fb119faf`, PR149's original physical reexecution and six own-class candidates `938fe5d3...`, original PR146 exact FULL104 metadata and V5's existing semantics. Main V25 still controls governance. This branch does **not** modify V28 handoff, source training runtime, other collaborators' PRs, or original receipts.

## Biological question and proposed target

For a cell `c` and canonical molecular address `q`, the student observes **only the lawful remaining measured RNA**, the identity of `q`, and predeclared permissible context. The `q` scalar is removed **before** any student context, pooling or normalization that could carry its realized value. The detached teacher can observe the full lawful measured state (including the query realization solely to construct a latent target), but supplies a **query-local contextual molecular state** rather than an expression-regression target. The proposed primary objective is donor-uniform weighted masked-block latent-state discrepancy, not recovery of the hidden gene's scalar.

The central risk is a model that appears to predict the teacher yet solves the task from the query ID, global pooled identity, measurement-support fingerprint, source/operator, cell key or library depth. The companion packet requires paired identity-only and identity+lawful-global/no-remaining-RNA interventions, explicit measured-zero/structurally-unmeasured/collision separation, and other measurement/source/cell-key attacks. These tests must be prospectively specified; no protected terminal result can inform mask, capacity or threshold selection.

## Exactly which choices remain open

The exact teacher target construction source/root; the current V5 query provider; a prospectively frozen source-stratified held-donor split and donor-specific `D_train`; selected masking policy, burden and proper qualification status; model width/depth/heads/token capacity justified by *real* PR146 operator support and GPU memory; an EMA presentation half-life; learning-rate/update horizon/seed; current104 like-for-like comparator; and G3 objective-matched six-state scorer and decision thresholds. They remain `null` in the JSON. **A scientific recommendation or documentation draft is not independent review or the named B1 execution contract.**

The exact 104-donor PR146 weights are correct for an all104 descriptive population, not automatically for a training-only subset after a held-donor split: the scientific base loss uses `p_i=1/(D_train*n_d)`. Original D1 equal-donor×operator weights and PR149 V2 relational eligible-anchor weights have distinct denominators and are not substituted for base JEPA. The existing 81-cell smallest donor count describes only a proposed without-replacement sampler capacity, not a chosen batch. The first diagnostic proposes **no relational auxiliary objective** because it lacks training authority.

The September16 nine-cell masking grid had **no qualifying policy**. Even a bounded uniform-random DEVELOPMENT candidate would be an *unqualified proposal*, not an automatic workaround for current B2, which still requires qualifying execution evidence. The prior source-class audit additionally identified V3 RNG/parameter candidates versus V1 closure class constraints. No numeric historical/synthetic geometry, mask .40, EMA .99/.996, previous 94 donors or purported D_shared is approved.

## Scientific approval and execution distinction

The owner/review group must prospectively decide the open choices, then independently validate exact source bytes and all 33 authority slots (32 upstream, one preexecution), appropriate executed qualifications, frozen target package and separate named B1 reader_fit training contract. Only after genuine B1 **and** B2 approvals may the real streaming adapter be connected and a bounded diagnostic run. The proposed first run must collect real nonzero gradients and actual Adam moments, poststep EMA, cursor/atomic checkpoint replay and explicit per-cell source-library/measurement/weight integrity. Evaluation of biological signal remains separate from engineering success.

Protected reader validation/oracle, foundation holdouts, pathology, D_shared and N1 terminal stay sealed. The full all-8,915 raw Level-4 count reaggregation has no verdict; PR146/149/150 physical metadata and relational checks do **not** substitute for it. No rerun is needed merely for S9: canonical dense solver already passed with `Library/bin` on PATH; the historical S9 defect claim was retracted.

## Reproduction

```bash
PYTHONPATH=src:. python scripts/v5/v29_teacher_decision_firewall.py docs/agent/JEPA_V29_TEACHER_DECISION_REQUEST_20260926.json
PYTHONPATH=src:. python -m pytest -q tests/test_v29_teacher_decision_firewall.py
```

A green result **proves only that the DRAFT keeps uncertainty explicit**. It must not be reported as an issued teacher target, training authority or biological validation.
