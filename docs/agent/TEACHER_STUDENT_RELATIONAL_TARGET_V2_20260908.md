# Teacher/Student Relational Target V2 — scale-free successor candidate

Date: 2026-09-08

Status: `RELATIONAL_TARGET_V2_MECHANICS_FROZEN__NOT_ACTIVE__TRAINING_UNAUTHORIZED`

## Why V2 exists

The earlier prospective V1 relational extension used absolute normalized Euclidean distance plus angle matching. Target Discovery subsequently established a different scientific object. TD57B showed that the donor-recurrent signal is the **anchored ordering of distances**, and that absolute cross-source distance equality is unnecessary. TD57C then falsified aggressive nearest-third locality, while TD59 supported a broader nearest-half mesoscale design on fresh panels but only under the frozen p95 criterion.

Therefore V1's absolute-distance objective is superseded as a candidate scientific target. It remains historical mechanics prior art only.

## Exact candidate relation

For teacher or student direct 160-D `cell_state`, use cosine distance

`d(a,b) = 1 - cosine(z_a, z_b)`.

For an anchored triplet `(i,j,k)`, teacher supervision is only

`y = sign(d_T(i,k) - d_T(i,j))`.

Exact teacher ties are unresolved. Absolute teacher distances are never regressed.

For resolved teacher relations, the differentiable student loss is

`softplus(-y * (d_S(i,k) - d_S(i,j))) / log(2)`.

This normalization makes a student tie exactly loss 1. Correct ordering is below 1 and reversed ordering above 1. No temperature, target distance scale, locality fraction, or loss weight is introduced by this module.

Implementation: `src/sea_ad_jepa/v4/teacher_student_relational_target_v2.py`.

## Triplet authority

Two modes are allowed mechanically:

1. deterministic complete within-group triplets for synthetic/mechanics tests;
2. exact externally frozen triplets for future global/mesoscale authorities.

The module does **not** choose nearest-half, nearest-third, k, a neighborhood fraction, or an adaptive locality. External triplets must use canonical comparator ordering `j < k`, remain inside one relational group, and contain no duplicates.

## Grouping and production schedule boundary

Current mechanics can validate externally supplied donor×operator grouped batches, but group size and groups-per-batch have no defaults. Those values must come from a separately frozen full-reader schedule authority. The 50k pilot cannot set production grouping, replay caps, locality, thresholds, or loss weights.

## Collapse and fine-structure are separate gates

V2 reports teacher/student geometry health using within-group variance, median pairwise spread, and entropy effective rank. Every eligible relational group is adjudicated separately against the matching teacher group; **no pooled group may rescue a collapsed group**. Numerical lower ratios are deliberately absent. A later `RELATIONAL_COLLAPSE_CALIBRATION_V1` must supply them from lawful outcome-blind production calibration.

Authority-valued evidence levels, seeds, group sizes, group counts, and batch sizes are accepted only as exact integers; silent float/string truncation is rejected. Canonical donor/operator identities used by the grouped-batch contract must be nonempty strings.

Passing collapse floors cannot substitute for relational fine-structure success. Learned-teacher continuity remains TD60, and partial-evidence student predictability is a later gate.

## Fine-matched null

A deterministic no-fixed-point permutation mechanic is provided for externally frozen fine-stratum IDs. The module does not invent those strata. Any real qualification stratum authority must be frozen separately and may not be selected from relational outcomes.

## Evidence schedule

20/40/60/80/100 percent evidence semantics are retained for qualification compatibility. Current frozen u0→u40 mechanics remain 60% evidence only. 100% is reference/qualification only and cannot be treated as a hidden-target training dose.

## Activation firewall

V2 is included in the V4 source authority so its exact bytes can be reviewed, but `teacher_student_runtime.production_update` does not import or call it. The current block-JEPA u0→u40 objective is behaviorally unchanged.

Before any relational loss can become active, all of the following remain mandatory:

- exact V4 independent review;
- lawful successor-u0 materialization and separate u0→u40 execution authority;
- u40 mechanical qualification;
- TD60 learned-teacher continuity 48/48 PASS;
- prospectively frozen partial-evidence student relational predictability PASS;
- full 4,553,407-cell reader-fit schedule/calibration authority;
- separately reviewed relational-loss integration and explicit execution authority.

Current terminal:

`RELATIONAL_TARGET_V2_MECHANICS_FROZEN__NOT_ACTIVE__TRAINING_UNAUTHORIZED`
