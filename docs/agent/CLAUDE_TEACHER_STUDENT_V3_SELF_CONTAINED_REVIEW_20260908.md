# Claude — Unified Teacher/Student V3 Self-Contained External Review

Date: 2026-09-08

Review the exact self-contained package generated by the canonical branch:

`production/teacher-student-unified-v1-20260907`

## Controlling V3 source identity

The active training implementation remains the already frozen V3 source:

- integrated source commit:
  `8f9c34f18ac7cf43572299901e5e4f5d65cecb24`
- source manifest:
  `docs/agent/TEACHER_STUDENT_INTEGRATED_SOURCE_MANIFEST_V3.csv`
- source root:
  `cd7faf6dd48f58387597b05f2e143ac629e1b74418d9720c4acdc9fcf4dfb584`
- predictor registry:
  `43922a62a885cbedee22c06363a8355c6561dad43c95f0a43147fc2f4cbe3592`
- healthy-teacher base:
  `9e1ee362773a8329f783015a04af4f7699135cc0710b1ee1ea66abc0aafd8534`
- population registry:
  `e9903bbb9d56663790f5f5b298c5633d87a71548466089e7cc6aae7c45728ee7`
- F1-B attack authority:
  `daa79afe19ab17f1f7cfa064754d671afd4ac4b284250605979f1d288862544b`

The packaging repair does not change those bytes.

## Why a new package was necessary

The prior ZIP ran green tests against the full repository but omitted local imports and authority-manifest members needed to reproduce those tests from the ZIP alone.

Read:

`docs/agent/TEACHER_STUDENT_V3_SELF_CONTAINMENT_REPAIR_20260908.md`

The repaired package contains a generated:

`TEACHER_STUDENT_REPLAY_DEPENDENCY_MANIFEST_V1.csv`

that explicitly distinguishes:

- controlling V3 source/test files;
- replay/import dependencies;
- prospective relational files.

## Required package verification

From a clean extraction:

```bash
set -euo pipefail
python - <<'PY'
from pathlib import Path
import csv, hashlib

root = Path(".")
manifest = root / "PACKAGE_MANIFEST.csv"
rows = list(csv.DictReader(manifest.open(newline="", encoding="utf-8")))
bad = []
for row in rows:
    path = root / row["relative_path"]
    if not path.is_file():
        bad.append((row["relative_path"], "MISSING"))
        continue
    data = path.read_bytes()
    digest = hashlib.sha256(data).hexdigest()
    if len(data) != int(row["bytes"]) or digest != row["sha256"]:
        bad.append((row["relative_path"], len(data), digest))
assert not bad, bad[:20]
computed = hashlib.sha256(manifest.read_bytes()).hexdigest()
recorded = (root / "PACKAGE_ROOT_SHA256.txt").read_text().strip()
assert computed == recorded, (computed, recorded)
print("manifest rows", len(rows))
print("package root", computed)
PY
```

Confirm no packaged path contains:

- `__pycache__`
- `.pytest_cache`
- `.pyc`

## Reproduce active V3 review

Install:

```bash
python -m pip install pytest numpy pandas scipy
python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
```

Then:

```bash
export PYTHONPATH=src:.
python scripts/agent/audit_teacher_student_integration_freeze_v3.py --root .
python -m pytest -q \
  tests/test_c2_mandatory_gradient_gate_v1.py \
  tests/test_f1b_successor_attack_suite_v1.py \
  tests/test_population_access_registry_v1.py \
  tests/test_healthy_teacher_training_contract_v1.py \
  tests/test_teacher_student_unified_runtime_v1.py \
  tests/test_teacher_student_integration_freeze_v3.py
```

The C2 test must actually replay K0 and K1 from the packaged JSON files; do not accept a skip caused by missing evidence.

Verify the healthy-teacher base manifest and population-registry manifest can each be fully reproduced from packaged members.

## Production mechanics review focus

Independently inspect that the active V3 runtime preserves:

1. exact 41,238-address / 160-D / 6-block / 4-head model geometry;
2. EMA teacher no-grad/eval semantics;
3. exact predictor 15-tensor mandatory registry;
4. four exact 40%-hidden MEASURED_SCALAR target-mask views;
5. forward under CUDA fp16 autocast;
6. scaled backward with autocast disabled;
7. unscale before gradient gates;
8. exact missing/nonfinite/all-zero rejection for 48 backbone + 15 predictor tensors;
9. no arbitrary small-gradient floor;
10. optimizer step proved before EMA;
11. both Adam moments finite/nonzero;
12. EMA parameters and buffers follow canonical equation;
13. exact movement adjudication against repeated AdamW decay-only counterfactual;
14. checkpoint/RNG/config/source bindings;
15. no biology-dependent early stopping;
16. u0→u40 only; no automatic u40→u205 continuation;
17. no legal resume from historical defect-inherited u10–u205.

## Prospective relational extension

Also review, but do not confuse with active V3 training:

- `docs/agent/TEACHER_STUDENT_RELATIONAL_EXTENSION_V1_20260908.md`
- `src/sea_ad_jepa/v4/prospective_relational_teacher_student.py`
- `tests/test_teacher_student_relational_v1.py`

Run:

```bash
python -m pytest -q tests/test_teacher_student_relational_v1.py
```

Review the six requested design areas:

1. **Representation:** direct 160-D teacher/student `cell_state`; no learned projection in V1.
2. **Exact relational loss:** 0.5 normalized within-group pair-distance Smooth-L1 + 0.5 within-group cosine/angle Smooth-L1; teacher sets distance scale; neighborhood overlap is diagnostic only.
3. **Relational batch:** prospective 8 donor×operator groups × 16 cells = 128; this does not replace the current frozen schedule.
4. **Fine-matched null:** deterministic no-fixed-point permutation within prospectively frozen fine strata; real stratum authority remains unresolved and therefore relational training is not activated.
5. **Collapse:** exact variance, pairwise-spread, and entropy-effective-rank metrics; numerical lower ratios require a separate outcome-blind calibration authority rather than invented defaults.
6. **Evidence/population:** current u0→u40 remains 60% evidence only; qualification may inspect 20/40/60/80/100; 100% is never a hidden-target training dose; moving to 4,553,407 FULL104 reader-fit cells requires a separate scale-up schedule/root/review.

A defect in this prospective relational adjunct should not be conflated with a defect in the already frozen active V3 source unless it reveals a contradiction in shared assumptions.

## Execution firewall

The package must not contain either:

- `docs/agent/HEALTHY_TEACHER_U0_U40_EXECUTION_AUTHORITY_V1.json`
- `docs/agent/HEALTHY_TEACHER_CONTINUATION_EXECUTION_AUTHORITY_V1.json`

Real training remains unauthorized.

## Requested terminals

If active V3 source/replay packaging is clean:

`PASS_TEACHER_STUDENT_UNIFIED_V3_INDEPENDENT_REVIEW__TRAINING_STILL_UNAUTHORIZED`

If the active V3 source or self-contained package has a defect:

`STOP_TEACHER_STUDENT_UNIFIED_V3_INDEPENDENT_REVIEW__<PRECISE_REASON>`

For the relational adjunct, report separately:

- `PASS_RELATIONAL_EXTENSION_V1_PROSPECTIVE_REVIEW`, or
- `STOP_RELATIONAL_EXTENSION_V1_PROSPECTIVE_REVIEW__<PRECISE_REASON>`

Do not authorize u1, real F1/T0/D1, pathology, reader-validation/oracle, DEV, or SEALED as part of this review.
