# JEPA V26 — independently verified parallel-lane ownership and GitHub collision map
Snapshot: 2026-09-25 Eastern evening / 2026-09-26 00:47 UTC. Status: DOCS-ONLY successor STACKED on existing PR #123 handoff. Main remains c49b13bd75c2d23716c777336db8fbfc78c09cd0. **Re-fetch live PR heads before any cherry-pick/merge or GPU input use**; this is an audited snapshot, not a mutex against future collaborators.

## Governing execution state

TRAINING=OFF; AUDIT_B_N1=UNOPENED; PROTECTED_FULL104_OUTCOMES=UNOPENED; D_SHARED_G5=UNOPENED; RARE_TAIL_MOLECULAR=UNOPENED; THERAPEUTIC_RANKING=OFF. No new permission follows from a docs-only review or a green synthetic CI job. The reader 104 fit / 22 validation / 23 oracle, distinct foundation 24 development + 24 sealed and separate Siletti whole-study holdout remain unchanged. Development metadata and legitimate fit-donor exploration are separate from prohibited outcome-responsive target search and from release of held-out results.

## Already assigned workstreams — do not restart

| Lane | Exact observed live head / PR | What it owns | Physical / scientific status |
|---|---|---|---|
| V5 optimizer-step chronology | PR #129 `eebe40a92c7d590a480657c08d9fcacdefa5e7a9` | one-use poststep receipt and contiguous cursor; legacy-V4/current-V5 seam audit | synthetic only, not runnable full current-V5 training |
| Frozen reader metadata | PR #130 `106df4971728be014ef438cb2e0c36c74d6e0455` | authenticated Aug24 metadata and 104 fit donors | 25 metadata adversaries, not per-cell raw Level4 lineage |
| Frozen pass1 count histogram | PR #132 `1e9080caeeea40e2a3a0879e236b1bc0a90e919f` | cheap frozen pass1 `duniq/cell_donor` ↔ Aug24 per-donor histogram | synthetic tests pass; REAL pass1 physical compare NOT yet reported |
| Single-update exact scientific proposal | PR #135 `4b4c6fa93f1e93d5feba1db0ff456d23e46de983` | exact donor-uniform per-slot proposal, unique selection rows and prospective max 81 presentations/update | 79 synthetic tests pass, no real source sampling or training |
| Prepared repeated sampling | PR #139 `9a36c7c71d5d0b10baef9db2fce232c99c200c5f`, STACKED on #135 | source-authenticate once and reuse immutable byte-backed row index with exact #135 parity | 102 synthetic tests pass; real frozen pass1 absent in ChatGPT environment |
| Heavy read-only raw count | PR #120 `136d1257467a7d80690d9f287ca7399c7138e038` + stacked #124 `8ad966faa9763e46cf097ede034691e7d151ce4a` | 8,915 Level4 exact count and per-row source-library verification on GPU machine | physical 104×17,186 all-raw outcome-blind reaggregation NOT proved in these PRs; no actual protected N1 burden authorization |
| Original historical 94-donor Layer-2 | PR #133 `b71291a927b56f67e645f9c468350c76a29b9a63` | SHA-bound original scratchpad search and exact estimator replay only if old files are recovered | original bind_population, screen_out, final_manifest ABSENT, historical R² authenticated as recorded but NOT replayed |
| NEW full104 104-fit linear comparator | PR #136 `ac3088df739c4e1f0d964d5d22f8d56727435eb4` + stacked independent #138 `85af8b1683cb6dc70867194a0b04ebc6a16f541b` | new same-cell molecular VALUE_ONLY linear LODO; fixes score cancellation, receipt roots, invalid donor code and read/hash race | 24 red-team tests SUCCESS [CI 36201475378](https://github.com/dushyant-mishra/sea-ad-jepa-agent/actions/runs/36201475378); no physical R² yet; DISJOINT VIEW row order independently unqualified |
| Independent bulk effects | PR #131 `8647d21174c5e7eab2ad01fc6b9251b069ba0e03` | physical independent P1-6 GSE241858/GSE240609 recomputation | physical same-source numeric parity DONE; no independent biological replication inference |
| CRISPRbrain clean scientific assessment | PR #137 `e130f55c7471b74b387f0bd0aca586c4a1614921` | verified target engagement and continuous concordance; current producer/receipt | reported clean physical replay: 7 CSVs and 540 scientific fields matched; 31 target-by-name, only 30 guide-anchored, 1 jointly engaged, pooled Pearson .0723 |
| Old dirty-producer replay | PR #134 `3fffc35010bd4d4b1bb6d9574277ac99cf2cecb5`, STACKED on #131 | reconstruct/replay exact initial dirty-origin CRISPRbrain run | **PHYSICAL CI FAILED** [run 36197352491](https://github.com/dushyant-mishra/sea-ad-jepa-agent/actions/runs/36197352491): committed producer SHA != dirty receipt's executed producer. Synthetic adversaries passed; DO NOT bypass this gate. |
| Independent cross-PR scientific/byte parity | PR #140 `ddc6779d6762593bb8aba01af2ff111e4a80bbea`, STACKED on #137 | read-only compare exact PR131 and PR137 existing 7 committed CSVs and every common scientific receipt field; attest clean producer at declared anchor | **10 fixture tests and exact-source real committed-artifact parity PASSED**, [CI 36206147809](https://github.com/dushyant-mishra/sea-ad-jepa-agent/actions/runs/36206147809); original dirty producer still unreplayed, no fresh biological analysis. |

All PR numbers refer to https://github.com/dushyant-mishra/sea-ad-jepa-agent/pull/<number>.

## Verified ancestry and collisions — actual GitHub compare, not inferred from branch names

**The V5 single lineage is #129 → #130 → #132 → #135 → #139**. PR #139 is 34 commits ahead of #132 and zero behind at this snapshot; its base is exact #135. These are STACKED, not five independent changes to the same training branch. Take the reviewed current successor once, following its parents, rather than cherry-picking five whole PR diffs. None proves a full physical current-V5 training step. The real next critical-path seam remains the **current-V5 update/target/mask/optimizer/checkpoint integration** under a prospectively named development-only diagnostic authority, with no fallback to V4 historical training receipts.

**The linear lane is #136 → #138**. GitHub compare confirms #138 is 5 commits ahead of #136 and zero behind. The #138 edits only the baseline script/tests/CI plus its independent audit file. Its published 24/24 zero-skip test success must not be confused with measured full104 R². The separately published Level4 A_full/B_full hashes differ from later V0/V1 disjoint-view hashes; original PHASE2_FEATURE_ROWS alone does NOT certify the new view row order. Independently authenticate V0/V1 derivation/selection_row mapping before promoting the new comparator. This is OFF the GPU diagnostic critical path.

**The two CRISPRbrain histories #131 and #137 really DIVERGED**: GitHub compare of PR131 → PR137 reports **9 ahead, 2 behind**, merge base `bfcd100a48a44a11571173580fc6333ec8715c7d`. They have the SAME source/receipt/output file paths from accidental shared-worktree commits, but PR131 and PR137 producer Git blobs and receipt bytes DIFFER. Seven already committed scientific CSV outputs are byte IDENTICAL; independent #140 CI proved all seven original and clean receipts' digests/row counts, all common science fields and original dirty vs clean producer distinction at pinned commits. PR137 adds explicit caveats to S3 instead of replacing underlying effect estimates. **NEVER blindly merge/cherry-pick both overlapping CRISPRbrain file trees** or overwrite PR137's clean producer/receipt with PR131's dirty record. Preserve PR131's two independent bulk-reproduction receipts. Keep dirty first-execution receipt separately versioned for history; use PR137's clean producer/receipt only for CURRENT clean scientific-result provenance after review. PR134's failed dirty-origin replay is an honest distinct failed historical recovery; do not convert it to a PASS by substituting PR137's newer producer.

**#120 → #124** is a separate stacked heavy raw-verification lineage. The PR132 donor histogram is a cheap complementary check that intentionally reads NO Level4 blocks; it is not a reason to repeat an 8,915-block heavy scan. If the independently authenticated full pass1→Level4 binder's small receipt already exists, reuse it. PR120's all104 donor_umi/donor_nnz reaggregation is a distinct requested physical result and remains unproven until its signed/hash-bound receipt arrives.

**Other noncritical branches**: PR #125/#127 handle GSE301119 matched-NT null and tie-safe correction; the corrected-code physical two-arm rerun has not been certified. PR #126 handles versioned scientific readiness; #128 outcome-blind MTG transport metadata; neither authorizes protected data, therapeutic ranking, or production training. These branches must not become blockers to the first healthy V5 diagnostic.

## Exact one-pass handoff to GPU operator and other parallel chat

1. **Do not re-run:** 221M SRA spots for GSE178317, independent P1-6 bulk recomputation, the PR137 two-screen first science audit, existing 104-fit metadata ZIP checks, historical T1 zero-gradient archaeology, original 94-donor LODO historical study or PR138 synthetic numeric fixes. PR140 already established cross-PR output parity without rerunning source biological screens.
2. **Physical queue, only on the machine that has the sources:** run the cheap PR132 frozen pass1 donor histogram once; carry through the separately existing pass1→Level4 physical binder and PR120/#124 raw-count verification if their distinct raw-read scope is authorized; publish immutable small SHA receipts. Do not rescan 8,915 blocks for the cheap histogram or for PR136's new baseline. Then allow PR139's physical source-pinned read-only proposal preparation, retaining its nontraining-only status.
3. **One primary scientific experiment:** integrate genuine current-V5 teacher/student target/mask, fully live gradients/Adam/poststep EMA, correctly weighted sampler and atomic restart. After named development-only diagnostic authorization, execute a bounded REAL reader_fit pilot, never old V4 production_update. No reserved outcomes. Freeze one configuration. Do not let unrelated therapeutic ETL repairs defer the first healthy diagnostic.
4. **If physical view derivation/selection-row attestation is independently recovered**, run new PR136/138 104-donor linear comparator on the exact original V0/V1 arrays and compare only *matching* population/split/target/weighting estimands. A fully pretrained JEPA on all104 fit donors is NOT donor held-out merely because the linear baseline is LODO. The old94 proxy stays historic without recovered exact scratchpad inputs.
5. **CRISPRbrain gate:** use PR137 plus the independent PR140 parity certificate; leave PR134's physical dirty-origin replay failed without source recovery. No further CRISPRbrain rerun required for current-V5 pilot. Separate within-study protocol contrast from independent biological replication; do not promote pooled outcome tables as benchmark truth.

## Do-not-spill-over checklist for every new branch

At the BEGINNING and again BEFORE PUSH: fetch live HEAD and target PR base; run `git worktree list --porcelain` and `git status --porcelain`; establish a **unique path-only owned worktree**; record initial exact head and hash of any physical input to be read; check open PR changed-filename lists for collisions; never `git add -A` in a shared worktree; never `git checkout --` or destructive reset another worker's files. Put generated outputs in a *new* exclusive directory; use exact source hashes and rename old receipts by scientific lineage, not by whichever commit was last pushed. A CI green status is scoped to its executed suite and head; demand zero-skipped critical gates. Before merging divergent branches, verify common-base ancestry and content hashes; never rely on matching filenames.

This coordination document itself is DOCS ONLY. It authorizes no GPU run, no training, no N1 protected masking, no external Siletti inspection, no broad data source substitution and no therapeutic prioritization.
