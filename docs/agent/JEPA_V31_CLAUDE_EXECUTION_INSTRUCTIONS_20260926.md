# CLAUDE INSTRUCTIONS — JEPA V31 controlled successor after independent audit

Copy these instructions as the primary assignment. You have the separate GPU laptop's heavy data where it is actually mounted. Do NOT assume those assets are available on GitHub or within other ChatGPT containers. Do NOT begin real expression training or access sealed data until independently approved B1 and B2 conditions actually hold.

## First: recover source truth, no duplicate analysis

Open LIVE main:START_HERE.md and main:docs/agent/JEPA_LATEST_HANDOFF_POINTER.json first; main V25 governed at c49b13bd75c2d23716c777336db8fbfc78c09cd0 during this audit. Read main historical-audits index, then PR150 V28 takeover/full handoff/results index, this V31 audit, and PR141 ownership collision map. PR149's inherited START_HERE is older and must not override main. Re-fetch exact PR heads and changed-file lists before working; compare ancestry and use one EXCLUSIVE clean worktree for each disjoint source lane. Independently review published PR151 through PR156 and confirm their exact-head CIs before duplicating work. PR154 already scanned 114 open-PR tips and PR155 all 263 branch names/232 distinct tips under a targeted JSON filter: inspect only CHANGED tips and the GPU-disk provenance, not another wholesale GitHub scan. Preserve frozen PR146/147/149 outputs and original receipt SHA identities; no global shared-worktree git add or blind cherry-pick of diverged branches.

## Lane 1 (critical): authorization-source provenance and version compatibility

Read src/sea_ad_jepa/v5/current_authority_closure_v2.py, current_training_authority_v1.py, current_trainer_preexecution_contract_v2.py, current_teacher_target_receipt_v2.py, PR149 exact six-own-class audit, PR151 exact consumer-class red-team and PR156 three-test synthetic issuance proof. V2 closure has 32 upstream roots; V2 teacher receipt has exactly 33 including preexecution. The six candidates are NOT six closed roots. Map EVERY closure typed input against the scientifically intended ACTUAL schemas, not only two known blockers: V3 panel-independent masking RNG and V3 FULL104 confirmation parameters do not satisfy V1 class guards; further budget V2/design V2/run-contract V4/execution V4/panel V3/precision V4 successors may also be incompatible semantically. Do not auto-cast/rename any object to V1, fabricate role hashes, import historical 94-donor candidate parameters, or relax the live validator. Propose an independently reviewed coherent new versioned current closure after scientific choices are made, with source-parent byte authentication, exact current schema/cross-role validators and negative swapping tests.

PR156's controlled test proves the current issuance ENTRYPOINT will accept a self-consistent INVENTED closure map and signed-looking V2 receipt without the actual graph validator having run. Treat this as a HIGH source-provenance boundary issue separate from basic SHA integrity. Implement a versioned, fail-closed issuer/adapter that binds the actually executed live-validated current closure and authentic producer/parent receipts, and rejects PR156's invented-map negative control. Provide an all-synthetic VALID graph positive control for mechanics, but do not label that as scientific authority. Explicitly enforce or bind separately approved frozen B1 reader_fit population contract at the effective GPU runtime. Never assume B2 cryptographic shape creates B1 permission. Do not rewrite the immutable historical issuer tests/receipts to hide the failure. Maintain TRAINING_OFF until real source-backed B1+B2 are reviewed.

## Lane 2 (independent): failed broad masking CI must be explained

PR143's dedicated G3 28-test CI passed; PR144's own G3 50-test integration CI passed. At PR144 exact head 21efb1047c73cfe7b88047f0aeee1978b035971f the broad FULL104 masking runner 36208393701 FAILED at its regression step and the no-skip check never ran. Recover the COMPLETE failing command, actual pytest assertion/traceback and source/environment ancestry from that run, or reproduce the identical test selection at exact head in a clean qualified environment. Do not infer causation from branch title; test whether inherited failure or PR144 own changes. Record failing test and why, fix ONLY if warranted in a separately owned worktree, add planted positive and negative controls and rerun exact affected broad suite plus JUnit zero-skipped/missing-test census. Mark the broad masking gate RED until a real independent exact-head successful replay. Do not substitute PR144 narrow green CI or a later workflow skipped by path filters.

## Lane 3 (scientific owner decision): finalize the ONE prospective teacher-target draft

Use PR152's existing typed JSON and 15-tests-passed reviewer memo, not another teacher proposal. Actual target is a query-local LATENT molecular/cellular state from a detached EMA teacher; student has canonical query identity plus lawful remaining strictly measured RNA, with query scalar withheld before ANY contextual mixing, normalization or leakage path. The teacher may observe lawful full measured state including query realization to construct its contextual latent target, but there is NO scalar expression regression objective. Freeze the real target-construction source SHA, shared query provider and explicit gradient boundaries. Prospectively require identity-only, identity+lawful-global/no-remaining-RNA, measurement-support-only, source/operator/library-depth and donor/cell-key cheating controls. A source fingerprint or query identity must not count as learned biology.

Obtain the scientist's explicit prospective approval of the true source-stratified held-out FIT-donor roster, actual D_train, selected one bounded masking candidate and burden with honest qualification status, real source-library normalization, base scientific p_i=1/(D_train*n_d) and explicit sampler q with p/q correction. Never substitute D1 equal-operator or relational V2 weight denominators. A full104-trained teacher CANNOT claim unseen same104 donor validation. September16 nine-cell grid selected NO qualified mask, and any uniform first-development mask remains UNQUALIFIED until reviewed scope and B2 execution evidence genuinely permit it. Freeze a GPU-resource-bounded provisional width/depth/head/FFN, operator-specific measured tokens/microbatch limits, presentation-based EMA half-life, LR, seed, finite update budget and stopping rules **BEFORE** development outcomes. Leave unsupported fields NULL; historical .40 mask/.99 or .996 EMA/width32/128x8/D_shared are NEVER accepted defaults. Freeze prospective objective-matched six-state G3 scoring, missing/nonestimable terms, negative controls, uncertainty/effect thresholds and like-for-like current104 linear comparison. Protected N1, D_shared, reader validation/oracle, foundation holdouts, pathology and Siletti must remain untouched.

## Lane 4 (engineering): integrate EXISTING negative controls, no new architecture

PR147 original 40-step CPU synthetic run proved nonthrowing updates and real varying gradients; old 40/40 all-zero report counters were VACUOUS and withdrawn. Current inactive V5 harness ALREADY refuses zero/nonfinite/teacher gradient INLINE before optimizer and EMA. PR153 separately passed 17 synthetic tiny-file controls on missing/malformed per-parameter reports, skipped/repeated optimizer step, beyond-decay parameter movement, BOTH Adam moments, EMA/presentation-cursor order and missing/truncated checkpoint parts/atomic commit. Do not rewrite the old inline gate or call synthetic tests a real GPU result. Rebind reviewed PR153 safeguards to the actual current protected-tensor registry and current receipt in a separately owned new V5 update path, never old V4 production_update. Fail CLOSED on missing verifier/report/checkpoint, empty Adam moments, non-finite/zero prohibited protected gradient or unproved step; optimizer once then EMA then accepted-presentation cursor then atomic fully re-openable checkpoint. Prove exact one-update replay and adversarial mutation with explicit positive control and exact zero-skip test census. This source integration still needs B1 and full current B2 before consuming actual reader_fit expression.

## Lane 5 (separate remote physical input task): all104 raw Level-4 verification

PR120 plus independent PR124 per-row source_library fix own the unresolved 8,915-block READ-ONLY raw donor UMI/NNZ comparison. The previous attempt timed out: NO scientific verdict. If remote read-only authority exists, build restartable SHA-authenticated immutable per-block/chunk receipts, exact once durable aggregator and interruption/resume + duplicate/source-swap/donor-ID-permutation red-team. Verify original frozen pass1 SHA, current raw manifest byte SHA, EVERY source_library against corrected donor source registry, full-source rather than ledger-subtotal normalization, complete selection-row coverage and exact integer output. Do NOT rerun PR146/149 calibration extraction or PR150 1,400-row derivative, do NOT open N1 terminal masking or protected outcomes. If remote disk inaccessible, publish explicit runbook and NOT_EXECUTED status, never invent a physical PASS.

## Only after real approval: one bounded current104 GPU pilot and separate biological evaluation

B1 requires a separately owner-approved frozen reader_fit training contract. B2 requires every actually current 32-root class/parent/role/execution cross-binding, one genuine preexecution root, exact source-attested V2 or reviewed successor receipt, corrected issuer provenance, critical-test and runtime-source proof. SCIENCE requires already frozen target/mask/split/scoring/EMA/model/resource settings. If any gate is absent, STOP before actual training. Otherwise build the sole current FULL104 streaming reader→student/predictor/detached teacher/optimizer/EMA/atomic checkpoint adapter, verify actual denominator, source/donor IDs, strict measured support, scalar-withhold and p/q before any update. Run one prospectively bounded reader_fit development diagnostic with genuine protected-gradient/Adam/poststep/EMA/step-replay receipts. Label engineering PASS independently from any biological score. Evaluate molecular information only on genuinely held-fit-donor development data using the objective-matched six-state G3 and exactly like-for-like current104 baseline after physical view row-order attestation; undefined six-state terms are NONESTIMABLE, not zero. Sealed populations remain closed absent their distinct prospective release authorization.

## Mandatory evidence and self-audit exit

For EACH lane report exact parent/head, path ownership, source/input whole-file hashes/sizes, population/support/split, scientific estimand p vs proposal q, experimental exposure, current-vs-historical status, command, complete run log, measured positive and injected negative controls, exact pass/fail/skip/xfail census, output SHA receipt and independent replay. Retain PR144 failed run and PR156 forged-envelope finding until specifically fixed; green negative-control diagnostic is not a fixed gate. Treat S9 as RETRACTED (qualified canonical env requires Library/bin PATH). Do not repeat old T0/QID/T1 archaeology, dirty-origin CRISPRbrain PR134 failed replay, or re-pick historical numbers. Publish a scoped draft PR per disjoint owner; never merge full diverged lineages blindly. If authorization remains open, deliver a reviewable honest audit and keep TRAINING_OFF, not a fabricated checkpoint.
