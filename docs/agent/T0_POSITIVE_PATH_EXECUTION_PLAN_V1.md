# T0 Positive-Path Execution Plan V1

Goal: maximize the probability of a **genuine** positive T0 without compromising confirmation.

## Parallel Track A — B2 / eligibility

Continue current producing-lane repairs. No production B2 until external review passes.

Then produce:
- authenticated candidate expression substrate;
- Q_DEPTH / Q_DETECT exact summaries;
- IMMUNE donor support counts;
- IMMUNE_FRACTION authority;
- technical_complete authority;
- age/sex authority;
- eligible donor universe;
- deterministic donor roles.

## Parallel Track B — exact parent-pass power simulator

Use the exact accepted V20 adjudicator and studentized Freedman-Lane implementation, both authenticated against externally frozen source digests.

The simulator:
- calls the exact frozen parent statistical primitives;
- synthesizes nonnegative AT8 outcomes only;
- estimates joint probability of clearing primary + composition + measurement parent requirements;
- has no approximate statistical fallback.

### Two runs

**Design run:** after donor roles, before target learning, to understand the exact 18-donor design envelope.

**Target-specific run:** only after the discovery target is irrevocably frozen. Compute confirmation STATE_SCORE pathology-blind, then rerun power on that exact predictor/covariate geometry before opening AT8.

The target-specific run may characterize power/executability but may not trigger target redesign from confirmation information.

## Parallel Track C — discovery qualification

Use the frozen V18 target learner exactly as accepted.

Require donor-disjoint OOF discovery predictions.

The discovery gate is calibrated from the exact-engine power curve and frozen before OOF discovery results are inspected.

The power curve's default effect parameter is the STATE_SCORE partial correlation after the combined pathology-blind basis:
`[1, age_c, age_c², sex, IMMUNE_FRACTION, Q_DEPTH, Q_DETECT]`.

Therefore discovery projects the matching delete-one-donor minimum joint-full-sensitivity OOF effect through the exact-engine power curve.

## No tournament in this T0

The V18/V20 target family is `PRIMARY_ONLY`. Adding a candidate tournament now would reopen the scientific specification.

If the current frozen target cannot qualify in discovery, preserve confirmation unopened and create a separately versioned successor T0 design. Do not silently expand the current family.

## One-shot sequence

```text
B2 accepted
  -> exact eligible donors
  -> deterministic roles
  -> exact-engine design power curve
  -> FREEZE discovery qualification thresholds
  -> discovery learner + LOODO OOF
  -> discovery qualification audit
     -> HOLD: confirmation remains untouched
     -> GO:
          freeze target
          -> pathology-blind confirmation predictor materialization
          -> rank/estimability preflight
          -> target-specific exact-engine power diagnostic
          -> open confirmation AT8 once
          -> frozen V20 adjudication
```

This plan does not alter any frozen V18/V20 decision threshold.
