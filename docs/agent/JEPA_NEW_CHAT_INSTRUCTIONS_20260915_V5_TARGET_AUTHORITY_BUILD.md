# JEPA NEW-CHAT BUILD INSTRUCTIONS — V5 TARGET AUTHORITY / ANTI-CARRYOVER

Date: 2026-09-15
Status: `CONTINUE_CURRENT_V5_TARGET_AUTHORITY_BUILD__NO_TRAINING_AUTHORITY`
Repository: `dushyant-mishra/sea-ad-jepa-agent`
Handoff branch: `handoff/jepa-v5-target-authority-build-20260915`

This file is the paste-ready operating instruction for a fresh ChatGPT/agent session. It is deliberately detailed so the next session does not restart the JEPA project, repeat closed audits, or silently inherit historical V4/V21/T0 scientific/numerical choices.

## 0. First instruction to the new chat

Continue the existing JEPA project. **Do not restart the project from scratch.** Use GitHub actively. Before changing anything, re-fetch live heads rather than trusting SHAs in this document. Treat branch names and commit messages as navigation, not scientific authority.

The current task is **not production training**. The current task is to finish the dataset-first current-V5 teacher-target/anti-shortcut/governance authority chain while continuously red-teaming for accidental historical carryover.

Keep these hard boundaries active unless an explicit later governance action changes them:

`TRAINING_OFF`

`NO_D_SHARED_OUTCOME_EXECUTION_OR_INSPECTION`

`IF_CONFIRMATION_DATA_COULD_CHANGE_A_DESIGN_CHOICE_DO_NOT_LOOK`

Do not inspect protected/pathology/DEV/SEALED outcomes, D_private, D_obs outcomes, TD60, or any confirmation result that could influence an unresolved design choice.

Do not mutate the production trainer merely to make a design executable. Current work is prospective specification, authority repair, static/mechanical qualification, outcome-blind masking/shortcut design, and governance cleanup.

## 1. Mandatory startup order

Open/read in this order before doing new work:

1. `START_HERE.md` from live `main`.
2. `docs/agent/JEPA_LATEST_HANDOFF_POINTER.json` from live `main`.
3. `docs/agent/JEPA_NEW_CHAT_HANDOFF_20260915_V5_TARGET_AUTHORITY_CURRENT.md`.
4. `docs/agent/JEPA_HISTORICAL_AUDITS_INDEX_20260915.md`.
5. `docs/agent/JEPA_NEW_CHAT_HANDOFF_STATE_20260915_V5_TARGET_AUTHORITY_CURRENT.json` from this handoff branch.
6. `docs/agent/JEPA_WORK_LEDGER_AND_FINDINGS_20260915_CURRENT.md`.
7. `docs/agent/JEPA_NEW_CHAT_HANDOFF_20260915_LAYER2_MASKING_CURRENT.md`.
8. `docs/agent/JEPA_NEW_CHAT_HANDOFF_20260911_TARGET_DISCOVERY_V5_INTEGRATED_CURRENT.md`.
9. `docs/agent/JEPA_RUNTIME_ASSET_STATUS_20260910_CURRENT.json`.
10. `docs/agent/JEPA_FORMULAS_AND_AUTHORITY_LEDGER_20260909_FINAL_R4.md`.
11. `docs/agent/JEPA_HEAVY_ASSET_REFERENCE_20260909_FINAL_R4.md`.
12. On the planning lane, read:
    - `docs/agent/V5_TEACHER_STUDENT_EMA_ARCHITECTURE_DRAFT_20260915.md`
    - `docs/agent/V5_ACCIDENTAL_CARRYOVER_LEDGER_20260915.md`
    - the dataset-to-target issue matrix added at planning commit `062b9b7360105fcb76986ce931333acaf9c9d7d3`
13. On `planning/v5-teacher-target-redteam-20260915`, read `docs/agent/V5_TARGET_IDENTITY_SHORTCUT_AUDIT_SPEC_DRAFT_20260915.md`.
14. On Claude's Layer-2 lane, read `docs/agent/V5_LAYER2_CROSS_VIEW_SHORTCUT_CLOSEOUT_20260915.md` and `analysis/v5_layer2_cross_view_shortcut_20260915/Z_RESIDUAL_OVER_CONTEXT_SPECIFICATION.md`.

Before repeating any analysis, classify the proposed work as exactly one of:

- `ALREADY_AUDITED`
- `SUPERSEDED`
- `OPEN`
- `CHANGED_INPUT_REQUIRES_REQUALIFICATION`

If it is `ALREADY_AUDITED`, do not rerun it merely because the chat changed.

## 2. Live heads to re-fetch immediately

These were the known heads during handoff creation; they are **not permanent authority** and must be re-fetched:

- `main @ c8898923fc10ffa5ef0662b04908b0d94bcb158b`
- `analysis/v5-layer2-cross-view-shortcut-claude-20260915 @ 219831b899b914984369c7a41828bf750554d1d9`
- `repair/v5-dshared-authority-v2-20260914 @ 3717c9c0a292dfcd883949d5d0bf36d263f79300`
- `planning/v5-teacher-student-ema-canonical-20260915 @ 88c45b97b3e9a4ad3c0fd178c992f844fd58dbab`
- `planning/v5-teacher-target-redteam-20260915 @ d9a678f261797f5dfc7a6e6b7a281e413ae7e0ae`
- this handoff branch starts from `main @ c8898923...` and contains the current machine state/instructions/plan.

Important governance fact: the canonical planning lane is materially divergent from main. **Do not blindly merge it.** Use exact-SHA evidence until reconciliation is deliberate and reviewed.

## 3. Governing scientific order

The permanent design order is:

`DATA -> SUPPORT/ESTIMABILITY -> SCIENTIFIC ESTIMAND -> TARGET SEMANTICS -> PRODUCTION GEOMETRY -> MODEL`

Do not reverse this order. In particular:

- do not choose a model width and then reinterpret D_shared to justify it;
- do not choose a masking scheme and then use downstream biology to tune it;
- do not choose a convenient sampler and let it silently define the scientific estimand;
- do not call a common-core coordinate biological merely because it is comparable;
- do not call a representation biological because a variable/file/function contains `bio` in its name.

Permanent semantic rules:

`NAMES_DO_NOT_CREATE_CAUSAL_OR_BIOLOGICAL_AUTHORITY`

`COMMON_CORE_SUPPORT_IS_COMPARABILITY_NOT_BIOLOGY_AUTHORITY`

`DIMENSION_ESTIMAND_AUTHORITY != BASE_TRAINING_ESTIMAND_AUTHORITY`

`QUALIFICATION_RANK_CEILING != PRODUCTION_MODEL_WIDTH`

`DO_NOT_UNLOCK_V5_BY_RECEIPT_ONLY`

## 4. Dataset facts that must shape the target

FULL104 authenticated working facts:

- 4,553,407 cells
- 104 donors
- 42 operators/matrices
- 41,238 molecular addresses
- 17,186 common-core addresses
- 8,915 Level-4 blocks
- sources SEA-AD, HVS, NPH52
- historical manifest root `66f589e56badb1487058f2c95940c3e4b37196e3ab5e9c6ea1ffbe7098d2ea29`

Heavy expression substrate is on the GPU laptop at:

`D:/Jepa project/outputs/full104_v014_20260826/03_phase2_state_derivation_v1/expression_level4/`

Do not pretend the >30 GB substrate is local if it is not. Heavy empirical runs belong on the machine that actually has the data. ChatGPT can continue code/governance/static-review work without fabricating access.

Permanent normalization fact:

`FULL_SOURCE_LIBRARY_EXTENDS_BEYOND_41K_LEDGER__SOURCE_DEPENDENT_OUTSIDE_LEDGER_FRACTION`

The 41K ledger sum must not silently substitute for an authenticated full-library denominator when the historical contract requires the latter.

Other target-relevant dataset issues:

- unmeasured is not biological zero;
- measured support differs by operator/source;
- common-core is useful for comparability but is not all biology;
- native support is evidence but is not automatically private biology;
- visibility/missingness is strongly QC/observation-bearing;
- source/operator structure is confounded with cohort/platform/region/disease/donor composition and real biology;
- SEA-AD dominates raw cell count relative to HVS/NPH52;
- source-specific donor transport differs strongly;
- correlated molecular partners can make masking tasks locally easy;
- target address identity itself can become a prediction shortcut.

## 5. Current representation state

Common-core K=2 split:

- V0 = 8,568 addresses
- V1 = 8,618 addresses
- disjoint union = 17,186

FULL104 array SHAs:

- V0 `3b3f102c6767727ca4ab56832f8e70baf203676d6b65973c42903b22b6d56ada`
- V1 `c41df46d842d643f04566b8523a8aa711fa54bec1c836e0b394c0146017f231c`

Visibility ablation showed visibility channels are strongly dominated by observation/QC state. Current candidate, **not frozen**:

`PRIMARY_MOLECULAR_REPRESENTATION_CANDIDATE = VALUE_ONLY_256`

Do not silently reintroduce visibility channels into the primary molecular path because an older tensor layout contained them.

`PRIMARY_REPRESENTATION_AUTHORITY_NOT_YET_FROZEN` remains open.

## 6. Claude Layer-2 work is closed for its tested scope — do not repeat

Claude's pushed closeout is:

`analysis/v5-layer2-cross-view-shortcut-claude-20260915 @ 219831b899b914984369c7a41828bf750554d1d9`

Established at the tested linear model class:

- matched measurement-state prediction never beat the clean-predictor construction;
- excess shared-measurement synergy peaks near +0.0051 at p=.25, about 1% of full-depth cross-view R2;
- within-donor cross-view signal beyond measured Q_DEPTH/Q_DETECT recurs across all three sources;
- 93/94 donors are positive, with 93.6% above 0.10;
- full-refit LODO roughly SEA_AD 0.241, HVS 0.045, NPH52 0.055;
- empirical/FULL104 weighting increases molecular increment over context to about +0.1207 versus +0.0311 in the unweighted reconnaissance sample;
- old pooled `context dominance` is therefore not population-level authority;
- residual-over-context remains specification-only.

Permanent interpretation limits:

`MEASUREMENT_SHORTCUT_NOT_DEMONSTRATED_AT_THIS_MODEL_CLASS` does not mean absent.

Do not call within-donor remainder biology merely because measured QC does not explain it.

Do not call source/operator structure technical without causal evidence.

`NO_REAL_RESULT_FROM_THE_DISCARDED_X4_WITHIN_ESTIMATOR_IS_AUTHORITY`.

Do not rerun Claude's context decomposition, estimand sensitivity, donor recurrence or full-refit LODO unless the underlying inputs/model class/scientific question materially change.

## 7. Teacher/student/EMA architecture already exists

Do not waste a session rebuilding the architecture from scratch.

Mechanics lineage:

`repair/v5-dshared-authority-v2-20260914 @ 3717c9c0a292dfcd883949d5d0bf36d263f79300`

Key files:

- `src/sea_ad_jepa/v4/teacher_student_runtime.py`
- `src/sea_ad_jepa/v4/ema.py`
- `src/sea_ad_jepa/v5/qualified_teacher_student_runtime_v1.py`
- `src/sea_ad_jepa/v5/qualified_teacher_target_receipt_v1.py`
- V5 optimizer/protected-registry/preexecution/checkpoint guards

Reusable mechanical skeleton:

`fp16 forward -> backward outside autocast -> unscale -> protected-gradient gate -> optimizer step proved beyond decay -> Adam exp_avg proved -> Adam exp_avg_sq proved -> EMA update -> successful presentation cursor advance -> atomic checkpoint telemetry commit`

Teacher mechanics:

- exact copy of online encoder initially;
- `eval()` always;
- no backprop teacher gradients;
- floating parameters/buffers EMA-updated;
- non-floating buffers copied exactly;
- exactly one EMA update after a successful optimizer step;
- mixed-precision skipped step must not advance EMA.

The blocker is **current V5 authority binding and target semantics**, not architecture existence.

## 8. Historical defaults are quarantined

Historical values such as:

`width=160, heads=4, blocks=6, effective_batch=128, microbatch=8, views=4, mask=.40, target_blocks=16, EMA=.996`

plus historical optimizer hyperparameters, scaler settings and seeds are mechanics history only.

They are not current V5 authority.

The new chat must continuously check every design/code path for accidental carryover of:

- old width/head/depth defaults;
- old batch/microbatch/view geometry;
- mask fraction/block-count defaults;
- `.996` EMA or optimizer-step-based time conventions;
- historical optimizer hyperparameters;
- historical RNG seeds;
- historical target package roots/receipt kinds;
- 6-block/48-tensor assumptions;
- historical `128x8` test vocabulary being mistaken for current geometry;
- unweighted block loss where current scientific weights are required;
- old protected-registry hashes after architecture changes;
- visibility channels as molecular input;
- historical V4 teacher target semantics becoming V5 authority merely through code reuse.

## 9. Known accidental-carryover seams — active red-team checklist

These are already discovered. Do not rediscover them as if new; repair/design around them.

1. `qualified_teacher_student_runtime_v1.py` defaults to historical V4 `production_update`.
2. The optimizer guard still validates the legacy V21/T0 target receipt schema.
3. `trainer_preexecution_contract_v2.py` hard-codes six blocks, 48 tensors, `PROTECTED_48_*`, and `HISTORICAL_128X8_CORRECTED_UPDATE_REGRESSION`.
4. `atomic_checkpoint_guard_v3.py` correctly uses a current protected registry but still imports part of the V2 critical-test/authority vocabulary.
5. Inherited predictor target queries use `modules.online.tokenizer.gene_identity` for hidden targets, creating a possible shared-parameter/co-adaptation shortcut.
6. Semantic carryover: `z_bio` naming can falsely imply biology authority.
7. Support carryover: common-core can be treated as biological truth, or native support as private biology, without evidence.
8. Estimand carryover: dimension-qualification weighting can be mistaken for base-training weighting.
9. Geometry carryover: D_shared qualification ceiling 512 can be mistaken for production width.

Treat accidental carryover as a standing review criterion for **every** new file and every authority schema.

## 10. Successful current-V5 patterns to preserve

Do not throw away the good dataset-first work.

Preferred patterns already exist in:

- `src/sea_ad_jepa/v5/data_contract_v2.py`
- `src/sea_ad_jepa/v5/schedule_authority_v2.py`
- `src/sea_ad_jepa/v5/proposal_policy_v1.py`
- `src/sea_ad_jepa/v5/production_protected_registry_authority_v1.py`
- `src/sea_ad_jepa/v5/atomic_checkpoint_guard_v3.py` current-registry portions
- `src/sea_ad_jepa/v5/biology_observation_adapter_v2.py` gradient-firewall pattern

These successfully separate or bind:

- support/evidence authority;
- scientific target estimand p;
- proposal q;
- importance weighting;
- relational sampling/weighting;
- schedule/presentation horizon;
- compute packing;
- RNG authority;
- EMA half-life presentations;
- current model-depth-derived protected registry;
- observation gradients away from the primary state.

Future current-V5 execution should be built around these explicit authorities, not by widening legacy V4/T0/V21 schemas.

## 11. Target-identity shortcut — current design issue

Inherited structural path:

`hidden target gene ID -> predictor query -> JEPA loss gradient -> online trainable gene-identity table -> EMA teacher -> future target`

This is **not** an allegation of hidden target-expression leakage. Hidden values are absent. The concern is a target-address identity/co-adaptation solution path and identity-only target predictability.

The draft audit specification is on:

`planning/v5-teacher-target-redteam-20260915`

Leading candidate, not frozen:

`FIXED_OR_SEPARATE_REPLAY_STABLE_TARGET_ADDRESS_CONDITIONING`

The target address still needs to be known, but its query code should not automatically share the online molecular identity parameter table.

Required objective-aligned comparators after teacher target semantics and thresholds are prospectively frozen:

- identity-only/shared-current query;
- identity-only/fixed-address query;
- full molecular/shared-current query;
- full molecular/fixed-address query;
- detached-online-identity diagnostic;
- cell-permuted molecular memory while target IDs stay fixed;
- target-ID perturbation where support/shape makes it lawful.

Do not invent a numerical permissible identity fraction after seeing results.

## 12. Masking shortcut — current design issue

Random masking can leave strongly correlated partners visible and allow local interpolation. Historical JEPA had Pearson-graph masking; later runtime has graph-free uniform masking. Neither is current authority.

Outcome-blind audit must compare at least:

- graph-free uniform;
- historical graph-expanded method as a diagnostic;
- donor/source-recurrent or cross-source-consensus dependency-aware hybrid masking.

Required diagnostics:

- correlated-partner exposure;
- address coverage;
- mask-budget concentration;
- rare-address coverage;
- source/operator dependence;
- deterministic replay;
- sparse measured-support failure behavior.

Do not build a pooled FULL104 correlation graph and call it biology. Pooled covariance can encode cohort/source/operator composition.

Do not choose the structural/random mixture from protected downstream outcomes.

## 13. Context shortcut posture

Do not hard-code source/operator residualization or a frozen context baseline just because the old unweighted reconnaissance result looked context-dominated.

Claude showed strong estimand sensitivity. Current posture:

`QUALIFY_FIRST__DO_NOT_HARD_CODE_RESIDUAL_BASELINE_YET`

Residual-over-context, if ever used, must preserve the full target and change credit assignment rather than redefining raw molecular input/target:

`prediction = stopgrad(frozen_context_baseline(context)) + molecular_increment(visible_molecular_state)`

Allowed label:

`PREDICTIVE_INFORMATION_BEYOND_FROZEN_CONTEXT_BASELINE`

Never call this a biological residual.

## 14. Scientific estimand separation

Keep these candidate estimands conceptually separate:

- empirical/FULL104;
- source-uniform;
- donor-primary/operator-balanced.

The model/runtime must consume externally frozen scientific weights. It must not infer the preferred estimand from which weighting gives better representation metrics.

Do not average the candidate estimands into an arbitrary compromise.

The base-training estimand is still open and is separate from the D_shared/dimension-qualification estimand.

## 15. EMA

Keep the EMA mechanism; reject historical `.996` as authority.

Preferred prospective convention:

`m_u = exp(log(0.5) * p_u / H)`

where `p_u` is the authorized scientific presentation mass consumed by accepted update `u`, and `H` is a frozen half-life in the same presentation unit.

The exact presentation unit and half-life remain open until the base-training estimand and schedule are frozen.

## 16. Exact current open blockers, in build order

The new chat should work in this order unless a newly re-fetched authority changes dependencies:

1. **Teacher target object/semantics**: write the exact prospective current-V5 teacher target contract. Explicitly decide whether inherited `LN(mean(final teacher gene states over target block))` is intended or merely V4 mechanics. Do not execute protected outcomes.
2. **Primary representation authority**: formalize VALUE_ONLY versus observation state boundaries; do not silently restore visibility.
3. **Base-training estimand/scientific-weight root**: freeze the scientific question separately from proposal/packing and separately from dimension-estimand authority.
4. **Target-address query authority**: finalize target-identity audit null/metrics/margins prospectively and decide whether fixed/separate address conditioning is the approved candidate.
5. **Masking authority**: outcome-blind dependency audit/specification and deterministic masking contract.
6. **Objective-aligned shortcut gate**: freeze competitors and superiority/non-inferiority rules before objective-aligned results are inspected.
7. **Current-V5 preexecution successor**: replace V2's 6/48/128x8 assumptions with current protected-registry authority and geometry-neutral test IDs.
8. **Current-V5 target receipt/optimizer guard successor**: create a distinct current-V5 schema; do not widen the legacy T0/V21 schema in place.
9. **Production model/rank geometry**: bind from the protected dimension chain; do not interpret 512 as width.
10. **Presentation-normalized EMA timescale**.
11. **Current-V5 update runtime**: only after the preceding design authorities are approved. It must consume explicit authorities/weights and must not call historical V4 `validate_production_config` or fall through to V4 `production_update` as production behavior.
12. **Mechanics/anti-cheat qualification** with training authority still OFF.
13. Only after all upstream roots are immutable and independently reviewed, create a **separate explicit production-training authorization**. Do not let any receipt/guard infer it.

Parallel but secondary open work:

- HVS/NPH52 donor-transport explanation without forced source invariance;
- V3-null governance reconciliation;
- measurement-robustness decision rule.

These secondary items must not reopen already-closed Layer-2 analyses without changed inputs.

## 17. How to work

Use an iterative self-checking/red-team process. When a design or code change is proposed:

1. state which historical audit classification applies;
2. identify which current authority it consumes;
3. list every historical constant/semantic it might accidentally inherit;
4. write the fail-closed test first where implementation is allowed;
5. implement the smallest change;
6. run targeted tests;
7. run adjacent regression tests;
8. independently re-read the code for leakage/carryover;
9. verify branch head and diff before claiming completion;
10. commit in small reviewable increments.

For independent tasks with no shared state, parallelize them. Do not parallelize writes to the same file/authority chain.

Before claiming anything is fixed, verify it with executable tests or exact source/receipt inspection. Do not treat a skipped test as a pass.

## 18. Heavy-machine division of labor

Claude/the GPU laptop has access to the >30 GB FULL104 substrate. Use that lane for heavy empirical computations only after the relevant audit/specification is prospectively frozen.

Good heavy-machine tasks later may include:

- outcome-blind dependency-graph statistics for masking;
- support/address-frequency summaries required by a frozen masking spec;
- full-population mechanics qualification once contracts are fixed;
- objective-aligned target-identity comparators only after teacher target semantics and decision thresholds are frozen.

Do **not** ask the heavy machine to search protected outcomes to choose architecture.

ChatGPT-side work can continue immediately on:

- code archaeology and exact current target semantics;
- current-V5 receipt/preexecution schema design;
- static anti-carryover tests;
- target-address interface design;
- masking audit contract and deterministic metrics;
- authority docs/governance reconciliation;
- TDD implementation after explicit design approval.

## 19. Branch discipline

- Re-fetch heads before every merge/rebase/cherry-pick decision.
- Do not delete branches merely because another branch has a similar name.
- Do not blindly merge `planning/v5-teacher-student-ema-canonical-20260915` into main; it is divergent.
- Prefer a clean successor branch from live main for each approved implementation unit, then deliberately port the exact reviewed planning concepts.
- Preserve historical/withdrawn defective code when it is needed to reproduce a defect claim, but label it do-not-run/forensic-only.
- For current authority, bind exact file/blob/commit SHAs in receipts rather than relying on branch names.

## 20. Expected first response from the new chat

After reading the startup documents and re-fetching live heads, the new chat should report:

1. exact live heads;
2. whether any authority/handoff file changed since this handoff;
3. the historical-audit classification of the first proposed task;
4. the top unresolved dependency it will work on;
5. the accidental-carryover checklist relevant to that task;
6. confirmation that `TRAINING_OFF` and D_shared/protected sealing remain active;
7. the specific branch it will use for any docs/code changes.

Then it should **start the work**, not merely restate project history.

## 21. Recommended first concrete task

Start with the teacher-target/preexecution authority seam, not with model training.

First recover the exact inherited teacher target computation and every dependency on V4 `BlockPredictor`, `gather_block_states`, gene identity and hidden target blocks. Then produce a prospective current-V5 teacher-target contract that explicitly binds:

- teacher input support;
- teacher state location/layer;
- block aggregation semantics;
- target normalization;
- target-address conditioning semantics;
- student-visible support;
- scientific weights/loss aggregation interface;
- gradient boundaries;
- EMA boundary;
- masking authority reference;
- representation authority reference;
- current receipt roots;
- anti-carryover assertions.

Do not freeze numeric model geometry in that contract unless its separate authority is already established.

In parallel, inspect `trainer_preexecution_contract_v2.py` and draft a geometry-neutral successor that consumes `ProductionProtectedRegistryAuthorityV1` rather than hard-coding 6/48/128x8.

These two tasks are complementary and can be reviewed independently before any current-V5 trainer implementation.

## 22. Terminal state at handoff

`TEACHER_STUDENT_EMA_MECHANICS_EXIST`

`CURRENT_V5_TEACHER_AUTHORITY_NOT_YET_ESTABLISHED`

`TARGET_IDENTITY_SHORTCUT_AUTHORITY_NOT_YET_RESOLVED`

`MASKING_SHORTCUT_AUTHORITY_NOT_YET_RESOLVED`

`BASE_TRAINING_ESTIMAND_NOT_YET_FROZEN`

`PRIMARY_REPRESENTATION_AUTHORITY_NOT_YET_FROZEN`

`CURRENT_V5_PREEXECUTION_SUCCESSOR_REQUIRED`

`CURRENT_V5_RECEIPT_SCHEMA_SUCCESSOR_REQUIRED`

`ACCIDENTAL_CARRYOVER_AUDIT_ACTIVE`

`BATCH_TECHNICAL_VS_BIOLOGICAL_DECOMPOSITION_NOT_IDENTIFIABLE_IN_FULL104`

`NO_D_SHARED_OUTCOME_EXECUTION_OR_INSPECTION`

`TRAINING_OFF`
