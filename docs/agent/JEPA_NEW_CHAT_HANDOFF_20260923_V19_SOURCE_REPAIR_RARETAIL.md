# JEPA V5/FULL104 — 2026-09-23 V19 new-chat handoff

**Status at verified GitHub snapshot:** N1 physical preflight STOPPED due to a real internal source-code disagreement in the original 242 MB heavy NPZ. Read-only rare-tail V2 PASS, molecular outcome UNOPENED. Windows rare-tail gateway fix implemented/tested in Linux CI; native Windows synthetic execution still required. Diagnostic source-lineage checker code/CI green; NOT physically executed against all Level-4 metadata on the GPU laptop. No repaired heavy NPZ has been created or independently qualified. Training OFF.

## Start and authority
Read this file, `docs/agent/JEPA_NEW_CHAT_HANDOFF_STATE_20260923_V19.json`, `docs/agent/JEPA_V19_DATA_RESULTS_SCRIPTS_MANIFEST_20260923.json`, `docs/agent/JEPA_V19_TAKEOVER_COMMANDS_20260923.md`, and `docs/agent/JEPA_HISTORICAL_AUDITS_INDEX_20260915.md`. Re-fetch live PR heads and GitHub Actions before acting; these SHAs record the 2026-09-23 snapshot, not permission to ignore later changes. The older September 17 pointer on the implementation ancestry and September 22 V18 docs-only handoff are HISTORICAL, not substitutes for V19.

Permanent scientific boundary: DATA -> SUPPORT/ESTIMABILITY -> SCIENTIFIC ESTIMAND -> TARGET SEMANTICS -> PRODUCTION GEOMETRY -> MODEL. Project goal: infer donor-generalizable latent cellular state from partial RNA, not hidden-gene scalar reconstruction. Frozen current primary molecular route VALUE_ONLY_256; QC/visibility observation only. Never silently import Stage81A3, 50K discovery, historical estimates, or exploratory RIDGE8 as FULL104 outcome evidence. No protected/pathology/DEV/SEALED, D_shared/G5 or TD60 inspection while upstream design remains open.

## Verified branch topology (2026-09-23)
| PR | Exact head | Role |
| --- | --- | --- |
| #59 | `1bdff2a8f12d7971652dcf92b5b81ca45c37a933` | Six-donor independent UMI/nnz comparison from authenticated Level-4 raw count blocks; its NPH52/SEA_AD per-donor *text* labels use transposed stored names and must NOT be authoritative. |
| #60 | `7a8b97ef268d3ec34be1d62d4edf0688e83978b8` | Read-only rare-tail physical structural review, no RNA. |
| #61 | `bf248a97f6e5249dc7922e4499fc18128cc89fc5` | Rare-tail V2 physical receipt authentication and guarded gateway. |
| #62 | `8348c7308156a23df08a83897027e55260e3c203` | N1 original-heavy physical binder, fails on actual stored source_names. |
| #63 | `59e16ab6edf3979d5cfc6d18c2f7ac2add6bb159` | Synthetic-only crash-safe N1 journal; PARALLEL descendant of #62 relative to #64/#67; no physical compute adapter. |
| #64 | `9c94a26553bdaa23d46a4efc59e85364d0c0df9b` | Claude real physical N1 STOP evidence; first run rejected stored source order. |
| #65 | `29ec6a0d49b006e49e34143b30a950ad156dbd3f` | Claude real read-only rare-tail V2 PASS and Windows EBADF finding. |
| #66 | `f13c8364fe997d800cb84ad5370da43e75e17d3e` | Separate draft rare-tail Windows gateway fsync fix on #65; Linux CI green, native Windows synthetic rerun outstanding. |
| #67 | `c4a893cbc98bfa736f3d44a2fe9a9ebe31dec6fa` | Diagnostic-only full-metadata N1 cell->donor->source verifier with adversarial tests; V19 handoff is docs-only from this head. |

All #59–#67 heads were re-fetched and unchanged at the above snapshot; all open when checked. #63 and #67 both descend from #62 but diverge after it. #66 belongs to parallel target lineage; DO NOT silently merge them or assume #67 contains the #63 journal or #66 fix.

## N1 source defect: first physical run STOPPED
Authenticated original heavy NPZ SHA-256 `f77dff47df71e2b97895f6e850db4d2a2ebdab441d195dedf91f582b4d53b5ae`, 242,087,519 bytes. Actual `donor_src` numeric code 0=HVS(41), 1=NPH52(17), 2=SEA_AD(46) matches the frozen source-stratified split, sampled donor-source vector and independent Level-4 metadata. But stored `source_names = [HVS, SEA_AD, NPH52]` and stored `src_of_cell` follow FIRST-APPEARANCE order (code1 SEA_AD, code2 NPH52). Hence `src_of_cell != donor_src[cell_donor]`. Original producer `analysis/v5_full104_information_channel_redteam_20260920/scripts/build_core_sufficient_statistics_20260920.py` used `source_names.setdefault(row['source'],len(source_names))` rather than canonical sorted names for cell labels, while donor aggregates indexed `cell_donor`. Four-way donor-level agreement does not certify every possible consumer of wrong cell labels.

Original #62 N1 binder MUST continue rejecting original bytes; `validate_bound_arrays(...,source_names=canonical)` on manually substituted labels is NOT a real physical PASS. PR #64 reports 10/10 synthetic/adversarial negatives rejected. Six-donor original UMI qualifier selected two numeric-code donors per source but printed/stored their names using the defective source_names; re-label transparently in a successor receipt, never rewrite original evidence. PR #59 verified six full donor rows, 387 qualifying count blocks, all 8,915 metadata block hashes and 4,553,407 exactly-once cell selections; it did NOT independently reaggregate all 104 donors' raw counts.

PR #67 diagnostic `audit_b_n1_source_lineage_v1.py`/CLI SHA-pins ORIGINAL NPZ and manifest, authenticates each of the 8,915 Level-4 metadata files, reconstructs donor and canonical source for all 4,553,407 selection rows, rejects mismatch against donor_src, and quarantines wrong source_names or src_of_cell independently. This is not a replacement for PR #62 and not a corrected artifact. Expected original result is `QUARANTINED_SOURCE_ENCODING__N1_STOP` (CLI exit 2). Synthetic physical fixture and resealed malicious metadata tests are included; hosted masking workflow latest exact #67 head `831 passed`, no-skip `825 passed`, both zero skipped.

**Prospective repair decision:** preserve the original NPZ unchanged, create a DISTINCT canonical-source derivative on Claude's GPU/Windows disk with BOTH `source_names` and `src_of_cell` corrected from independently SHA-verified metadata. Prove all other NPZ member arrays are bit/byte-identical by per-array dtype/shape/digest (especially `donor_src`, `donor_nnz`, `donor_umi`, core, donor registry and numerical pools). Independently verify full cell/donor/source mapping and exact source donor/cell counts. New artifact FILE SHA and source-vector digests invalidate old artifact-binding receipts: issue a new qualifier with explicit six-donor transport scope, corrected donor text labels and SHA binding; issue new physical N1 binder/preflight code/receipt pinned to new file and qualification. Never claim #59's six-donor check verifies 104 raw-UMI rows. Independently inspect any source-dependent numerical arrays/consumer before reusing them; if a numeric array is affected, STOP and rebuild/qualify from raw Level-4 instead of pretending metadata-only repair suffices. No derivative exists yet, and no full physical #67 diagnostic receipt exists yet.

## Rare-tail V2 and Windows fix
Claude #65 real V2 read-only preflight: `READY_FOR_INDEPENDENT_REVIEW__NO_RNA_OPENED`; receipt canonical SHA `84e6f716fec00fd660f67dc18bd468759c47a7cc935e9a359c4c4bebabb8efa7`, file SHA `feac846b76b39f6c07b93ef3f78adeb7539b3b0a3b7933a7ec677cf003774ade` (3,093 bytes). Real authenticated 105,553-cell sample and Level-4 manifest; verified against a root containing zero NPZ count blocks. Structural receipt file SHA `7b9ff5938e3940fca8640e98217b1c9cf075597305d80bbc04c6df15d1eae801` and structural provenance file SHA `3f1ecdffbeea6d942ee5dc8d488a8de98cb9c99d00c79cd10a7bb695bd9d9d`. NPH52 folds 1–3: exactly four eligible donors, ZERO SLACK; do not relax prospective thresholds.

Old guarded gateway called `os.fsync` on read-only `stage.open('rb')`, fails Windows `EBADF` after expensive hypothetical compute but before receipt; no real molecular compute was run. #66 minimally switches staged fsync handle to `rb+`, adds Linux CI synthetic test that refuses fsync of read-only descriptors. Exact #66 head's target CI run `35813927942`: 441 passed twice, no skips. Changing gateway bytes changes gateway normalized-source SHA; any old external gateway approval is invalid. Claude must run native Windows synthetic-only suite, independently review new source digest and guard mandatory-path/bypass before any possible expression-opening authorization. Older frozen molecular runner remains technically directly callable; governance must forbid bypass. Rare-tail molecular outcomes UNOPENED.

## CI verified on immutable heads (hosted Linux; NOT physical scientific evidence)
`#63 @59e16ab6`: runtime 40/40, Stage-A 159/159, target 369/363, masking 828/822 PASS twice, zero skips across explicit fail-on-skip checks. `#66 @f13c8364`: target 441/441 PASS, no skips. `#67 @c4a893cb`: runtime 40/40, Stage-A 159/159, target 369/363, masking 831/825 PASS, explicit fail-on-skip gate all GREEN. Diagnostic heavy-data command itself still requires Windows physical run. #65 evidence-only changes triggered no Actions; its Windows machine focused suite was 71 passed / 1 failed / 0 skipped BEFORE #66 portability fix.

## What to do next (ordered)
1. Read V19, verify actual live GitHub heads and files. Do not repeat T0/T1/C2, QID/F1 archaeology, Stage81A3 scope, source/operator Layer-2 discovery or Stage-A structural authority without changed input.
2. On GPU/Windows, execute #67 diagnostic on the original byte-pinned NPZ and Level-4 metadata only, capture immutable quarantine receipt + physical file SHA. STOP on any mismatch beyond the established first-appearance encoding.
3. Create new source-canonical derivative in a new output path, never overwrite the original; independently compare ALL other member arrays and relevant downstream source consumers, with negative controls preserving histograms. Requalify new bytes/labels and #62-style binder; hand independent receipt and exact hashes back for review. No real N1 execution.
4. Independently run #66 synthetic rare-tail focused tests natively on Windows; verify source-bound approval invalidation, exclusive intent, post-subprocess crash, atomic result and receipt, and direct old-runner bypass prohibition. No real RNA opening.
5. Only AFTER new physical N1 preflight receives independent review, integrate #63 crash-safe journal through a reviewed adapter on a successor draft branch; synthetic restart-equivalence and tamper controls BEFORE any real N1. Exact 256 targets × 4 folds × 6 rungs, four masking policies, frozen B4 seed `1267387626254385975` (exact int), RIDGE8@5% decision-driving precision, fixed thresholds; no target/policy/seed reroll.
6. Publish separate reviewable code/evidence PR(s) and refreshed handoff; no silent merges, no outcome promotion. If any physical/digest/array check fails, stop and report exact first error and role.

## Required end state before external experimental approval
`AUDIT_B_N1=UNOPENED; MASKS=NONE; BURDEN=NOT_RUN; RARE_TAIL_MOLECULAR=UNOPENED; TD60=UNEXECUTED; PATHOLOGY_DEV_SEALED=UNOPENED; D_SHARED_G5=UNOPENED; TRAINING=OFF`.

V19 branch contains #67 diagnostic code + THESE DOCS ONLY. It does NOT contain #63 crash-safe code or #66 Windows code; those are referenced independent PRs. No physical corrected heavy NPZ, 30GB+ Level-4 count blocks or Windows-only log bytes were uploaded to GitHub.