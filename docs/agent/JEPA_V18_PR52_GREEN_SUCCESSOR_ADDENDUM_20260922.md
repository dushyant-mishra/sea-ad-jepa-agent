# V18 correction/addendum — PR #52 test-only successor verified green

**Latest observed target successor:** PR #52 `target/v5-full104-rare-tail-molecular-ci-fix-20260922` (confirm live branch name on GitHub), exact head `213552a46903cd92b8a0338df185f0fce9655832`, based directly on original PR #51 `f8718b66f1c8bf39787408582fbbc85d5ada97d5`. Exact-head target-semantics Actions run `35739933731` **SUCCESS**, including **Fail closed on skipped regression tests SUCCESS**. This is a **test-only** correction touching exactly:
- `tests/test_v5_full104_rare_tail_molecular_execution_contract_v1.py`
- `tests/test_v5_full104_rare_tail_molecular_primitives_v1.py`
- `tests/test_v5_full104_rare_tail_molecular_runner_v1.py`

The previous original PR #51 head remains **RED** (420 passed, 3 failed) and is **superseded for execution readiness by the green PR #52 descendant**; do not claim #51 itself became green or that PR #52 was merged. The three historical failures and exact explanations remain in the main V18 handoff as the audit record. PR #52's only changed files are the three tests, so the proposed molecular authority and contract **source/semantic bytes are inherited unchanged from PR #51**, subject to independent fresh SHA validation before a physical run.

**Scientific status remains unchanged:** rare-tail metadata structural PASS-like `STRUCTURALLY_POSSIBLE__MOLECULAR_ESTIMABILITY_UNPROVEN`; molecular outcome **UNOPENED**, no expression/count matrix accessed by the preflight, TD60 **UNEXECUTED**, training **OFF**. A green test-only CI successor is permission to independently red-team the production runner and prepare a **read-only molecular runtime preflight**, NOT permission to execute the molecular outcome.

**Next-chat target priority:** start from PR #52 exact green head (or inspect a later successor). Independently check test-only diff, production all-42-operator fail-closed gate, two-panel frozen hashes, all 24 source×fold×panel cases, 64 matched-null replicates, zero-slack NPH52 folds 1–3, q95 nearest-half/64-triplet cap, and authority/contract semantic hashes. Then create a no-RNA-read runtime preflight receipt on a new evidence branch. Do not open X/Y/Z outcomes until that receipt has been reviewed.

**Cross-branch warning:** PR #50 green masking code and PR #52 green target code are **parallel**, not merged together in this docs-only handoff. Freeze source hashes per branch and integrate only after reviewing exact ancestry/path overlaps.

At this V18 addendum: **N1 unopened; masks none; burden not run; molecular outcome unopened; TD60 unopened; pathology/DEV/SEALED unopened; D_shared/G5 unopened; training off.**
