# F1 QID paired-wrong authority lineage review — 2026-09-11

Status: `UNRESOLVED_HISTORICAL_AUTHORITY_GAP__NOT_PROVEN_ACTIVE_V5_TRAINER_BUG`

## Finding

Historical F1 code names a quantity `paired_wrong_similarity`, but the real producer computes it from the matched-null student state rather than from a separately specified wrong-query pairing.

Reviewed historical repair commit:

`dd078625c3537f5ff2c3f8c0b382ba2803b24ee2`

Relevant files:

- `scripts/v4/contextual_target_f1_preflight_executor_v1.py`
- `scripts/v4/f1_real_producer_v1.py`
- `docs/agent/F1_EA35_REPAIR_RESPONSE_20260907.md`

The executor defines:

`qid_margin = own_similarity - paired_wrong_similarity`

and the producer constructs:

`own = cosine(S_correct_contextual, T_true_contextual)`

`wrong = cosine(S_null_contextual, T_true_contextual)`

then passes `wrong` as `paired_wrong_similarity`.

The same repair lineage describes `S_null` as a matched-null intervention that substitutes normalized values from a frozen donor-distinct source row while retaining recipient physical/evidence/query/operator/source identity. That is a matched-null state intervention. It is not, by itself, an authoritative definition of a paired wrong query.

## Authority question

No frozen authority has yet been recovered that states:

> For QID, the paired wrong query is defined to be the matched-null student generated by the donor-distinct matched-null source map.

Until such authority is recovered, do not silently treat the matched-null arm as a paired-wrong-query arm.

## Required resolution

Exactly one of these must occur before QID is used as production qualification evidence:

1. recover an older frozen authority that explicitly defines the paired-wrong mapping and prove the historical implementation matches it; or
2. prospectively redesign/freeze QID with an explicit deterministic wrong-query pairing rule, independent provenance, and adversarial tests.

Do not invent a pairing from available data after seeing outcomes.

## V5 impact traced so far

Live V5 planning head reviewed:

`planning/v5-full-population-cheat-proofing-20260909` @ `1de20b1c222c7fb27fcef5ec1a4b798d5b26a534`

The canonical `src/sea_ad_jepa/v4/teacher_student_runtime.py` training/update path reviewed at that head does not reference `qid` or `matched_null`, and `docs/agent/TEACHER_STUDENT_V5_DATA_FIRST_DESIGN_20260908.md` defines V5 training targets in terms of donor-weighted block-JEPA and relational losses rather than QID.

Therefore this review does **not** currently classify the historical QID alias as an active V5 optimizer-path defect.

Remaining lineage obligation: audit every V5 qualification/anti-cheat evidence consumer for inherited F1 QID claims. If any V5 gate consumes historical `qid_margin`/`qid_win` or treats matched-null similarity as wrong-query discrimination, that gate must fail closed until the paired-wrong authority question is resolved.

## Frozen conclusion

`MATCHED_NULL_CONTROL != PAIRED_WRONG_QUERY_CONTROL` unless an explicit authority proves the equivalence for the QID estimand.
