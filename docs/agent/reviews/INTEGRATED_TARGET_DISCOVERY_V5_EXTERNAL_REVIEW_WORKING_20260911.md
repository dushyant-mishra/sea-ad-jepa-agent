# Integrated Target Discovery → V5 External Review — Working Ledger

Date: 2026-09-11
Status: IN_PROGRESS_NO_GO
Reviewer scope: raw discovery lineage through V5 optimizer authorization

This ledger is intentionally prospective and incomplete until exact-head verification closes. It records only reviewer-established findings and cleared concerns.

## Cleared or materially reduced concerns

1. Frozen V20 remains immutable and is not to be modified by this review.
2. V20 discovery donor/gene role assignment is deterministic and not selected by downstream AT8 magnitude.
3. V20 LOODO preprocessing/ridge quantities inspected by the reviewer are recomputed within the legal training fold rather than obviously learned globally across all discovery donors.
4. V20 target/provenance machinery substantially binds raw expression/address/cell/donor identities and learned target parameters.

## Confirmed blockers

1. V21 malformed/nonfinite nuisance input can be absorbed as scientific non-estimability instead of failing structurally invalid.
2. V21 decision-capable power logic accepts caller-supplied score arrays and therefore is not structurally coupled to the required 28-fold nested OOF provenance.
3. The current Stage-C ridge refinement contract/implementation requires the center to remain the winner and therefore cannot genuinely recenter/refine away from the coarse anchor.
4. The `t/sqrt(n)` → `sqrt(12)` noncentral-t power projection is not yet calibrated to the exact final confirmatory test and cannot support a production ≥80% power guarantee as written.
5. The 28-LODO jackknife minimum is an empirical influence/worst-observed-omission sensitivity statistic, not a formal lower confidence bound.
6. S0–S4 selection currently lacks a complete hash-bound production measurement artifact that proves the scalar evidence was generated from the declared discovery lineage/common core/thinning/ridge-stability pipeline.
7. A distinct V21 46-development-donor freeze/serialization authority is required; the frozen V20 28-donor package must not be reused as if it represented the future 46-donor refit.
8. V5 pre-execution authority does not yet close a single immutable qualified-target-package-root → teacher-target → actual optimizer update lineage.
9. V5 still has open anti-shortcut qualification items: asymmetric-zero cosine edge handling, actual frozen-gate execution over deterministic controls, donor-held-out nuisance-recovery attacks, deliberate identity/same-cell/shared-view/lookup/duplicate/technical-only/corrupted-biology attacks, and exact-head qualification evidence.
10. FULL104 expression block/location identity remains a separate runtime blocker; TRAIN cache closure is not a substitute.

## Standing prohibitions

- No S0–S4 biological selection yet.
- No fresh `reader_validation` or `reader_oracle` opening.
- No production training.
- No modification of frozen V20.

## Repair authority

Implementation follows:
- `docs/superpowers/specs/2026-09-11-integrated-target-discovery-v5-repair-design.md`
- `docs/superpowers/plans/2026-09-11-integrated-target-discovery-v5-repair-plan.md`

A final verdict will replace this working status only after the repair branch is re-reviewed and verified.
