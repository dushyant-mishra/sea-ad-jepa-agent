# V26 104-donor linear baseline — independent adversarial successor
Date: 2026-09-25. Base: PR #136 @ ac3088df739c4e1f0d964d5d22f8d56727435eb4. This review changes ONLY the four existing PR136 files and this new audit. No current V5 training/ETL source, raw FULL104 binary, older 94-donor recovery, GPU execution, protected partitions, PR135 sampler or PR132 pass1-bridge code modified.

## Source/history reconciliation and anti-duplication
- PR #133 correctly concluded ORIGINAL selected 94-donor operator-centred LODO cannot presently replay: the historical bind_population.npz, screen_out.npz and final_manifest.csv are absent and their ephemeral producer scripts are missing. Do not regenerate proxy files and claim to reproduce the old R². The PR136 comparator is a NEW 104-reader-fit source-conditional donor-uniform uncentred VALUE_ONLY probe with different weights, population and reference SST.
- PR #135 owns prospective deterministic donor-uniform sampler and current-V5 development diagnostic; PR #129 owns one-use optimizer receipts; PRs #130/#132 own reader metadata and the exact donor-count bridge; PR #120 and PR #124 own all-104 physical Level4 count and source binding; PRs #131/#134/#137 own perturbation and CRISPRbrain work. NO overlapping reimplementation in this branch.
- Upstream 4,553,407×512 V0/V1 arrays' exact-byte hashes and pass1 immutable hash are frozen. The existing V5 full104_pass1_physical_binding_v1 verifies pass1→Level4 physical metadata and counts when run, but DOES NOT by itself authenticate V0/V1 selection_row order. PR136 expressly requires separate independent view-row attestation before scientific promotion. No FULL104 physical data mounted or analyzed in this review.

## Findings actually fixed in this successor

F1 NUMERICAL HIGH: the original closed-form `score_moments` allowed large positive/negative quadratic terms to cancel to ~0 when targets have a large offset; `max(0, sse)` could then fabricate perfect R². Added a conservative 64-epsilon bound on EACH SSE/SST quadratic term sum, refusing `SCORE_NUMERICALLY_UNRESOLVED` rather than returning apparently perfect fits. A hostile 1e9-offset positive fixture proves the rejection. A future compensated/centered-moment estimator could handle these cases; the present code fails closed and does NOT claim arbitrary-range numerical support. Normal-range direct-score tests remain unchanged.

F2 INPUT/AUTHORITY HIGH: a reviewer-supplied SHA of the physical receipt alone did not ensure its internal FULL104 block-manifest, registry or observation-state SHA roots, all six semantic digests, canonical source ordering, 42×41,238 geometry or terminal-outcome status matched the current physical pass1 verifier. A different JSON could be rehashed and passed through a careless external review. Added explicit consistency checks against the independently frozen physical-verifier roots, exact named semantic SHA syntax, source_names, geometry, and `terminal_masking_outcomes_inspected=false`. Six separate tampering tests rehash internally corrupted synthetic receipts; they MUST still fail. No test-generated receipt is evidence of physical input.

F3 INPUT/SPILLOVER MEDIUM: `gather_moments` indexed donor_ids directly by code without rejecting negative/out-of-range codes or object-valued IDs. The top-level `run` validated source codes but the helper was still independently callable. Added first-use helper validation; tests inject negative index, out-of-range index, object donor IDs. Prevent a negative index silently mapping to the last valid donor.

F4 TIME-OF-CHECK MEDIUM: run verified pass1 and V0/V1 SHA once, then reopened/mmaped file paths. Persistent on-disk replacement between initial hash and use could produce a result tied to the old hash. Rehash all three named inputs AFTER scoring and BEFORE publishing immutable receipt. A tiny synthetic run with a synthetic second-verification failure proves no receipt is written after a changed source. Remaining short-lived swap-then-swap-back / kernel-level race requires production operator controls; an independent view-row semantic attestation remains required. This incurs ~18.7GB extra sequential view reads, NOT a duplicate 8,915-block raw audit.

## Executed verification and status
- Original PR136's 14 tests remain; this successor adds ten adverse/positive fixture cases (including six distinct hash-consistent but semantically corrupted receipts). Local independent Python pytest: 24 passed, zero skipped. CI has been strengthened to enforce **exactly 24 tests, zero failed/errors/skipped** on the successor PR head. Verify the actual GitHub run before promoting CI status; do not report a submitted workflow as executed.
- PHYSICAL FULL104 LODO: NOT EXECUTED, R² UNMEASURED. Historical 94-donor documented prior R² remains NOT REPLAYED. The source physical pass1→Level4 receipt and independently reviewed V0/V1 row-order attestation remain mandatory. This review does not open reader_validation, oracle, 24+24 foundation heldouts, Siletti, pathology or D_shared.
- A diagnostic JEPA trained on ALL reader_fit donors is NOT donor-held-out versus this fold-refitted ridge. Do not compare raw R² as a head-to-head; use matching fold-retrained JEPA or explicitly distinguish evaluation settings.

## Immediate next actions without overlap
1. Independent review of this successor and GitHub zero-skip CI; do not merge directly into main without its PR136 base lineage.
2. If existing full pass1→Level4 evidence and V0/V1 selection_row certificates become physically available on the GPU machine, run the NEW 104-donor comparator once, attach new small receipt + executable commit + full SHA. The historical 94-donor scratchpad search is separate and should not block it.
3. Keep first real V5 diagnostic with Claude/other chat (#135) as critical path. Never run all-104 raw Level4 checks again merely for this new linear comparator.

## Additional provenance red-team: A_full/B_full are NOT the V0/V1 inputs

The authentic published Level-4 feature-matrix manifest at `docs/history/full104_v014_20260826/03_phase2_state_derivation_v1/feature_matrix_level4/PHASE2_FEATURE_MATRIX_MANIFEST.csv` records:
- `A_full.npy`: SHA256 `ea60949152369d4aef28dff0e77230854255a45b0535400b7d7fc07798956aa7`;
- `B_full.npy`: SHA256 `11076b973e14e6b9d6bc232db9b1ca7d7e9e23c784a5e5db294f993bbf54af02`;
- `PHASE2_FEATURE_ROWS.csv`: SHA256 `9c734945925c04dd7b108d0f37e39d9ea49687b4701ebce6b94b01c2ed09ac59`;
- `ASSEMBLY_SEEN.npy`: SHA256 `0339d2e79599419f369d78cddf14448019f89476eb674000437b72a1b4fb640e`.

But the FULL104 disjoint-view substrate consumed by the original September 15 Layer-2 audit and the NEW PR136 comparator instead has `V0_full.npy` SHA256 `3b3f102c6767727ca4ab56832f8e70baf203676d6b65973c42903b22b6d56ada` and `V1_full.npy` SHA256 `c41df46d842d643f04566b8523a8aa711fa54bec1c836e0b394c0146017f231c`. **Neither V0 nor V1 is byte-identical to either Level-4 A_full/B_full file.** A superficial claim that the published `PHASE2_FEATURE_ROWS.csv` automatically binds V0/V1 selection-row order would be INVALID. The historic V0/V1 builder/rebuild-to-source lineage and the physical V0/V1 selection-row map must be authenticated separately. The historical Layer-2 PR133 documents that the scratchpad `rebuild.py` and `screen.py` producers for its 94-donor derived sample have disappeared; do not assume those scripts are or are not the producers of the full V0/V1 files without further evidence.

This mismatch is NOT proof that the V0/V1 rows are wrong: disjoint molecular views are allowed to differ in content from A/B. It is proof that **whole-array SHA + a different upstream manifest alone cannot certify matching row semantics.** The comparator stays `NEW_DEVELOPMENT_BASELINE_NOT_HISTORICAL_REPLAY` and its existing `view_selection_row_lineage=MUST_BE_INDEPENDENTLY_ATTESTED_BEFORE_SCIENTIFIC_PROMOTION` cannot be cleared by repointing a manifest or rehashing the same arrays. This audit did not consume the 18.7GB arrays or protected data. No new 8,915-block raw scan is warranted until the specific V0/V1 derivation path is identified.
